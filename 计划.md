# 毕业装备面板 —— 开发计划与进度

> **这个文件是我（助手）每轮自动继续时的第一读物。**
> 每轮开始：读这里 → 找第一个未完成的任务 → 做 → 回来更新状态和日志。
> 每轮结束：进度必须写回这个文件，否则下一轮等于失忆。

---

## 硬约束（每轮都要遵守，不许自己放宽）

1. **不 push、不打 tag、不发 Release。** 版本号锁在 `1.14.0`。发布等用户通知。
2. **只在分支 `feat/bis-panel` 上提交。** `main` 停在 `08a7e5d`，远端也是。
3. **不凭记忆手打中文游戏名词。** 只用从本机 SavedVariables 或插件 locale 文件里
   复制出来的字符串。拿不到就留英文并记在「待用户提供」里。
4. **不联网传任何本机数据。** 只读取公开信息，且必须走代理 `http://127.0.0.1:7897`。
5. **临时脚本跑完即删**（`_*.js` / `_*.py`）。留下来的必须是 `tools/` 或 `app/` 里的正式文件。
6. **每次改完代码跑 `node tools\run-tests.js`，全绿才算完成一步。**
   它跑三个测试套件 + 渲染检查，退出码非 0 就是有问题。
7. 不确定要走哪条路的决策，**不自己拍**，写进「待用户决定」。
8. **不要再往 README 里加东西。** 用户第 13 轮明确说过：「不要给 Readme 写那么多东西，
   有毛病吗写那么多，不要写 Readme」。所以：
   - 新功能**不写** README 章节。改代码就只改代码。
   - 只在用户明确要求时才动 README。
   - 「顺手把 README 补上」不是好习惯，是违反约束。之前把它堆到 1416 行，
     其中绝大部分是我自己的实测长文，用户并没有要。
9. **不要把来源插件的数据/名字写进 README。** 同一轮的第二条指示。
   代码注释里说明数据来源没问题（那是给维护者的），但 README 里不要写。

---

## 已查明的事实（不要重复调查）

### 图标（**已解决**，见 A 组任务）
- 本机 `E:\World of Warcraft\_retail_\Interface\Icons\` 有 **22786 个散装 .blp**，共 185 MB。
  全部 6676 字节 = 64×64 BLP2 / DXT5 / 带 mipmap。文件时间 **2024-08-18**。
- 这是一份**旧的、不完整的**提取：`inv_10* / inv_11* / inv_12*` 的图标**一个都没有**（各 0 个），
  是 BFA 前后的旧图标包。**当前赛季装备的图标不可能从这里拿到**，这条路对装备是死的。
- `tools/blp.js` 已写好并验证：BLP2 → PNG，零依赖，出图正确（已肉眼确认宝石和食物两张）。
  现在没有用到它 —— 留着，因为它是唯一能读本机客户端图标的办法，将来若要走 CASC 提取会用上。
- **网络可达性实测（这台机器，中国大陆）**：
  - `render.worldofwarcraft.com/us/icons/56/<name>.jpg` —— **直连 200，不用代理**。只开放 18 / 56 两种尺寸，64 是 403。
  - `wow.zamimg.com/images/wow/icons/large/<name>.jpg` —— 直连 403，**走代理 200**。
  - `nether.wowhead.com/tooltip/item/<id>` —— 走代理 200，返回 JSON，带 **`icon` + `quality` + `ilvl`**。
  - 暴雪 CDN **缺当前赛季新图标**（烈毒之渊那批 `*_ulatek_d_01` 在 us/eu/tw/cn 四个区全 403），
    这 158 个只有 zamimg 有。所以下载必须两个源按顺序试。
  - zamimg 对不存在的名字正确返回 404（试过乱名），所以「下到了」就是真图，不是占位。
- **itemId → 图标名 + 品质已经解决**：`tools/fetch-icons.js` 按 itemId 查 wowhead tooltip，
  产物 `app/item-icons.js`（`AE_ITEM_ICONS` / `AE_ITEM_QUALITY`）+ `app/icons/` 462 个 jpg（约 1 MB）。
  568 个 itemId **100% 覆盖**，0 断链，0 孤儿。
- **品质分布 `{1:14, 3:54, 4:500}`** —— 「BiS 一定是紫装」是错的：附魔卷轴是蓝的、合剂是白的。
  原来的代码硬写 `qualityColors[4]`，把这 68 件都染错了。
- 消耗品用 **BisData 自带的图标名**，不用 wowhead 的：wowhead 对同一件东西可能给出后缀带 `-`
  的变体名（`241324` → `..._white-`，`241326` → `..._red--`，两张图 zamimg 上都真实存在、md5 不同、
  都是合法合剂图标）。客户端那份更权威。

### 数据质量审计（BisData 0.5.78-live / updatedAt 2026-08-29 / addonVersion 0.64.1）
对得上的：
- 8 个大秘境副本和本机赛季 18 完全一致（BisData 用 journalInstanceID，模型用 challengeModeID，两套编号同 8 个副本）。
- 团本是当前的烈毒之渊（instanceId 1320）。
- 66 件低 ID 装备（158366~159255）来自塞塔里斯神庙/诸王之眠/红玉新生法池 —— 旧副本进 M+ 池
  仍掉原物品，**不是脏数据**。
- 每部位 `usagePct` 之和从不超过 100%（最大 100.1），同组内无重复 itemId，戒指 11/12 只有 0/80 组重复。

问题（这些是「数据可能很差」的具体证据）：
- **没有任何样本量字段。** 72% 背后是 5 个人还是 500 个人，数据里查不到。
  （已确认原始 Lua 就没有，不是我的生成器丢的。`count` 的 812 次命中全是 `encounterId` 被 grep 截断。）
- **206 / 1264 个部位组的使用率之和不到 50%**，中位数只有 72.9% —— 尾部被截断了，
  剩下那几十个百分点穿的是什么，数据里没有。
- **`weights`（属性权重）是硬编码的**，40 个专精只有 38 种组合，取值全部量化到 0.1
  （0.3~1.0），不是模拟出来的。
- **`specRawToCN` 只按专精英文名建键**：`DEATHKNIGHT/FROST` → 「冰法」（法师的名字），
  `PRESERVATION` / `DISCIPLINE` 根本没有行。已在 `labels.js` 用拒绝名单挡住。
- **饰品 13/14 有 1/80 组完全相同**（可疑但不严重）。
- **插件的 `sourceCategories` 只有 5 个键**（raid / mplus / crafted / world / other），
  但数据里实际用到 7 个 —— `tier`（292 行）和 `quest`（5 行）没有中文名。
  已在生成器里从数据自己的 `source` 文本补上（tier→「套装转换」、quest→「奇点声望任务」），
  并加了测试盯着。**不手写**：这两个词是从数据里抄的，不是我编的。
  （曾想用 shipped locale 里的短词「套装」，但只找到一处来源，而 `EllesmereUILocales`
  里 `L["Set"] = "设置"` —— 同一个英文词在另一个语境是完全不同的意思，
  说明按英文单词查 locale 不安全。）
- ~~我的 `gen-bis.js` 丢掉了 `bonusIDs`~~ **已处理，见下面「升级轨道」** —— 结论是不存原始
  `bonusIDs`，改存从它解出来的轨道。

### 升级轨道（B3 的产物，**已解决**）
- BisData 的 4066 行装备里 3963 行带 `bonusIDs`（89 种不同取值、591 种不同组合）。
- `bonusIDs` **不是按 itemId 静态的**：498 件里 **386 件有多套**（`158366` 一件就有 12 套），
  所以只能按行存。原样存 19.0 KB（直接）/ 24.6 KB（内化池+下标）。
- 插件里 `bonusIDs` **只有一个用途**：拼游戏内 `|Hitem:...` 链接，让游戏自己的 tooltip 显示
  正确装等（`GearInsight.lua` 里 8 处、`ui/DungeonGuide.lua` 1 处，全是这个）。网页没有这个
  机制，装等本来也已经在行里。**所以原样存是白花钱。**
- 有价值的是解开它。`AlterEgo/Data/UpgradeTracks.lua`（作者注明抄自 raidbots `bonuses.json`）
  给出赛季 17/18 的 10 条轨道 × 6 级 = 60 个 bonusID，数组下标就是升级等级。
  实测 **3601/3963 行（90.9%）能解出轨道，且没有一行同时命中两个轨道**。
- **轨道推不出来，只能存**：装等有 3 个值对应两种轨道（308 / 318 / 321），
  连 `(装等, 来源分类)` 都还有 **5/24 组歧义**（`321/raid` = 英雄 6/6 ×528 vs 神话 2/6 ×10）。
- 解不出的 362 行：`12214`/`12497`/`13751`/`8960`/`13836` 这类非轨道 bonusID（37 种），
  多是套装坯子和旧赛季物品。这些行不显示轨道徽章，不猜。
- 轨道中文名有**两处互相独立的 shipped zhCN locale**，完全一致：
  `EllesmereUILocales/zhCN.lua` 和 `ItemInfoOverlay/Locales/zhCN.lua` 都写「英雄」「神话」。
  生成器两边都读，不一致就 `exit(1)`。
  （`ItemInfoOverlay/Utils.lua` 的**代码注释**里把 Myth 写成「史诗」，那不是 locale，按 locale。）
- 体积：`app/bis-data.js` 186931 → 201010 字节（+13.7 KB，+7.5%）。

### 代码
- `app/bis.js` 的图标层已改完：`iconUrl()` 默认用包内 `app/icons/`，`iconBaseUrl` 变成
  纯覆盖项（留空 = 用包里的）；`itemQuality()` 出真品质；`iconImg()` 统一出 `<img>`，
  带 `onerror` 自摘，缺一张图不会留碎图标记。
- **测试 harness 现在是正式文件**：`tools/dom-stub.js`（DOM 桩）+ `tools/run-tests.js`（跑全部）。
  之前每轮都要重写一个临时 harness，那是纯浪费。
- 渲染冒烟测试已经是 `tools/run-tests.js` 的一部分（不再是临时脚本）：40 专精 × 2 视角 +
  天赋 3 类别共 83 次渲染，真实角色对照能命中 10/16（术士 vs DEMONOLOGY）。
- **行的格式变了**（B3）：`[itemId, ilvl, 使用率, 来源下标, 可升级上限, 轨道码]`。
  后两位可省，但**不许跳着省** —— 有轨道码就必须占住 `mx` 的位置（没有写 0），
  否则 `r[4]` 是 mx 还是轨道码得看长度猜。`mx` 从 `undefined` 变成了 `0`。
- **两个生成器报的 KB 数曾经是错的**：用 `str.length`（字符数）当字节数，中文一个字符 3 字节，
  `bis-data.js` 少报 15 KB、源文件少报 160 KB。已改成 `Buffer.byteLength`。

### 替代数据源调研（B4，**只调研，换不换是用户的决定**）

可达性（本机，中国大陆，代理 `http://127.0.0.1:7897`）：

| 站点 | 直连 | 走代理 | 结论 |
| --- | --- | --- | --- |
| archon.gg | 403 | 403 | **Cloudflare 挡住，两边都进不去** |
| warcraftlogs.com | 403 | 403 | **两边都进不去**（公开页也不行） |
| u.gg | 403 | 403 | 两边都进不去 |
| bloodmallet.com | 200 | 200 | 直连可用，有 JSON 接口 |
| raidbots.com/static/data/live/bonuses.json | 200 | 200 | 直连可用，1.76 MB JSON |
| wowhead.com | 403 | 200 | 只能走代理（已在用它查图标名） |
| murlok.io | 200 | 200 | 直连可用，但**只有 PvP** |
| wowanalyzer.com | 200 | 200 | 是「打完复盘」工具，不出 BiS 表 |

各家能给什么：

- **archon.gg / warcraftlogs**：本机根本访问不到，无法作为数据源。这一条把「换成
  更权威的使用率统计」这个方向基本堵死了 —— GearInsight 的数据本身就是从 WCL 抓的，
  而我在这台机器上抓不了。
- **murlok.io**：`/<职业>/<专精>/3v3` 全是竞技场。试了 `mythic-plus` / `mythic` /
  `raid` / `pve` 四个路径全 404。**PvP 站，替代不了。**
- **bloodmallet.com**：接口是 `/chart/get/<类型>/<战斗风格>/<职业>/<专精>`，返回 JSON。
  - **有样本量**：`simc_settings.iterations = 60000`、`target_error = 0.1`，
    还有 `metadata.SimulationCraft` / `bloodytools` 的 git hash 和时间戳。
    这正是 GearInsight 缺的东西。
  - **有 zhCN**：`translations[物品英文名].cn_CN`，62 个饰品**零缺失**，9 种语言。
  - **但性质不同**：它是 SimC 模拟出来的 DPS 数值（`data[名字][装等] = dps`，
    模拟了 15 个装等档位），**不是「多少人在用」**。
  - **覆盖面窄**：11 种数据类型里只有 5 种有数据（trinkets / secondary_distributions /
    races / potions / phials）；talents / tier_set / gems / enchantments / embellishments /
    food 全部返回 `{"status": "error", "message": "No standard chart with these values found."}`。
    战斗风格也只有 `castingpatchwerk` 有数据，另外 4 种全空。
  - **结论：能补饰品和副属性配比，替代不了全套部位。** 而且换过去等于把「实际在用」
    换成「理论最优」，是产品定位的改变，不是数据源的替换。
- **raidbots `bonuses.json`**：9742 条 bonusID 的权威解释，568 条带 `upgrade`。
  - **已用它独立验证我的轨道解码**：BisData 用到的 12 个轨道 bonusID，装等和 raidbots
    **12/12 完全一致**；赛季 18 那 30 个 ID 的 `upgrade.name` + `level` + `max` 和 AlterEgo 的表
    **30/30 一致**（`upgrade.fullName` 直接就是 `"Hero 6/6"`）。赛季 17 那 30 个在 raidbots 的
    **live** 数据里已经没有 `upgrade` 字段（只剩 `itemLevel`），所以「不一致 30」是
    live/历史的差别，不是错 —— 用装等验证仍然全对。
  - 也证明了解不出轨道的那 37 个 bonusID **确实不是轨道**：`6652`/`13667`/`13696` 是
    `serverside: true`，`8790`~`8793` 是 `craftedStats`，`13695` 是 `socket: 1`，
    `12384`/`13771` 是镶嵌效果，`8960` 是 `item_limit_category`。**不显示才是对的。**

顺带量出来的两件事（都还没做，见任务 B5 / B6）：

- **73 行带 `Sporefused: Myth`**（bonusID `13786`），AlterEgo 的 `upgradeBonusLabels` 里有这个
  英文名，但**全盘搜不到任何 zhCN 译名**。这 73 行现在完全没有轨道徽章。
- **1039 行 / 102 件装备带插槽 bonusID**。「插槽」的 zhCN 有来源（`Syndicator/Locales.lua`
  的 `L["KEYWORD_SOCKET"]`），但**本机没有插槽 bonusID 的清单** —— 那 116 个 ID 是从 raidbots
  的 JSON 里数出来的。要显示插槽就得让生成器多依赖一次 raidbots（打包时联网）。

（测量过程中的一个教训：第一版用 `/bonusIDs\s*=\s*\{[^}]*?\}/` 之类的正则扫行，
跨不过嵌套的 `stats = { … }`，只匹配到 480/3963 行，于是「0 行带 label」——
那是测量假象，不是结论。改成按行边界切分才得到真数字。）

### 天赋树的数据找到了（第 7 轮，**长期卡住的那件事解开了**）

之前的结论是「天赋树画不出来，缺的是树的结构，插件里根本没有」。**结构本身在外面有，
而且本机能访问。** 两个来源，都是直连 200、不需要代理：

| 来源 | 内容 | 大小 |
| --- | --- | --- |
| `raidbots.com/static/data/live/talents.json` | 40 个专精的完整树结构 | 3.2 MB |
| `wago.tools/db2/TraitDefinition/csv?locale=zhCN` | 天赋定义 → 名字 / SpellID | 830 KB |
| `wago.tools/db2/SpellName/csv?locale=zhCN` | SpellID → 中文法术名 | 10.0 MB |

**raidbots 的 `talents.json` 正是面板缺的东西。** 顶层是 40 个专精的数组，每个带
`traitTreeId / className / classId / specName / specId / classNodes / specNodes /
heroNodes / subTreeNodes / fullNodeOrder`。节点字段：
`id, name, type, posX, posY, maxRanks, entryNode, next, prev, entries[]`，
英雄节点多 `subTreeId / requiresNode / freeNode / freeLevel`。
`entries[]` 里有 `id, definitionId, maxRanks, type(active|passive), name, spellId, icon, index`。
**`posX/posY`、`next/prev`、`icon`、`maxRanks` 全都有** —— 就是 `TREE_FORMAT_DOC` 要的那些。
坐标范围实测 X 1500~14100 / Y 1500~7650。

**关键一步：`entries[].id` 就是 `talent-data.js` 里的 `entryID`。**
实测 40/40 专精 specId 都能对上，我的 3040 个 entryID 里 **3039 个（100.0%）**
在 raidbots 里存在，**38/40 个专精逐专精 100% 命中**。

**中文名 100% 有解，而且不是手写的。** 连接链 `entries[].definitionId → TraitDefinition.ID`
→ 优先 `OverrideName_lang`，否则 `SpellID → SpellName.Name_lang`：
3334 个不同 definitionId **全部 3334 个都拿到中文名**（6 个走 OverrideName、3328 个走 SpellName），
查不到 0、拿到但仍是英文 0。样例：`Rake → 斜掠`、`Frenzied Regeneration → 狂暴回复`、
`Power Nexus → 能量枢纽`。**这是暴雪自己的字符串表，比插件 locale 更权威。**

**体积实测（这是要不要做的关键）**：

- 节点条目 40 专精相加 4613，去重后 2893，**按规范化 key 去重 3110**（见下）
- 只留必需字段的 JSON **447.7 KB**；名字/图标做字典后 **378.4 KB**（和现有 `talent-data.js` 520 KB 同量级）
- **图标是真正的大头：2094 个不同图标，按现有 462 张平均 2.2 KB 估算约 4.58 MB**，
  其中只有 3 张已经在包里。这比现在整个发布包（1.5 MB）大三倍。

**职业树能不能按职业共享：不能，但原因不是数据不一致。** 13 个职业里 9 个的三个专精
职业树「不同」，但按节点 id 集合比对后发现**字段差异 0 处** —— 差的只是
**哪些节点存在**（部分节点按专精开放）。所以正确做法是**全局 intern 节点**
（4613 → 3110 个不同节点），每个专精只存节点 id 列表。英雄子树 41 个，每个 14 节点。

**顺手用 `talents.json` 验出一个上游真 bug。**
`WARRIOR/PROTECTION` 的 171 个 entryID 里有 **49 个不属于防护树**：35 个是
武器（Arms）专精树的节点（Mortal Strike / Overpower / Die by the Sword / Tactician），
14 个属于英雄子树 60 = **Slayer**，而防护只能选 Colossus + Mountain Thane。
定位到是**单独一条玩家记录**（`mplusFarm / 红玉新生法池 / 绝密宝宝`，越界 49 项，
120 条记录里就这 1 条），即那一条其实是个武器战。同样地 `MONK/MISTWEAVER`
有 4/85 套越界、1 个 entryID（125051）raidbots 里完全没有（只出现在 1/86 套里）。
**38/40 个专精完全干净。** 这是 WCL 上游认错专精，不是我的转换问题
（转换本身早就验过 4797 条 0 不一致）。

---

## 任务列表

状态：`[ ]` 未做　`[~]` 进行中　`[x]` 完成　`[!]` 卡住（写明卡在哪）

### A. 图标本地化 —— **已完成**

- [x] **A1** `tools/blp.js`：BLP2 → PNG 解码器，零依赖。已验证出真图。
      （最后没用上 —— 散装目录里没有任何 10.x/11.x/12.x 图标，当前赛季没法靠它。文件留着备用。）
- [x] **A2** ~~gen-icons.js 从散装目录转~~ → **改为 `tools/fetch-icons.js`**：按 itemId 从 wowhead 查
      图标名 + 品质，再从暴雪 CDN / zamimg 两个源下图到 `app/icons/`。带断点续传
      （`tools/.icon-cache.json`）、限流、重试、`--limit` 试跑、`--names-only` / `--images-only`。
- [x] **A3** `app/bis.js` 接本地图标：装备 / 宝石 / 附魔 / 消耗品四处全部出图，
      品质改用真值，`iconBaseUrl` 降级为覆盖项。
- [x] **A4** 设置面板 + `settings.js` 注释 + README 三处过时说法全改（原本写着「装备图标显示不了」）。
- [x] **A5** ~~游戏内插件导 fileID~~ **不需要了**。wowhead tooltip 直接给图标名，绕开了
      fileID → 文件名 那一环（那一环需要 50 MB 的社区 listfile）。
- [x] **A6** 两个图标源的分工和原因已记在 `tools/fetch-icons.js` 文件头 + README。

### B. 数据源可替换（为「换数据源」铺路）

- [x] **B1** ~~schema 文档 + 校验器~~ **已完成**：`tools/verify-bis-data.js`。
      **schema 就写在校验器里**（`SCHEMA` / `SPEC_SCHEMA` / `SUB_KEYS` / 行格式注释），
      不另写 .md —— 另写一份迟早和代码脱节，写在校验器里它每次跑都被执行一遍。
      2016 项检查，`--data` 可以指向任意候选文件。已接进 `tools/run-tests.js`。
      **变异测试 31/31**：第一版漏了「`target` 缺 `farm`」（schema 里只写 `type: 'obj'`，
      子键要求只存在于散文描述里 → 补了 `SUB_KEYS` 逐个验子键，检查项 1296 → 2016）。
- [x] **B2** ~~数据质量审计写进 README + 面板显示样本量缺失~~ **已完成**：
      README 新增「这份数据有多可信」一节（对得上的 / 靠不住的两张清单，全是实测数字），
      已知限制里补了样本量那条，功能清单里加了覆盖率徽章。
      面板侧的做法不是再加一句免责声明，而是**每个部位组显示「记录 xx%」徽章**
      （= 列出的几件使用率之和，不到 50% 标警告色，tooltip 说明剩下的百分比数据里没有
      以及本数据不带样本量）。1264 个部位组一一对应，渲染检查里有硬断言。
      顺手用掉了 `.slot-head .tag` / `.tag.ok` / `.tag.no` 三条**从来没被创建过的死 CSS 规则**。
- [x] **B3** ~~修 `gen-bis.js` 丢 `bonusIDs` 的问题~~ **已完成，但不是照抄回来**：
      查清 `bonusIDs` 在插件里只用于拼游戏内 `|Hitem:` 链接（网页没这机制，装等本来就在行里），
      原样存 19 KB 白花。改成**解成升级轨道**（「英雄 6/6」）存 —— 见上面的事实小节。
      行格式变成 `[itemId, ilvl, 使用率, 来源下标, mx, 轨道码]`，尾部 0 可省但不许跳着省。
- [x] **B4** ~~探测替代数据源~~ **已完成，结果记在上面「替代数据源调研」**。一句话总结：
      **没有可用的替代品**。archon.gg / warcraftlogs / u.gg 本机 403（Cloudflare，直连和代理都不行），
      murlok.io 只有 PvP，bloodmallet 是模拟 DPS 而不是使用率、且只覆盖饰品和副属性配比，
      raidbots 只有 bonusID 词典。**换数据源这条路在本机是堵的**，所以重点应该放在
      「把现有数据的缺陷说清楚」而不是「换一家」。
      副产品：用 raidbots 独立验证了轨道解码（装等 12/12、名字+等级 30/30 全对）。
- [ ] **B5** 73 行 `Sporefused: Myth`（bonusID 13786）现在没有轨道徽章。AlterEgo 的
      `upgradeBonusLabels` 有英文名，**但全盘没有 zhCN 译名**，所以卡在「不许手写中文」这条规矩上。
      → 进「待用户提供」。
- [ ] **B6** 1039 行 / 102 件装备带插槽 bonusID，可以显示「插槽」标记（zhCN 有来源）。
      但插槽 bonusID 清单只有 raidbots 有，本机没有 —— 要做就得让生成器多联一次网。
      → 进「待用户决定」。

### C. 质量与收尾

- [x] **C1** ~~重建渲染冒烟测试~~ **已完成**：`tools/run-tests.js` 里的渲染检查，83 次渲染。
      断言是硬的（占位块必须 0、`<img>` 必须过 5000、不得引用不存在的文件）——
      第一版只统计不断言，变异测试（`AE_ITEM_ICONS = {}`）下占位块 0 → 3963 它照样返回「通过」。
- [x] **C2** `app/bis-tests.js` 补了 6 项图标测试（覆盖率、名字字符集、品质取值范围、
      宝石/附魔/消耗品覆盖）。**127 项全绿**（33 解析 / 66 模型 / 28 毕业装备）。
- [x] **C3** README 更新：图标的来源和两个 CDN 的分工、品质字段的发现、换赛季要多跑一步
      `fetch-icons.js`、目录树新文件、测试项数 82 → 127、已知限制里两条过时说法。
- [ ] **C4** 全部测试绿，工作区干净，提交到 `feat/bis-panel`。（每轮都过一遍，不算完成）
- [x] **C7**（本轮新增）**实测发布包内容**，不靠读脚本推断。`tools\build-release.ps1 -SkipBuild`
      产出 1497.9 KB / 502 个条目：
      - `app/icons/` **462 个都在**，解压后 1035.8 KB、压缩后 **987.6 KB**（jpg 压不动），
        占整个包的 **66%** —— 这是「待用户决定」第 4 条要的数字。
      - `$dropFromPkg` 四项全部排除干净（`launcher.ico` / `.icon-cache.json` /
        `dom-stub.js` / `run-tests.js`）。
      - 该在的都在，包括新加的 `tools/verify-bis-data.js`。
      - `data/` 里只有构建时生成的 `说明.txt`（内容是「这个文件夹可以删」的提示），
        没有任何个人数据；`.git` / 临时文件 / 旧 zip 一个都没进。
      - 版本号仍是 **1.14.0**，脚本不联网、不碰 git、不发布。
      **顺手记一个自己犯的错**：第一版检查脚本把所有条目都报成「缺」——
      `Compress-Archive` 写进中央目录的条目名用的是**反斜杠**，而我拿 `/` 去匹配。
      是检查脚本错了，不是包错了。解析 zip 时先把 `\` 归一成 `/` 才对。
- [x] **C5** 修两个生成器把字符数当字节数报的 bug：`out.length` 对中文差 3 倍，
      `bis-data.js` 一直被报成 182.5 KB，实际 196.3 KB。改用 `Buffer.byteLength`。
- [x] **C6**（做 B1 时顺带抓到的两个真 bug）
      1. **来源徽章 292 行显示英文 `tier`、5 行显示 `quest`**：插件自己的 `sourceCategories`
         只有 5 个键（raid/mplus/crafted/world/other），而数据里实际用了 7 个分类。
         中文名不许手写，所以让生成器**从数据自己的 `source` 文本补**，且只在该分类下
         `source` 唯一时才补（`tier` → 「套装转换」，`quest` → 「奇点声望任务」），
         不唯一就留英文并警告。加了测试盯住「每个用到的分类都有标签」。
         · 顺带否掉一个诱人的错解法：`Syndicator` 里有 `L["KEYWORD_SET"] = "套装"`，
           但 `EllesmereUILocales` 里 `L["Set"] = "设置"` —— 同一个英文词在另一个 locale
           里是完全不同的意思。**按通用词查 locale 不安全**，只能用数据自己的文本。
      2. **`.item .tag` 底样式整条是漏的**：CSS 里只有 `.item .tag.use` 和 `.item .tag.trk`，
         没有 `.item .tag` 本身；`.slot-head .tag` 是另一个祖先管不到。于是来源徽章的
         `border-width` 是 0，而 `bis.js` 里 `badge.style.borderColor = catColor(cat)`
         只设颜色 —— **等于给一条不存在的边框上色，徽章一直是裸文字**，而且没有
         `flex: none` / `nowrap`，6 个字的「奇点声望任务」会被压缩或折行撑高整行。
         补了底样式，`.use` / `.trk` 简化成只覆盖颜色。

### D. 天赋树 —— **已完成**（第 8 轮，提交 `ea5c83e` / `5101f5c` / `ee64a85`）

这是面板**最大的已知限制**，已经解决。D 组刻意设计成**不依赖图标**：
2094 张天赋图标实测 4.58 MB，是现在整个包的 3 倍，那件事仍在「待用户决定」第 6 条。
没有图标的树用「名字方块 + 连线 + 点数」照样能看懂点在哪。

- [x] **D1** `tools/fetch-talent-tree.js`（19 KB）→ `app/talent-tree.js` **415.3 KB**，
      中文名 **5387/5387 = 100.0%**，40 专精 / 2891 节点 / 41 子树 / 6579 连线。
      四个源缓存在 `tools/.talent-raw/`（gitignored，14 MB），
      **只把连接后的结果**存进 `tools/.talent-names.json`（271 KB，入库）——
      重新生成既不用 10 MB 的 CSV 也不用联网。`--refresh` / `--offline` / `--proxy`。
- [x] **D2** 节点全局 interning：4613 引用 → 2891 节点。
      **一致性守卫抓到我自己的错误设计**：把 `next`/`prev`/`freeNode` 也全局化时
      **133 个节点冲突**（同一个法师职业节点在奥术通向 108664、火焰 108656、冰霜 108655；
      血 DK 的某个职业节点是白给的而冰霜/邪恶的不是）。
      → **静态字段全局共享，拓扑（`edges` / `free`）按专精存。**
      `tools/verify-talent-tree.js`（22 KB）**76377 项检查**，变异测试 **30/30 全抓**。
- [x] **D3** 交叉验证 `app/talent-data.js`，并且**把两种诊断分开报**：
      「跨专精污染」= entryID 在树里但属于别的专精（WARRIOR/PROTECTION **1/77**）；
      「引用未知 entryID」= raidbots 里根本没有（MONK/MISTWEAVER **4/86**，entryID 125051）。
      两个数都和第 7 轮独立测出来的一致，38/40 专精干净。
- [x] **D4** `app/bis.js` 天赋页画树。**坐标没有除以 150** —— 计划原本那么写是错的：
      实测坐标最大公约数只有 10，还有「相差 10」的近重复坐标，直接缩放会把节点叠在一起。
      实测它本质是网格（职业树严格 600×600，专精树以 600 为主，英雄子树 5×5），
      所以**按容差聚类成列/行再摆网格**，容差 50~250 都试过，同格重叠恒为 0。
      节点上放**全名**而不是缩写：实测同一棵树内「前 2 字重名 496/3447」
      （「强化」既是强化法术吸取也是强化法术反制），缩写会主动骗人。
      顺手把面板里 39 个英文英雄天赋名换成中文（靠 `TraitSubTree` 的英文名连接，39/39 对上）。
- [x] **D5** `tools/run-tests.js` 加第 5 行「天赋树渲染」：40 个专精各画一遍 +
      3 个类别各画一遍，**硬断言** 树数 = 渲染次数 × 3、节点 ≥ 4000、点亮比例 40~95%、
      同一专精内节点 id 不许重复、中文名 0 例外、点数徽章格式。
      变异测试 **9/9 全抓**（含「专精树画成职业树」——
      这条一开始漏了，因为总数看着仍然合理，加了「节点 id 不重复」才抓住）。
      英雄天赋中文名另做 2 个变异，2/2 抓到。
- [x] **D6** README 那一节从「画不出来」改成「能画了，数据从哪来」，
      写清中文名来自暴雪 DB2（不是我翻的）、没有图标是刻意的选择、
      以及仍然做不到的三件事（导出串 / 点着改 / 上游 1 套脏数据）。
      同时更新了已知限制、功能列表、目录结构、换赛季步骤（现在是四步）、换数据源一节。

### E. 天赋导入串 —— **卡在一个只有用户能验的问题上**

第 10 轮开始查的。起因是想给天赋面板补上「和你自己的天赋比一比」，
结果先撞见一件更要紧的事：**README 里我自己写过的一句话是错的**。

- [x] **E1** 找到玩家自己的天赋数据在哪。`WTF` 下 `TalentLoadoutsEx.lua` 和
      `BtWLoadouts.lua` 存的就是**官方导入串**，本机 36 个文件里 54 个串。
      （AlterEgo 的存档里**完全没有**天赋字段，`talent` / `loadout` / `traits` 全 0 次，
      只有 `specID` 出现 125 次。所以「比一比」的数据只能从这两个插件来。）
- [x] **E2** 串头 **54/54 全解开**：8 位版本（全是 2）+ 16 位 specID + 128 位 `treeHash`，
      标准 base64、每字符内低位在前。`treeHash` 是**按职业树**的（防护 / 惩戒圣骑同一个，
      鲜血 / 邪恶死骑同一个，浩劫 / 复仇 DH 同一个）。
- [x] **E3** 纠正 README。原话「导出串要 `treeHash` 和游戏权威的节点顺序，公开数据里没有这两样」
      **一半是错的**：raidbots 的 `talents.json` 里就有 `fullNodeOrder`，
      13 个职业全查过 —— 同一职业内每个专精的这份顺序**完全相同**，
      且等于该职业各专精节点集合的并集（死骑 206 = 本专精 114 + 别专精 92 + 未知 0；
      德鲁伊多 2 个、圣骑多 1 个对不上）。缺的只有 `treeHash`。
- [x] **E4** `tools/decode-talent-string.js`（9 KB，永久工具，不是临时脚本）。
      `<串>` / `--scan` / `--verify` / `--flat` / `--rankbits N` / `--wtf` / `--limit`。
      中文名从 `app/talent-tree.js` 取，节点顺序从 `tools/.talent-raw/talents.json` 取。
- [x] **E5 解开了**（第 12 轮）。**解码器一直是对的**，五个「反面结论」查的全是错的方向 ——
      真正的原因是**串和树的版本不匹配**，而我一直用「自我一致性」当判据，那种检查
      永远看不出版本漂移。

      **破局的不是又一个假设，是找到了真值**：raider.io 的角色 profile
      （`/api/v1/characters/profile?fields=talents`）里，`talentLoadout` **同时**给出
      导入串 `loadout_text` 和它自己解出的节点表 `loadout`
      （`[{node:{id,…}, entryIndex, rank, grantedNode}]`）。串和答案来自同一个生产者，
      对不上就一定是我错。十一轮里我从来没有过这样一个外部判据。

      **版本假说的直接证据**（同一批 32 个角色，两种串放一起比）：
      赛季历史串（排行榜 `roster[].loadout`）干净 **3/32**，当前串（profile）干净 **27/32**，
      「历史脏 → 当前干净」**24 人**，反向 **0 人**。历史串是照着旧版天赋树编的。

      **对真值逐节点比对，两处真缺陷（都不是位流错位）**：
      · `subtree` 类型的英雄天赋选择节点在 raidbots 里**没有 `maxRanks` 字段**，
        我的「没读到 partial 位就取上限」拿到 `undefined`。真值一致给 1。改成退到 1 后
        rank 不符 **33 → 0**。
      · `entryIndex` 位流里根本没有：二选一节点的 2 位下标是真的，
        `tiered` 节点（同名三档）要按 `entries[].maxRanks` 累加推。补上后不符 **25 → 0**。

      **两类看着像错、实际不是错的残差**：
      · 「我多解出 37 个」——全是 `granted`（`purchased` 位为 0）节点，
        raider.io 的 `loadout` 数组**不列 granted 节点**。口径差异，不是解码差异。
      · `granted` 就是 `purchased` 位的反面：2406 个节点真值 **0 处不符**。

      **落地**（提交 `9cf9f8c`）：
      · `tools/fetch-talent-truth.js`（联网）→ `tools/talent-truth.json`（38.2 KB，已提交，
        32 条串 × 2406 个节点真值，不含角色名/服务器）。
      · `tools/verify-talent-decode.js`（**离线**，只读两份 committed 产物）→ 套件第 9 行
        「天赋串解码」，少解 0 / rank 0 / entryIndex 0 / granted 0。
      · 节点顺序表进了 committed 产物：`app/talent-tree.js` 新增 `nodeOrder`
        （**按职业**一份，13 个职业 2896 个 id，+18.2 KB → 433.5 KB）。
        生成时硬校验「同职业各专精的 `fullNodeOrder` 完全一致」（13/13 一致，0 分歧），
        所以按职业存一份不是省事，是有据。解码从此不依赖那 14 MB 的 gitignored 原始文件。
      · `tools/mutate-decode.js`（6 个变异，6/6）。

      **仍然没验证到的一处**（写在 `entryIndexOf` 的注释里）：32 份样本里 `tiered` 节点
      只出现过满级（31/31 都是 `rank=4 → entryIndex=2`，而 `maxRanks` 是 1/2/1，
      累加恰好落在下标 2），所以「按 maxRanks 累加」和「永远取最后一档」**分不开**。
      要分开得有一份没点满 tiered 的样本。二选一那一路是真验过的（下标直接来自位流）。

      下面这五条是**当时排除掉的方向**，留着记录我错在哪 —— 它们查的都是布局，
      而问题在版本：
      · **字段布局不对** —— 18 种布局全试（额外位有无 × 位宽 4/5/6 × 顺序 × 嵌套），
        最好的就是现在这个（嵌套 + 6 位），`--flat` 是 **0/54**，还引入 24~46 个点数超上限。
      · **版本错位** —— 「和该专精主流 hash 一致的串才对」被数据**反过来否掉**：
        一致的 5% 干净，不一致的 42%。
      · **节点顺序取错** —— 只留本专精节点或按 id 排序都是 **0/54**，全量 `fullNodeOrder` 最好。
      · **顺序只差几个节点** —— 按专精搜一组「幽灵节点」补丁（要求修好该专精**全部**串），
        补到 13 个仍然 0/N，10 个专精里只有 Devourer DH 不需要补丁。
      剩下的错误里最大一类是「二选一下标放在了没有二选一的节点上」（47/54），
      而那 47 个节点**全部是 `type=single entries=1`** —— 对齐正确的流里不可能出现这种事，
      所以它是**错了一位之后的下游垃圾**，不是独立的毛病。
      两个很强的线索（下一轮从这里接）：
      · **没有任何一个串是从第 0 个节点就错的** —— 第一个违规点最早在 4%，中位 16%，最晚 42%。
      · **同一专精的不同串，第一个违规点下标完全相同**（复原萨 8 串都是 35，
        复仇 DH 5 串都是 42，浩劫 DH 4 串都是 96），**连 `treeHash` 不同的也一样**。
        按内容漂移会散，下标恒定只能是顺序本身的系统性偏差。
      **教训（比结论重要）**：这五条全是「换一个我猜的参数，看干净率涨不涨」。
      干净率是我自己定的判据，用它验我自己的假设是循环论证。
      十一轮里唯一有效的一步，是去找**别人给出的正确答案**。
      下次再遇到「解不开的格式」，先问「谁手里有这个格式的正确解」，
      而不是「我还有哪个参数没试」。
- [x] **E6 已回答**（不用问用户了）：全 0 的 `treeHash` 游戏**认**。
      证据：raider.io 排行榜上 **99/99** 条 `loadout` 串的 `treeHash` 全是 0，
      而这些串就是给玩家粘进游戏用的。另外用户已经说了「导入导出串本来就有」，
      所以**根本不需要编码器**，这一项连带作废。

### F. 无障碍 —— **已完成一轮**（第 11 轮，提交 `8ede310` + `ec49426`）

第 11 轮开始查的。起因：README 里写「悬停节点给中文名 / 二选一 / 层级要求 / 点数」，
去核实这句话是不是真的，结果核出了一串**关于我自己的测量工具**的问题。

- [x] **F1** 核实 tooltip 确实能显示。`app/tip.js` 是委托到 document 上的，
      读的是 `data-tip`，天赋节点每个都带 —— 这句话是真的。
- [x] **F2** 修掉 `tools/dom-stub.js` 的**保真度缺口**。桩只有 `setAttribute`，
      不做「属性 → attribute」反射。而 `app/` 里到处是 `img.alt = ''`、`td.title = ...`
      这种**属性赋值**（`.title =` 有 48 处），在真浏览器里会反射成 attribute，在桩里不会。
      后果：我用桩量出「621 个 `<img>` 全都没有 alt」，**那是假的**，桩自己看不见而已。
      现在桩对 `id/alt/title/src/href/type/value/width/height/tabIndex` 做双向反射。
- [x] **F3** 用修好的桩重新量，面板本身比我以为的干净：
      `<img>` **全有 alt**（装饰性图标，`alt=""` 是正确写法，不是漏写）、
      `<button>` **全有可见文字**、带 `data-tip` 的元素 **2386/2386 都有可见文字**
      （所以 tooltip 是补充信息，不是元素的名字 —— 这一点很重要，
      否则就得上 `aria-label`，而那会覆盖可见文字，反倒违反 WCAG 的 Label in Name）。
- [x] **F4** 找到并修掉**一个真的缺陷**：天赋树画布是一堆绝对定位的 `<div>` 拼出来的图，
      **没有 role、没有说明**。读屏软件只会念到一串互不相干的天赋名。
      现在每个画布带 `role="group"` + 中文说明。**实测串**（鲜血死骑，从渲染里 dump 出来的，
      不是我照着代码念的）：「职业天赋，共 44 个天赋，点了 31 个，合计 35 点」。
      第一版写成「职业天赋**天赋树**，…」—— `title` 本身已经是「职业天赋」，
      后面再拼「天赋树」就重了。是 dump 出来才看见的，照着代码读看不出来。
      选 `group` 而不是 `img`：`role="img"` 会把子节点全藏起来，那些天赋中文名就读不到了。
- [x] **F5** 把这四条量化结果**升级成永久断言**（`run-tests.js` 第 6 行「无障碍」）：
      图标全有 alt、按钮全有名字、带 `data-tip` 的元素全有可见文字、画布全有 role + 说明。
      变异测试 **5/5 全抓**（含两个「把可见文字抽掉、只留 tooltip」）。
- [x] **F6** 纠正 `app/tip.js` 头注释里的两处错话：说「31 处调用点，加上面板」——
      实际 `.title =` 有 48 处，而**面板一处都没有**（面板直接写 `data-tip`，
      所以走不到 `title → aria-label` 那条镜像路径）。
- [x] **F7** 变异测试脚本**转成永久文件** `tools/mutate-a11y.js`。
      理由是账算出来的：变异脚本被 `rm -f _*` 吃掉过**三次**（第 9 轮丢了
      `_mutrender` / `_muthero` / `_muttree`，本轮丢了 `_muta11y`）。
      断言留在 `run-tests.js` 里，但**证明断言有效的那个东西**每次都得重写 ——
      而改了断言之后正是最需要立刻重跑它的时刻。它还会把「锚点失效」
      单独报出来并计入失败：一个没打上的变异，报成「没漏」就是空测试。
- [x] **F8** 加**打包一致性守卫**（`run-tests.js` 第 8 行）。F7 带出来的新风险：
      `tools/` 是被整个目录递归复制进发布包的，`.gitignore` 管不到，
      所以新增的永久脚本会**默认进 zip**，而它依赖 `run-tests.js`（发布包里没有）。
      守卫的做法：扫 `tools/*.js` 找提到 `run-tests.js` / `dom-stub.js` 的文件，
      再从 `build-release.ps1` 里正则出 `$dropFromPkg`，两边必须对上。
      **判据后来widened过两次**：第二条 `/^mutate-.*\.js$/` 也算测试专用（`mutate-decode.js`
      只 spawn 校验器、不提到 harness，第一条判据漏了它）；再加一条**缓存目录检查**，
      拿 `.gitignore` 里的 `tools/.<dir>/` 当真相来源，每一个都必须在 `$dropDirsFromPkg` 里。
      当前**8 个测试专用脚本 + 3 个缓存目录**全在名单里（第 13 轮 `mutate-icons.js` 是
      守卫自己要来的）。注意 `verify-*.js` **故意不算**测试专用 —— 那些是要发的。
      **反向验过**，而且反向验本身也永久化成了 `tools/mutate-pkg.js`（4/4）。

### G. 换数据源到 raider.io + 打包天赋图标 + 补齐中文名（第 12 轮起）

用户第 12 轮一次定了五件事。这一组是它们的落地。**顺序有依赖**：G1 先做（不联网、
立刻能验），G2 是探路（决定 G3 的形状），G3 是主体工作量，G4 独立可并行。

- [x] **G1 补齐中文职业 / 专精名 —— 已完成**（提交 `6357308`）。产物：
      `tools/fetch-class-names.js`（11 KB，永久）→ **`app/class-names.js` 2.2 KB**
      （`window.AE_DB2_NAMES`，13 个职业 + 54 个专精，按 **classFile / specID** 建键）。
      接进 `app/labels.js` 的解析链，顺序 **你的设置 > DB2 > 内置表 > 插件自带 > 英文**。
      三处入口都加载了它（`index.html` / `tests.html` / `run-tests.js`），
      在 `run-tests.js` 里是**硬依赖**：缺文件直接抛，不许静默退回旧兜底表。
      **实测结果**：13/13 职业中文、40/40 专精中文、同职业内 0 重名、
      `251` 从「冰法」变「冰霜」、`256` → 「戒律」、`1468` → 「恩护」，另外 `1480` = 「噬灭」。
      生成器有 4 道硬校验，其中最有价值的是**交叉校验**：从 `labels.js` 里
      解析出那 9 个「从你存档里收来的」职业名，和 DB2 **逐字比对，不一致就拒绝生成** ——
      实测 9/9 一致，这同时验证了两个来源都是我以为的那份数据。
      「玩家职业 = 13 个」这件事也是**两条独立定义**求交：`专精数 >= 3` 和
      `被天赋树的 40 个专精覆盖`。（第一版只用了「有专精」，结果把 ClassID 14
      「冒险者」算进来变成 14 个，硬校验当场拦住了 —— 这个 fail 是有用的。）
      断言收紧：旧的「4 个职业退回英文也算过」改成「一个英文都不许剩」，
      新增「40 个专精全是中文」和「DB2 与存档收来的 9 个逐字一致」。
      变异测试 `tools/mutate-names.js`（永久）**5/5**，含「文件整个缺失」。
- [x] ~~G1 原始调研~~（下面是做之前量到的东西，留作依据）：
      来源是**暴雪自己的 DB2**，不是插件 locale：
      `https://wago.tools/db2/ChrClasses/csv?locale=zhCN`（7156 B）和
      `https://wago.tools/db2/ChrSpecialization/csv?locale=zhCN`（8340 B）。两者都**直连可达**，不用代理。
      **实测覆盖率 40/40** —— 我 `talent-tree.js` 里的 40 个专精，DB2 里全有中文名，一个不缺。
      这一举治掉三个老问题的**病根**：`specRawToCN` 是**按专精英文名**建表的，所以
      `FROST` 一个键撞死骑和法师（死骑显示成「冰法」），`PRESERVATION` / `DISCIPLINE` 干脆没行。
      DB2 是**按 specID 建表**的，撞不了。已实测拿到的那 13 个缺失项：
      唤魔师/湮灭·恩护、法师/奥术·火焰·冰霜、武僧/酒仙·踏风·织雾、
      潜行者/奇袭·狂徒·敏锐、牧师/戒律、**死亡骑士/冰霜**（不是「冰法」）。
- [x] **G2 探明 raider.io 能给什么、给不了什么** —— **做完了**（第 12 轮）。两个端点，分工清楚：
      - **排行榜** `GET /api/v1/mythic-plus/runs?season=…&region=world&affixes=…&page=0`
        → 200，113878 B，20 支队伍 × 5 人 = 99~100 人。每人带
        `character{name, class{id,name,slug}, spec{id,name,slug}, realm{…slug…}, region{…slug…}}`
        + `role` + **`loadout`（官方天赋导入串）**。**没有装备**：
        `item_level` / `items` / `gear` / `bonus` 出现 **0 次**。
      - **角色 profile** `GET /api/v1/characters/profile?region=&realm=&name=&fields=gear,talents`
        → 200，约 40 KB/人。**装备和天赋都有**，这是 G3 的数据源：
        · `gear = {created_at, updated_at, source, item_level_equipped, item_level_total,
          artifact_traits, corruption, items}`，`items` 是 **16 个槽位**，每件带
          `item_id, item_level, name, icon, item_quality, tier, enchant(s), gems(_detail), **bonuses**`。
          **`bonuses` 就是 bonusID 列表** —— B6「插槽徽章要 raidbots 的 socket bonusID 名单」
          有可能从这里解决，不必再依赖 raidbots。
        · `talentLoadout = {loadout_spec_id, loadout_text, loadout[]}`，
          **`loadout[]` 是它自己解出的节点表**：`{node{id,treeId,subTreeId,type,flags,entries[],
          posX,posY,row,col}, entryIndex, rank, grantedNode}`。这一项直接解开了 E5（见上）。
      - **两个端点的字段类型不一样，会咬人**：排行榜里 `realm`/`region` 是**对象**（取 `.slug`），
        profile 里是**字符串**（`"cn"` / `"Grim Batol"`）。我按对象取 `.slug` 去拼配对键，
        得到 `undefined/undefined/名字`，于是统计出 0 份 —— 又一次仪器错误。
        **教训：跨端点拼数据前，先把两边的字段类型都打出来看一眼。**
      - **⚠️ 第 13 轮更正了 G2 的一条结论。** 第 12 轮我记的是「`spec` 参数被忽略，
        返回的是整个职业」。**错的。** 决定性的一问是「换 `spec` 值，返回的人是不是同一批」：
        `priest/discipline` p0 与 `holy` p0 **只重叠 2/100**，与 `shadow` p0 **重叠 3/100**。
        所以 **`spec` 确实过滤**，要按专精分别抓，不能抓一次职业就分给三个专精。
        当时为什么会错：我看的是返回里 `character.spec` 字段的分布 ——
        请求 discipline 得到 `{holy:53, discipline:39, shadow:6}`，看着像「没过滤」。
        真相是**这个榜单按「玩过这个专精的角色」排，而 `character.spec` 是该角色
        当前/主要专精**，两者不是一回事。
        **教训：判断「参数有没有生效」要看返回的集合变不变，不要看返回里某个字段的分布。**
        顺带记一个可用的真值来源：**天赋串头部自带 16 位 specID**，离线就能读
        （`tools/decode-talent-string.js`）。实测 1091 条串全部解出 specID，
        且与站点 `spec` 字段一致（balance 8 页：串头「平衡」625、站点 `balance` 629，
        差的 4 个正是没带串的那几个）。**要判断一条串属于哪个专精，读串头，不要信旁边的字段。**
      - **翻页深度（本轮实测）**：`druid/balance` p0~p5 共 600 人，**去重 600、重复 0**，
        带串率 98~100%。页数没有明显上界：**p20 和 p60 都是 200、都满 100 人**。
        所以「每专精抓 N 页」是可行的，成本线性。
      - profile 的 `realm` 要用**英文 slug**：中文服务器名会 400
        （`Failed to find realm 灵魂出竍 in region cn`，顺带证明 URL 编码没错、是它不认中文名）。
      - 代价已量：一个角色一次请求约 40 KB。44 个角色的缓存 1.9 MB。
        排行榜一页给 ~100 人，**要装备就得逐个角色请求** —— G3 要为此设计增量缓存和限速。
- [ ] **G3 换数据源**：缓存到本地 + 用户手动触发更新 + 只读本地。用户的要求写死这三条：
      「要缓存下来，允许用户手动触发更新，更新后也是存在本地，读本地的」。
      形状照 `app/bis-data.js` 的老路子：`tools/fetch-rio.js` 联网 → 生成
      `app/rio-data.js`（`window.AE_RIO = {...}`），面板只读这个文件，**运行时不联网**。
      需要一个新校验器 `tools/verify-rio-data.js`（照 `verify-bis-data.js` 的样子，
      格式说明写在校验器里），并接进 `run-tests.js` 的 VERIFIERS。
      「手动触发更新」在纯 `file://` 下不能靠 `fetch()`（**实测不可用**），
      所以走**已有的那条路**：`tools/scan.ps1` 那种「跑一下脚本 → 重写 app/*.js → 刷新页面」。

      **抽样策略（第 13 轮按实测定的。写在动手之前，是为了先把判据固定住）**：

      | 要什么 | 从哪来 | 单价（实测） |
      | --- | --- | --- |
      | 天赋串 + 角色名单 | 每专精排行榜 `/api/mythic-plus/rankings/specs?class=&spec=&page=N` | 290 KB / 100 人 = **2.9 KB/人**，带串率 98~100% |
      | 装备 | 角色 profile `?fields=gear`（**不要** `gear,talents`） | **9638 B/人**（带 talents 是 38381 B，4 倍） |

      · **按专精分别抓** —— `spec` 确实过滤（见上面 G2 那条更正）。
      · **目标 100 人/专精**。理由是可算的：比例统计 N=100 时 95% 置信区间约 ±10%，
        N=50 是 ±14%，N=200 是 ±7%。BisData 那个「81%」连样本量都没有，
        所以 100 已经是质变；再翻一倍要多花一倍时间，先不花。
      · **筛人用 `character.spec` slug == 请求的专精。** 要的是「这个人现在穿的装备」，
        而现穿装备对应他**当前**专精，不是他打榜那一把用的专精。
        实测命中率：holy 90/100、shadow 79/100、balance 79/100、**discipline 只有 39/100**。
        按最低值算，**每专精翻 3 页**够凑 100 人。
      · **总预算**：约 120 次榜请求（35 MB）+ 4000 次 profile（38 MB）≈ **73 MB / 4100 次请求**。
        串行 492 ms/次 ≈ 34 分钟，并发 4 ≈ 9 分钟。这是**打包前一次性成本**，可接受。
      · **并发 4 + 退避重试。** 第 12 轮量到并发 6 在 18 个里出 1 个连接错（status 0，不是 429）；
        本轮修图标名又量到 wowhead 在并发 2 下第一条 200、之后全 502。两次都说明
        **没有 429 不等于没有限速** —— 5xx / status 0 必须当「问得太快」退避重试，
        不能当「没有数据」。这正是本轮 1/19 那个假读数的成因。
      · **增量缓存**在 `tools/.rio-raw/`（已 gitignore、已在 `$dropDirsFromPkg`），
        按 `prof-<region>-<realm>-<name>.json` 命名，存在就跳过 —— 中断能续。
      · **产物必须带真实样本量**：每个专精、每个部位各自的 N。这是换数据源的**主要理由**，
        不是副产品。UI 上那句「记录 xx%」要改成真实的 N。

      **槽位名映射也不用手写**（第 13 轮实测）。rio 用英文槽位名（`head`/`finger1`/…），
      面板用暴雪槽位编号（1/2/3/5…）。这张表是**推出来并交叉验证的**，不是我凭记忆写的：
      拿两边都出现的 **206 个物品**当桥，看同一个 `item_id` 在 BisData 里落在哪个编号。
      结果 **12 个槽位票数 100% 一致**：
      `head=1 neck=2 shoulder=3 chest=5 waist=6 legs=7 feet=8 wrist=9 hands=10 back=15`，
      外加 `mainhand=16`（12/15）、`offhand=17`（9/11）。
      **3 对槽位按物品分不开**，这是槽位本身可互换、不是数据缺陷：
      `finger1/finger2` 都投 11 和 12（9:9），`trinket1/trinket2` 都投 13 和 14（11:10），
      主副手互投。所以映射写成 `finger1→11, finger2→12, trinket1→13, trinket2→14`
      是**按序号对齐**，而 `mainhand→16, offhand→17` 有多数票支持。
      生成器要把这张表连同「206 个桥物品、12 个 100% 一致」的判据写在代码里，
      并在生成时重算一遍：**一致率掉下来就拒绝生成**（照 G1 交叉校验的样子）。

      **还没量的一件事**：40 个专精我只量了 4 个的命中率（牧师 ×3、德鲁伊平衡）。
      冷门专精（湮灭、噬灭、酒仙）可能更低、3 页不够。所以抓取工具要
      **按实际命中数决定翻几页**，并把「翻到上限仍不足 100 人」的专精显式报出来，
      而不是静默给一个小样本 —— 那正是 BisData 现在的毛病。

      **slug 不用维护表**（第 13 轮实测）。请求要 `class=` / `spec=` 的 slug，
      但它可以从 `app/talent-tree.js` 里现成的 `cls` / `specEn` **机械推导**：
      小写 + 空格转连字符（`Death Knight` → `death-knight`，`Beast Mastery` → `beast-mastery`）。
      拿站点自己当判据逐对验证（错的 slug 返回 400 `could not find requested class/spec combo`），
      **40/40 全部被认下来**，没有一个是我手写的。验证时只读状态码就掐断连接，
      省掉 40 × 290 KB = 11.6 MB 的白下载。

      **profile 装备的真实形状**（43 份本机缓存、687 件装备，离线数出来的）：
      · 槽位键 **15 个恒有**（head neck shoulder back chest waist wrist hands legs feet
        finger1 finger2 trinket1 trinket2 mainhand）+ `shirt` 24/43 + `offhand` 18/43。
        **注意不是 16 个** —— 第 12 轮记的「16 槽位」是单份样本的数，双持 / 盾才有 `offhand`。
      · 每件恒有 `item_id, item_level, icon, name, item_quality, is_legendary, bonuses`；
        `enchant` 339/687、`tier` 168/687（**不是恒有**，不能当必填字段验）。
      · **`icon` 和 `item_quality` 是白送的。** 这两样正是 `fetch-icons.js` 现在要逐个
        查 wowhead 才拿到的（BisData 里 2187 件一个都没有）。换到 raider.io 之后
        这一步可以整个省掉，连 `tools/.icon-cache.json` 都不必再要。
      · **`bonuses` 也是白送的** → B6「插槽徽章要 raidbots 的 socket bonusID 名单」
        大概率能直接从这里解决。

      **⚠ 会改变 G3 形状的一条：raider.io 的物品名 0/687 是中文，全是英文。**
      BisData 现在给的是中文名，直接换过去等于把面板从中文退回英文 —— 这是**功能退化**，
      不能接受。

      **解法已实测定下来：用 DB2 整表，不逐个查。**
      `https://wago.tools/db2/ItemSparse/csv?locale=zhCN` → HTTP 200，**48.03 MB，15 秒**，
      103 列 **175164 行**，`ID` 在第 0 列、**`Display_lang` 在第 5 列**就是中文物品名。
      对本机 402 个不同 `item_id` **命中 402/402（100.0%）**。
      样例：`2575 Red Linen Shirt → 红色亚麻衬衣`、`41253 Blue Workman's Shirt → 蓝色工匠衬衫`。
      这和 G1 拿职业/专精中文名是**同一条路**（暴雪自己的 DB2，直连，不用代理），
      所以中文名这件事已经不是风险了。
      · 48 MB 只是**下载**体积，不进包：抓取工具只留「用到的 id → 中文名」，
        照 `tools/.talent-names.json` 的样子存成一份几十 KB 的提交产物。
        整表缓存进 `tools/.db2-names/ItemSparse.csv`（该目录已 gitignore）。
      · **逐个查 wowhead 的方案作废**（4000 份 profile 会有几千个不同 id，
        按实测 250 ms 间距要 20 分钟以上，还会限速）。但那条链路量到的东西留档：
        `nether.wowhead.com/tooltip/item/<id>?locale=4` 给中文
        （`271456` → 「翡翠督军的淬火战角」，实测）。`locale` 码是试出来并留下证据的：
        0=enUS 1=koKR 2=frFR 3=deDE **4=zhCN** 5=enUS(?) 6=esES 7=ruRU 8=ptBR。
      · **顺带又踩一次同一个坑**：第一次试 locale 时 0/2/4/5/6/9 全部 502，
        看着像「这些 locale 不支持」。加退避重试后 0/2/4/5/6 全部 200 ——
        **又是限速，不是不支持**。同一轮里这已经是第二次把 502 读成「没有数据」
        （第一次是修天赋图标名）。规矩已经写下来了，我还是又犯了一次，
        说明它得进代码而不是留在纸上：抓取工具里的 HTTP 助手要**默认**对 5xx 退避重试。
- [x] **G4 打包天赋图标 —— 做完了**（提交 `268ef21`）。

      **成品**：`tools/fetch-talent-icons.js` → `app/talent-icons/`，**2094/2094（100%）**，
      5.14 MB，56×56（和装备图标同档）。包 1630.9 KB → **7065.5 KB**（实测 zip，
      2606 个条目，包内图标 2094 张与 `icons` 字典一一对应）。
      节点画法：图标 24px 在上、中文名在下，`.tnode` 从 62×32 长到 **62×50**，
      `CELL_H` 44 → 62 保住 12px 间隙，点数徽章改成右下角绝对定位
      （放在流里会把总高顶到 58px、超出 50px 的框）。未点的图标 `grayscale + brightness(.55)` 压暗。

      **卡了一轮的真问题：上游给的图标名有 19 个不存在。**
      两个源（暴雪 CDN / zamimg）都是 404，一开始看着像「源没有图」。
      拿 `entries[].spellId` 去 wowhead 的 spell tooltip 查真名，19 个全部查到：

      | raidbots 给的 | 真名 | 坏在哪 |
      | --- | --- | --- |
      | `spell_frost_ring_of_frost` | `spell_frost_ring-of-frost` | 连字符被规范化成下划线 |
      | `warlock__bloodstone` | `warlock_-bloodstone` | 同上 |
      | `inv121_ability_hunter_aimedshot` | `8026692` | 新图标还没名字，真名是数字 FileDataID |

      前一类 16 个，后一类 3 个（升腾三个变体 8026696/7/8、瞄准射击 8026692、破天投枪 8026700）。
      **不是源的问题，是上游的名字本身不存在。** 影响 25/40 个专精、29 个节点，不能忽略。
      修复通道写进抓取工具，对应关系存 `tools/.talent-icon-fix.json`（提交进仓库：
      它是查出来的事实，重查要联网 + 走代理）。
      **关键决定：存盘用 raidbots 那个坏名字。** `app/talent-tree.js` 的 `icons` 字典就是坏名字，
      面板按字典取文件名 —— 按真名存盘的话 `app/` 就得再带一张映射表，
      那张表会成为第二个要跟着上游维护的真相来源。文件名当键，坏名字就只是个键。

      **两次仪器故障（都差点被当成数据）**：
      1. 修复通道第一版没有重试，19 个只修好 1 个，看着像「wowhead 查不到」。
         复现出来是**第一条 200、之后全是 502** —— 查得太快被挡了。
         502 被我当成了「这个名字修不好」。改成并发 1 + 5xx 退避重试 + 请求间隔后 **19/19**。
      2. 核对包内容的脚本用正斜杠匹配条目名，而 zip 条目是**反斜杠**分隔，
         于是读出「天赋图标 0 个」和「style.css 也缺」。后者不可能，才发现是仪器。

      **一次假通过**：图标接上、套件全绿，但「渲染检查」那行仍然报 **7947 个图标** ——
      和一张天赋图都没有的时候一模一样。原因是天赋渲染走 `checkTalents`，
      而数图标的代码只在 `checkRender` 里。**4304 个节点、2094 张图，一条断言都没看过。**
      补了 5 条：有没有图标 / 路径是否指向 `app/talent-icons/` / 文件是否存在 /
      有没有 `class=ti`（少这个，`style.css` 那一整段全落空，实测漏过一次）/
      **图标和名字必须来自同一个 entry**。
      最后一条是唯一能抓住「`icons[ent[2]]` 写成 `icons[ent[1]]`」的 ——
      那样照样得到一个存在的图标名、存在的文件、正确的前缀，前四条全过，
      而二选一节点会图文不符。做法是对着上游反查：显示的名字属于哪个 entry，
      图标就必须是那个 entry 的。实测 **图文配对 4304 次，不符 0**。

      **`tools/mutate-icons.js`（永久，6/6）**，而且比其它套件多一条契约：
      每个变异体必须触发**指定的那一条**断言（`want` 关键词），只要非零退出不算通过 ——
      被别的断言抓到会报「串了」。六个变异体：不挂图标 / 固定取第一个 entry /
      不设 class / 去装备图标目录找 / 反查一次都不做（证明「配对数 < 3000」这条下限不空）/
      删掉一张真在用的图标文件。
      打包一致性守卫自己要求把 `mutate-icons.js` 和 `.talent-icon-fix.json` 加进
      `$dropFromPkg`（现在认到 8 个测试专用脚本）。

---

## 待用户决定（不要自己拍）

> **第 12 轮：用户一次答了 5 条。** 原文：
> 「需要导入导出的串，这个应该原本就有吧，天赋图标要打包，有异议的地方可以去 wcl 或 io 站
> 获取数据相关内容，数据源不要用那个插件的了，用 raider.io 的实际数据源 ok 不 ok 啊，
> 但是要缓存下来，允许用户手动触发更新，更新后也是存在本地，读本地的，
> 缺中文名的职业，用网络资源解决一下，你自己解决一下」
>
> 落地成 **G 任务组**。已答掉的：第 1 条（换 raider.io）、第 3 条（不再带 GearInsight）、
> 第 6 条（天赋图标要打包）、第 7 条（不用我编码，串本来就有）。
> 「缺中文名」从「待用户提供」移进 G 组 —— 用网络资源自己解决。

1. ~~**换不换数据源，换成哪个。**~~ **已定：换 raider.io**，见 G 组。
   原文如下（保留，因为 B4 的调研结论仍然成立，只是结论被新信息推翻了）： ~~B4 会给出调研结果~~ **B4 已做完，结论是没有可用替代品**
   （详见上面「替代数据源调研」）。所以这一项现在的实际问题变成：接受现有数据的缺陷
   （无样本量、列表被截断）并把话说清楚，还是干脆不做这个面板？我的判断是前者，
   因为「顶尖玩家在穿什么」本身有价值，只要不假装它是模拟结论。
2. ~~fileID → 图标名的表从哪来~~ **不需要了**，wowhead tooltip 直接给名字。
3. ~~**要不要继续带 GearInsight 的数据。**~~ **已定：不带了**，换 raider.io（G 组）。
4. **发布包里多了 1 MB 图标（462 个 jpg）。** 接受不接受？不接受的话只能回到运行时拉图，
   而那在国内靠不住（zamimg 直连 403）。
   **已实测**（`tools/build-release.ps1 -SkipBuild` 出的包，502 个条目）：
   整包 **1497.9 KB**，其中图标压缩后 **987.6 KB —— 占 66%**。jpg 本来就压过，
   zip 再压只省了 4.6%（解压后 1035.8 KB）。也就是说不带图标的话包是 ~510 KB。
5. **要不要显示「插槽」标记（B6）。** ⚠ **rio 的 profile 白送 `bonuses`，这条大概率不用你决定了** ——
   `app/rio-data.js` 里已经有 `sock` 字段，494/2432 件带插槽标记，没有多联一次网。
   下面是原始记录（保留）：1039 行 / 102 件装备带插槽 bonusID，中文名有 locale 来源
   （`Syndicator/Locales.lua` 的 `KEYWORD_SOCKET` = 「插槽」）。**代价是生成器要多联一次网**：
   插槽 bonusID 清单只有 raidbots 的 `bonuses.json`（1.7 MB，直连可达）有，本机磁盘上没有。
   现在 `gen-bis.js` 是纯离线的，加这个就破了这一点 —— 所以不自己决定。
6. ~~**天赋树的图标要不要打包。**~~ **已定：要打包，而且已经做完了**（G4，提交 `268ef21`）。
   **实测终值**：2094 张 56×56，**5.14 MB**，包 1630.9 KB → **7065.5 KB**（zip，实测）。
   预估的 4.58 MB / 「约 6 MB」都偏低了 —— 预估用的是 40 个样本的均值 2626 B，
   实际全量均值 2573 B 但**修回来的 19 张**和图标本身的尺寸分布让总量到 5.14 MB。
   原分析保留：
   **D 组已做完并入库**，树当时是**不带图标**发的：
   节点画成方块 + **完整中文名**（不是缩写 —— 实测同树内前 2 字重名 496/3447，缩写会误导），
   坐标 / 连线 / 点亮状态 / 点数都是真的。带图标的包实测会从 **1630.9 KB** 变成约 6 MB。
   三个选项：
   - **不带**（现在的做法，已入库）：包 1630.9 KB，树可读，鼠标悬停给中文名 /
     二选一的两个选项 / 层级要求 / 点数。
   - **带**：要新写一个 `tools/fetch-talent-icons.js`（照 `fetch-icons.js` 的样子，
     图标名已经在 `app/talent-tree.js` 的 `icons` 字典里，2094 个，不用再查），
     跑一次下 2094 张，包变成 ~6 MB。**没写，等这一条定了再写。**
   - **只带热门的**：只下实际出现在 `talent-data.js` 里的节点图标（**实测 1447 张 ≈ 3.17 MB**）。
     但注意这个省法有个逻辑问题：天赋树要画的是**整棵树**，没点的节点也要显示，
     所以缺的那 647 张会变成没图标的空节点 —— 省 1.4 MB 换来一棵半残的树，不划算。

8. **⚠ 换到 raider.io 之后，BisData 独有的那半个面板怎么办（第 14 轮量出来的，需要你拍）。**

   G3 的抓取和产物已经做完了（`app/rio-data.js`，849.3 KB，40 个专精各 94~100 人）。
   但把面板真正切过去之前，有一件事必须先说清楚：**这两份数据不是同一个东西的两个版本，
   rio 只覆盖了 BisData 的一部分。** 离线数出来的对照：

   | 面板上的东西 | BisData | raider.io | 切过去以后 |
   | --- | --- | --- | --- |
   | 每部位的装备分布 | 有，但每部位只列 1~7 件 | **有，不截断** | **明显变好** |
   | 样本量 | **完全没有** | **每部位一个真实 N**（中位数 100） | **明显变好** |
   | 覆盖率（列出来的加起来占多少） | 团本中位数 69.7%，大秘境 75.2%；111 / 95 组不到 50% | **恒等于 100%** | **明显变好** |
   | 候选件数（中位数） | 3 件 | **20 件** | 变好 |
   | 物品图标名 + 品质 | 没有，要另查 `app/item-icons.js` | **白送**（`i` / `q`） | 变好 |
   | 插槽标记 | 没有（B6 一直卡着） | **白送**（`sock`，494 件） | 变好 |
   | **属性权重**（暴击 9 / 精通 7 那种条） | 有（`weights` + `statNames`） | **没有** | **会消失** |
   | **单体 / 多目标偏好**（`target`） | 有 | **没有** | **会消失** |
   | **武器类型分布**（`weapon`） | 有 | **没有** | **会消失** |
   | **推荐宝石 / 附魔**（`gems` / `ench`） | 有 | **没有** | **会消失** |
   | **掉落来源 + 首领名**（`srcs`，26 条） | 有 | **没有** | **会消失** |
   | **升级轨道徽章**（`tracks`，面板上 3601 个） | 有 | **没有** | **会消失** |
   | **消耗品清单**（`consumables`，35 种） | 有 | **没有** | **会消失** |
   | **团本 / 大秘境分开两套** | 有（`raid` + `mplus`，各 632 组） | **只有大秘境** | 团本那套会消失 |
   | 平均装等 / 装等容差（`ilvl` / `tol`） | 有 | 只有每件的平均装等 | 部分消失 |

   也就是说：**「不带 GearInsight 了」的字面执行结果，是面板上有一半内容直接消失** ——
   属性权重、宝石附魔、掉落来源、升级轨道、消耗品、团本毕业装。这些东西 raider.io
   的两个端点都不提供（榜单只给角色和天赋串，profile 只给当前穿的装备）。

   **三条路，请你选一条：**

   - **A. 只换分布，其余保留。** 部位分布、样本量、图标、品质、插槽走 rio；
     属性权重 / 宝石附魔 / 掉落来源 / 升级轨道 / 消耗品 / 团本那套继续用 BisData，
     并在界面上标明每块的来源。**功能最全，代价是包里仍然带 GearInsight 的数据**，
     和你说的「数据源不要用那个插件的了」有出入。
   - **B. 全切 rio，消失的就砍掉。** 严格照你说的做。面板变成「顶尖玩家每个部位穿什么，
     带真实样本量」+ 天赋树，干净且每个数字都可追溯；但上面那 7 项功能没了。
   - **C. 全切 rio，消失的另找来源。** 属性权重能从 bloodmallet / murlok.io 拿（都不用代理，
     第 11 轮探过），宝石附魔和掉落来源要再找。**工期最长，而且要新增两个上游依赖。**

   **我的默认（你不回就这么办）：走 A，但把来源标签做得很显眼** ——
   因为「砍掉一半功能」这种事我不该自己替你决定，而 A 是唯一不损失现有功能的路。
   如果你的本意是「彻底不要那个插件的数据」，回一句「走 B」即可。

7. ~~**天赋导入串：全 0 的 `treeHash` 游戏认不认？**~~ **已答，而且问题本身问错了。**
   用户说「这个应该原本就有吧」—— 对的。**raider.io 的 M+ 排行 API 里每个玩家都带
   `loadout` 字段，那就是官方天赋导入串本身**（实测 99 份，全部能解出职业/专精）。
   所以我根本不需要写编码器，也不需要 `treeHash`：串是现成的，直接转发。
   顺带把「全 0 hash 认不认」也答了 —— **raider.io 那 99 份串的 `treeHash` 128 位全是 0**
   （本机存档里是 4 份全 0、50 份非 0）。一个发给几百万人复制粘贴的站点全用零 hash，
   说明游戏不校验它。**教训：在动手造一个东西之前，先找找它是不是已经存在。**
   我为了「生成导入串」啃了两轮位流，而 API 里现成躺着 99 份。
   下面是原始记录（保留）：
   背景见 README「天赋导入串：解到一半」一节。要点：编码一个官方导入串需要
   `fullNodeOrder`（**有**，在 raidbots 那份数据里，按职业，13 个职业全对上了）和
   `treeHash`（**没有**，公开数据里查不到）。但本机存档里有 **2 组串的 `treeHash` 是全 0** 的
   （一个复原萨满、一个 Devourer DH），说明**插件**收下了全 0 hash。
   如果**游戏**也认，那 `treeHash` 就不需要了，40 个专精都能编码 ——
   这是「把别人的套路一键粘进游戏」这个功能唯一的拦路石。
   **怎么验**：进游戏，天赋界面点「导入」，粘下面这一串。这是**复原萨满**的，
   从你自己 `TalentLoadoutsEx.lua` 里原样取出来的（不是我编的），
   而且它的 `treeHash` 那 128 位**全是 0** —— 这正是要验的那种串：
   ```
   CgQAAAAAAAAAAAAAAAAAAAAAAAAAAAAGAAAAAzMzMzyMWmZMzwsMWMsgFYDmxiGbDIzAbMzMLYWmZmRmtFzMTjZwALzYGbGDziZAAMA
   ```
   认了（能看到一套完整天赋）→ 告诉我，我就能写编码器，40 个专精都能编；
   报「无效的导入串」→ 这条路封死，我把 README 那一节改成结论。
   注意别真点「应用」，看一眼能不能识别就行。

## 待用户提供（本机拿不到的字符串）

- ~~4 个职业中文名：EVOKER / MAGE / MONK / ROGUE~~ **不用你提供了**（G1）：
  暴雪 DB2 的 `ChrClasses?locale=zhCN` 全有，13 个职业一个不缺。
- ~~死骑冰霜专精中文名~~ **不用你提供了**（G1）：DB2 按 specID 建表，`251 死亡骑士/冰霜`，
  撞不到法师的「冰霜」。插件那个「冰法」正是「按英文名建表」的必然结果。
- ~~`PRESERVATION`（唤魔师）/ `DISCIPLINE`（牧师）的中文专精名~~ **不用你提供了**（G1）：
  `1468 唤魔师/恩护`、`256 牧师/戒律`。
- **`Sporefused` 的中文名**（B5）：影响 73 行装备的轨道徽章。AlterEgo 的 `upgradeBonusLabels`
  只给英文 `Sporefused: Myth`，raidbots 也是英文 `tag: "Sporefused: Myth"`，
  全盘 7900 个文件里没有任何 zhCN 译名。有了译名这 73 行就能显示轨道。

---

## 迭代日志

追加在末尾，一轮一条，写「做了什么 + 验证结果」，不要写「打算做什么」。

- **第 0 轮**：撤销 v1.15.0（Release / tag / 远端提交全删，代码留在工作区，
  安全分支 `wip-bis-v1.15.0` 指向 `14ee307`）。版本号退回 1.14.0。
  查明图标瓶颈是 itemId → 图标名的映射，不是 CDN。写出 `tools/blp.js` 并验证解码正确。
  做完 BisData 数据质量审计。建立本文件。

- **第 1 轮**：图标全部打通，离线可用。
  - 建分支 `feat/bis-panel`，把 v1.15.0 的代码提交在上面（`a624736`），`main` 不动。
  - 先否定了自己上一轮的方案：散装图标目录里 `inv_10*/inv_11*/inv_12*` **各 0 个**，
    当前赛季装备根本不在里面 —— 「纯本地转 BLP」这条路对装备是死的。
  - 改探网络可达性，找到两个能用的源（暴雪 CDN 直连可达；zamimg 走代理，且是
    当前赛季新图标的**唯一**来源）。wowhead tooltip 接口同时给了图标名和**品质**。
  - 写 `tools/fetch-icons.js`，产出 `app/item-icons.js` + `app/icons/`（462 个 jpg，1 MB）。
    568 个 itemId 100% 覆盖，0 断链，0 孤儿。
  - `app/bis.js` 四处调用点全改成本地图标 + 真品质。
  - **抛弃临时 harness**，改成正式的 `tools/dom-stub.js` + `tools/run-tests.js`。
  - 干活中抳出的真 bug：
    1. `fetch-icons.js` 第一版读 `c.itemId`，而 `gen-bis.js` 压缩后的键叫 `c.id` ——
       35 个消耗品被静默漏掉，而覆盖率还报 100%（**漏掉的东西不在分母里**）。
    2. zamimg 那批第一版下成 `medium`（36×36），和其他 56×56 不一致。
    3. 渲染 harness 第一版**只统计不断言**，变异测试下占位块 0 → 3963 仍报「通过」。
    4. 回退路径（BisData 自带图标名）指向 3 个**不存在的文件** —— “有退路”只是句话。
    5. 品质硬写 `qualityColors[4]`，把 54 件蓝的和 14 件白的染成了紫色。
  - `build-release.ps1` 加排除列表：缓存和测试 harness 不进发布包。
  - README 五处过时说法全改（本来写着「装备图标显示不了」）。
  - **验收：`node tools\run-tests.js` → 127 项测试 + 渲染检查全绿。**

- **第 2 轮（B3）**：`bonusIDs` 处理完，但结论和任务原本的说法相反。
  - 任务写的是「修 `gen-bis.js` 丢 `bonusIDs` 的问题」。先量了再做：原样存要 19~24.6 KB，
    而查遍两个插件，`bonusIDs` **只用来拼游戏内 `|Hitem:` 链接**让游戏 tooltip 显示对的装等 ——
    网页没有这个机制，装等本来就在行里。**所以「补回来」本身是没价值的，补的应该是解码结果。**
  - 从 `AlterEgo/Data/UpgradeTracks.lua` 抄到赛季 17/18 的轨道表（10 轨 × 6 级 = 60 个 bonusID），
    解出 **3601/3963 行（90.9%）** 的「英雄 6/6」这种标签，**0 行命中两个轨道**（无歧义）。
  - 证明轨道**推不出来、只能存**：装等 308/318/321 各对两种轨道，连 `(装等, 来源)` 都还有
    5/24 组歧义（`321/raid` 是英雄 6/6 ×528 vs 神话 2/6 ×10）。
  - 中文名不手写：**两处互相独立**的 shipped zhCN locale（EllesmereUILocales、ItemInfoOverlay）
    完全一致才用，生成器两边都读、不一致就 `exit(1)`。
  - 行格式扩成 6 位，`mx` 位不再允许跳过（详见上面「代码」一节）。
  - 顺手抓到的真 bug：**两个生成器报的体积全是错的** —— 用字符数当字节数，中文 1 字符 3 字节，
    `bis-data.js` 少报 15 KB、源 Lua 少报 160 KB。所以我以为加了轨道「体积反而变小」，
    实际是 186931 → 201010 字节（+13.7 KB）。已改用 `Buffer.byteLength`。
  - 新增 4 项数据测试 + 渲染检查加「轨道徽章数」断言。**两批都做了变异测试**：
    7 个数据变异（轨道码越界 / 等级 0 / 等级 7 / mx < ilvl / 缺中文名 / 删轨道池 / 覆盖率清零）
    全部被抓；渲染断言在 `tracks = []` 下从「通过」变成报错。
    第一次变异跑还白跑了一次 —— 改动点插在渲染循环**之后**，什么也没证明，挪到前面才有效。
  - 渲染出的徽章数 **3601** 和生成器统计的解码行数完全一致（两条独立路径互相印证）。
  - **验收：`node tools\run-tests.js` → 131 项测试 + 渲染检查全绿。**

- **第 3 轮**：格式校验器（B1），以及两个顺手抓到的真 bug。
  - `tools/verify-bis-data.js`：**schema 就是文档**，不另写 .md（另写必然和代码脱节）。
    2016 项检查，覆盖顶层字段、`items` / `srcs` / `tracks` / `specs` / 行 / 宝石 / 附魔 /
    消耗品 / 部位名与属性名覆盖 / 来源分类标签覆盖。有 `--data` 参数，可以拿来验**候选**
    数据源的产物 —— 这正是「换数据源只换生成器」需要的那道关。
  - 变异测试 31 个，第一轮 **30/31**：删掉 `target.farm` 它照样过。原因是 schema 里
    `target: { type: 'obj' }` 只保证「是个对象」，三套子键只写在**说明文字**里。
    补成可执行的（`SUB_KEYS` + 属性名逐个验），31/31，检查项 1296 → 2016。
    **教训：写在注释里的约束等于没有约束。**
  - **真 bug 1：292 行套装 + 5 行任务装备的来源徽章一直显示英文 `tier` / `quest`。**
    插件自己的 `sourceCategories` 只有 5 个键（raid/mplus/crafted/world/other），
    数据里实际用到 7 个。中文名不许手写，所以从数据自己的 `source` 文本补：
    这两类各自只有一种 source 文本（`套装转换` / `奇点声望任务`），无歧义才用，
    有歧义就留英文并警告。加了测试，删掉任一标签立刻失败（6 个变异全抓到）。
    · 顺带否掉了一个更省事但不安全的做法：从 locale 里查 `L["Set"]`。
      `EllesmereUILocales/zhCN.lua` 里 `L["Set"] = "设置"` —— 同一个英文词在别的语境里
      是「设置」。**通用词查表不安全，得看语境。**
  - **真 bug 2：`.item .tag` 这条 CSS 底样式是漏的。** 只写了 `.tag.use` 和 `.tag.trk`，
    `.slot-head .tag` 是另一个祖先管不到，所以来源徽章 `border-width` 是 0，而
    `bis.js` 里 `badge.style.borderColor = catColor(cat)` 只设颜色 ——
    等于给一条不存在的边框上色，徽章从一开始就是裸文字，没有胶囊、没有 padding、
    没有 `flex: none`（会被压缩）、没有 `nowrap`（6 个字的「奇点声望任务」会折行撑高整行）。
    补上底样式，`.trk` 简化成只覆盖颜色。
  - `.gitignore` 加 `_*` —— 上一轮我自己把临时文件 `_msg.txt` 提交进去了（`74a849a`）。
    规矩靠 gitignore 执行，不靠我记得。
  - 校验器接进 `run-tests.js`，破坏 `tracks[0]` 验证过两层都会失败、退出码 1。
  - **验收：`node tools\run-tests.js` → 133 项测试 + 渲染检查 + 2016 项格式校验全绿。**

- **第 4 轮（B2）**：把「这份数据有多可信」变成用户看得见的东西，而不是又一句免责声明。
  - **每个部位组加了「记录 xx%」徽章**（使用率之和），不到 50% 标警告色，
    悬停给出「剩下的 xx% 穿的是什么，数据里没有」+「这份数据不带样本量」。
    为什么按部位显示而不是在页脚多写一句：截断程度**每个部位都不一样**
    （1264 组里之和的中位数 72.9%、最低 8.6%、有 206 组不到 50%），
    一句笼统的话既不能告诉用户哪一组不可信，也很容易被跳过。
  - 复用了 CSS 里三条**死规则**：`.slot-head .tag` / `.tag.ok` / `.tag.no` 早就写好了，
    但 `bis.js` 从来没创建过带这些 class 的元素。没删，改成派上用场。
  - 布局上踩到一个点：`.slot-head .tag` 和 `.mine` 都带 `margin-left: auto`，
    两个都在的时候剩余空间被**平分**，`.mine` 就不再靠右了。所以覆盖率徽章紧跟部位名放，
    并用 `.tag.cov { margin-left: 0 }` 覆掉。
  - README 新增「这份数据有多可信」一节，用实测数字写：对得上的四条 + 问题五条
    （没有样本量 / 尾部被截断 / 权重是硬编码的 / 上游分类表缺 tier+quest / 饰品重复），
    并说明为什么仍然选它。已知限制里也加了一条。
  - 渲染检查新增两条硬断言：**覆盖率徽章数必须等于部位组数**（1264 == 1264，
    和生成器的统计独立对上），且文字必须形如「记录 93.6%」。
    两个变异（不画徽章 / 文字写成 `cov 93.6`）都被抓到、退出码 1。
  - **验收：`node tools\run-tests.js` → 133 项测试 + 渲染检查 + 2016 项格式校验全绿。**

- **第 5 轮（B4）**：替代数据源调研，结论是**没有可用的替代品**，并顺带独立验证了轨道解码。
  - 探了 8 个站点的可达性（直连 + 代理各一次）。三个被 Cloudflare 挡死（archon.gg /
    warcraftlogs / u.gg，**直连和代理都 403**），三个可达（bloodmallet / raidbots / murlok.io），
    wowhead 只有走代理能通。
  - 逐个查结构，没一个能替代：murlok.io **只有 3v3 PvP**（`/mythic-plus`、`/raid`、`/pve` 全 404）；
    bloodmallet 是**模拟 DPS 而不是使用率**，而且这个专精只有 5/11 种数据类型、
    只有 1/5 种战斗风格有数据（其余返回 76 字节的 `{"status": "error"}`）；
    raidbots 只是 bonusID 词典，没有任何装备推荐。
  - **副产品：用 raidbots 的 `bonuses.json` 独立验证了上一轮的轨道解码。**
    BisData 实际用到的 12 个轨道 bonusID，装等 **12/12** 和 raidbots 完全一致；
    赛季 18 那 30 个 ID 的 `upgrade.name` + `level` + `max` **30/30** 和 AlterEgo 的表完全一致。
    赛季 17 那 30 个在 raidbots 的 live 数据里已经没有 `upgrade` 字段（只剩 `itemLevel`），
    所以改用装等验 —— 也全对。两条完全独立的路径得出同一个结果。
  - 同时确认了解不出轨道的 37 个 bonusID **确实不是轨道**：raidbots 显示它们是插槽
    （`socket: 1`）、制造属性（`craftedStats`）、镶嵌效果（`effect`）、服务端标记
    （`serverside: true`）、物品唯一分类（`item_limit_category`）。「解不出」是对的行为。
  - 挖出两个可做的改进（都进了待决定/待提供，不自己拍）：**73 行** 带 `Sporefused: Myth`
    却没有轨道徽章（缺 zhCN 译名）；**1039 行 / 102 件**带插槽 bonusID 可以标「插槽」
    （中文名有 locale 来源，但清单只有 raidbots 有，要联网）。
  - **一次测量失误值得记**：第一版统计「带 label / 带插槽的行数」时用
    `/\{[^}]*?bonusIDs\s*=\s*\{([^}]*)\}/g` 切行，`[^}]*?` **跨不过嵌套的 `stats = { … }`**，
    只匹配到 480 / 3963 行，于是两个数都报 0。看着像「没有这种数据」，其实是正则漏了 88%。
    改成按 `{ itemId =` 边界切行之后才得到 73 / 1039 这两个真数。
    教训和上一轮同一个：**先验证测量本身，再相信测量结果**。

- **第 6 轮（C4）**：验证发布包真的装了我以为的东西。
  - `tools/build-release.ps1 -SkipBuild` 出包，**没有任何联网 / git / 发布动作**（读过脚本确认），
    版本号仍是 1.14.0。写了个临时脚本自己读 zip 的中央目录逐条核对。
  - **`app/icons/` 462 个条目全在**，解压后 1035.8 KB、压缩后 987.6 KB；
    整包 1497.9 KB / 502 条目 → **图标占 66%**。jpg 本来就压过，zip 再压只省 4.6%。
    不带图标的话包是 ~510 KB。这个数已经填进「待用户决定」第 4 条。
  - `$dropFromPkg` 四项（`launcher.ico` / `.icon-cache.json` / `dom-stub.js` / `run-tests.js`）
    确认都不在包里；`index.html` / `bis*.js` / `item-icons.js` / `talent-data.js` /
    `verify-bis-data.js` / `tests.html` 确认都在；没有 `data/` 个人数据、没有 `.git`、
    没有 `_` 开头临时文件。
  - 两个「异常」查过是故意的：`魔兽看板.exe` 是启动器本体，`data/说明.txt` 是脚本
    现场生成的空目录占位说明（内容是写死的两句话，不含任何个人信息）。
  - **我的检查脚本第一版全是假阴性**：`Compress-Archive` 写进中央目录的条目名用的是
    **反斜杠**，而我拿 `/` 去匹配，于是「app/icons/ 条目 0」「index.html 缺」——
    全是我自己的 bug。改成解析时把分隔符统一成 `/` 才看到真相。
    教训和上一轮那个 `[^}]*?` 跨不过嵌套表一样：**先怀疑量具**。

- **第 7 轮（打通天赋树的数据来源，代码还没动）**：A/B/C 三组除了卡在用户身上的 B5/B6 已经做完，
  所以给自己定了下一个长期目标 —— **面板最大的已知缺陷「天赋树画不出来」**。
  这一轮只做调研和验证，一行功能代码都没写，因为先要确认数据真的存在。
  - **树的结构找到了**：`raidbots.com/static/data/live/talents.json`，3.2 MB，**直连 200，不用代理**。
    40 个专精，每个带 `classNodes / specNodes / heroNodes / subTreeNodes`，
    节点字段 `id, name, type, posX, posY, maxRanks, next, prev, entries[]` ——
    正是 `AE.TALENT_TREE_FORMAT` 里我自己写下的那份格式所需要的全部字段。
  - **关键的连接验证**：`entries[].id` 就是 `talent-data.js` 里的 `entryID`。
    **38/40 专精 100% 命中**，全局 3039/3040。
  - **两个不命中的专精查清了，是上游数据的真 bug，不是我的转换问题**：
    · 防护战 77 套里有 1 套其实是**武器战**的天赋（含 Mortal Strike / Overpower /
      Tactician 等武器专精节点 + subTree 60 = Slayer，而防护战只能选 Colossus / Mountain Thane）。
      定位到具体记录：`mplusFarm / 红玉新生法池 / 绝密宝宝`，越界 49 项，影响 1/120 条玩家记录。
    · 织雾僧 4/85 套越界，涉及 entryID 125051（raidbots 里根本没有，只在 1/86 套里出现一次）。
    · **38/40 专精完全干净。** 这解释了为什么之前「验证编码 0 处不一致」和这里「有越界」
      能同时成立 —— 我之前验的是「我有没有转错」，没验过「上游给的对不对」。
  - **中文名也有了，而且是最硬的来源**：`wago.tools` 提供暴雪 DB2 的 CSV 导出，
    `?locale=zhCN` 直连可达。连接链 `entries[].definitionId → TraitDefinition.ID
    → (OverrideName_lang 或 SpellID → SpellName.Name_lang)`。
    **实测 3334/3334 = 100% 拿到中文名，0 个漏、0 个回落成英文。**
    这不是插件 locale，是**游戏自己的字符串表**，比现有任何来源都硬。
  - **量了体积**（决定了 D 组怎么切）：
    · 只留必需字段的 JSON 447.7 KB，名字/图标做字典后 **378.4 KB**（和现有 `talent-data.js`
      的 520 KB 同一量级，可接受）。
    · 节点条目 40 专精相加 4613 个，**规范化去重后 3110 个**（可以做全局节点池）。
    · **图标 2094 张 ≈ 4.58 MB，是现在整个发布包的 3 倍** → 进「待用户决定」第 6 条。
      所以 D 组刻意设计成**不带图标也能用**（画圆圈 + 中文名）。
  - **纠正一个「职业树按职业共享」的错判**：第一版脚本报「9 个职业各有 3 种不同的职业树」，
    看着像每个专精的职业树都不一样。按节点 id 集合重新比才发现**字段差异 0 处**，
    差的只是「哪些节点在」（专精门控）。所以能共享的是**节点本身**，不是整棵树。
  - **也纠正了自己刚写进计划里的一个没测量的数**：「只带热门图标 1387 张」是我拍的，
    实测 1447 张 / 3.17 MB；而且这个省法逻辑上有问题（没点的节点也要画，会变成空圈），
    已在计划里改掉。
  - 验收：`node tools\run-tests.js` 仍然 133 项全绿（这一轮没碰功能代码）。

- **第 8 轮：D 组全部做完，天赋树画出来了**（提交 `ea5c83e` / `5101f5c` / `ee64a85`）。
  这是面板最大的已知限制，现在不是限制了。
  - **D1 生成器** `tools/fetch-talent-tree.js` → `app/talent-tree.js` **415.3 KB**：
    40 个专精，节点引用 4613 → **全局去重 2891**，条目引用 5387 → 3414 个不同 entryID，
    连线 6579，英雄子树 41，名字字典 3106，图标字典 2094，**中文名 5387/5387 = 100.0%**。
    四个源缓存在 `tools/.talent-raw/`（14 MB，已 gitignore），
    **只把连接后的结果**存进 `tools/.talent-names.json`（271 KB，入库）——
    重新生成不需要那 10 MB 的 `SpellName.csv`，也不需要联网。
  - **一个自查guard 又抓到我自己的错设计**：`next` / `prev` / `freeNode` 全局 interning
    失败 **133 处**（`prev` 90 / `next` 55 / `freeNode` 30）。原因是这些字段**专精门控**：
    同一个法师职业节点，奥术连 108664、火焰连 108656、冰霜连 108655；
    鲜血死骑的某个职业节点是白给的，冰霜/邪恶的不是。
    → 定下规则：**静态字段（坐标/点数/类型/条目）全局共享，拓扑（edges/free）按专精存。**
    这是第三次靠 guard 抓到错设计（166 处 `srcs` 冲突、133 处拓扑冲突、`target.farm` schema 漏洞）。
  - **D2/D3 校验器** `tools/verify-talent-tree.js`：**76377 项检查**，
    schema 写在校验器里（照 `verify-bis-data.js` 的路子）。**变异测试 30/30 全抓**。
    头两轮只抓到 28/30，漏的两个都补成了**实测出来的硬规则**，不是我拍的阈值：
    · 原来「名字字典允许 5% 英文」是我凭空给的宽容度，实测 **0/3106** 不含中文 ——
      改成**任何一项非中文就失败**，并留一个当前为空的 `ALLOW_EN` 白名单，
      将来真出现未本地化的新天赋时报错信息会指到这里。
    · `maxRanks == 0` 实测**只出现在 40 个 subtree 节点上**（条目层面只出现在 80 个 subtree 条目上），
      所以规则从「0~10 之间」收紧成「非 subtree 节点必须 ≥ 1」。
  - **交叉验证拆成了两种诊断**（原来混成一个数，对不上第 7 轮的测量）：
    · **跨专精污染**（entryID 在树里但属于别的专精）：`WARRIOR/PROTECTION 1/77`
    · **引用未知 entryID**（raidbots 里根本没有）：`MONK/MISTWEAVER 4/86`，1 个 ID（125051）
    两个数现在和第 7 轮独立量出来的 1/77、4/85 完全对上。
  - **对齐了两个口径不一致的计数**：生成器说 5387 条目、校验器说 3414，一度以为有 bug。
    独立数了一遍：上游按引用 4613 节点 / 5387 条目，去重后 2893 节点 / 3414 条目，
    产物 2891 节点（差的 2 个是丢掉的占位节点，对应 6 个引用）。
    **全部对上**，生成器的输出文案改成「条目引用」以免下次再误判。
  - **D4 画树**（`app/bis.js` + `app/style.css`，290 行新增）：
    · **坐标不量化。** 计划里原本写「除以 150」，实测坐标最大公约数只有 10，
      而且存在「相差 10」的近重复坐标 —— 除法会把两个节点叠在一起。
      改成**把不同坐标值按容差聚成列/行**：实测容差从 50 到 250 同格重叠**始终为 0**，
      最大 9 列 × 11 行（职业树是严格的 600×600 网格，专精树 600 为主，英雄子树 5×5）。
    · **节点里放全名，不放缩写。** 本来想在圆圈里塞 2 个字，实测**同一棵树内前 2 字重名 496/3447**
      （「强化」既是强化法术吸取也是强化法术反制）—— 那样的标签是**误导**，不是省地方。
      改成方块节点放完整中文名（2322/3447 个名字正好 4 字）。
    · 连线用旋转的 `<i>` 而不是 SVG：全库没用过 SVG，测试用的 DOM 桩也没有 `createElementNS`。
    · 「套路」按钮按人数排序可切换；`state.build` **故意不持久化** ——
      套路编号是生成时的数组下标，重新生成数据后会指向另一套天赋。
    · **顺手修掉了 39 个英文英雄天赋名**：`TraitSubTree` 的中文名 + 把英文名作为
      子树行的第 4 个字段存下来做连接键，**39/39 全部对上**，全部有中文。
  - **D5 渲染断言 + 变异测试**：
    · 发现旧 harness 的天赋渲染是**假绿** —— 只 `renders++`、什么都不断言，
      而且 40 个专精只测第 1 个。现在**每个专精都画一遍**，渲染 83 → 123 次。
    · 新增一行输出：**40 个专精，129 棵树，节点 4304，点亮 3198，连线 6377，点亮 3990，
      点数徽章 412，英雄天赋名 60**。断言含 `树数 === 渲染次数 × 3`（照 `cov === slots` 的路子）、
      点亮率必须在 40%~95% 之间、节点名必须含中文、同一专精内**节点 id 不许重复**。
    · 变异测试 9 个，第一轮漏 1 个：「专精树画成职业树」—— 因为职业树被画两遍时
      节点总数依然合理。补的办法是**把 nodeId 写进 DOM**（`data-node`）并断言不重复，
      重跑 **9/9 全抓**。另外单独变异了英雄天赋中文名那两条，**2/2 全抓**。
  - **D6 README**：「天赋树画不出来」整节重写成「能画出来了，中文名来自暴雪自己的数据表」，
    含两个数据源的表格、连接验证数字、以及**仍然做不到的三件事**
    （导不出游戏能认的串、不能在面板里点着改、上游有 1+4 套脏数据）。
    另外修了「第三步不能省」（现在有四步）、目录结构、`tests.html` 那句「多两项检查」（现在四项）。
  - **发布包核对**：`tools/build-release.ps1 -SkipBuild` → **1630.9 KB / 505 条目**（+133 KB
    就是 `talent-tree.js` 压缩后的大小）。
    **发现并修掉一个真会出事的坑**：`Copy-Item -Recurse` 是按**磁盘**复制的，
    `.gitignore` 管不着它 —— 那 14 MB 的 `tools/.talent-raw/` 本来会被打进用户的包。
    而 `Remove-Item -Force` 不加 `-Recurse` 删不掉目录，所以光加进文件清单没用，
    另开了一个 `$dropDirsFromPkg` 清单。实测确认已排除，同时排掉了 271 KB 的中文名缓存。
  - 验收：`node tools\run-tests.js` **七行全绿** ——
    133 项测试（33/66/34）+ 装备渲染（123 次 / 7947 图标 / 占位块 0 / 轨道徽章 3601 / 部位组 1264）
    + 天赋树渲染（40 专精 / 129 棵树 / 4304 节点）+ 数据格式 2016 项 + 天赋树格式 76377 项。
    临时文件已清空，三个提交都在 `feat/bis-panel`，没有 push、没有 tag、没有 Release。

- **第 9 轮**：给天赋树补**几何断言** —— 这一轮补的是我自己的一个盲区：
  **这棵树我从来没有真正「看」过。** 之前 21 条断言全是「DOM 里有没有这个元素」，
  而树的位置完全由 CSS 绝对定位决定。方块两两叠在一起、或者跑到画布外面，
  上面每一条都照样打「通过」。桩不算布局，所以按 CSS 的数字自己算。
  - **三条新断言**（进 `tools/run-tests.js`，不是一次性脚本）：
    · **不重叠**：同一画布内任意两个方块的中心距不得同时在宽、高之内。实测 4304 个方块 **0 重叠**，最小间隙 12px。
    · **不溢出**：方块必须完整落在画布矩形内。实测 **0 溢出**。
    · **网格聚类契约**：上游 `posX` 相差 ≤ 100 的两个节点**必须**落在同一列，> 100 的**必须不**落在同一列（`posY` 同理）。实测 **0 违约**。
  - **方块尺寸读自 `app/style.css`**（正则抓 `.tnode` 的 `width` / `height`，读不到就抛异常），
    没有写死在测试里。写死的话改了 CSS 忘了改测试，几何检查会拿旧尺寸算出假的「不重叠」。
  - **变异测试 8/8**，其中两个变异是改 **CSS**（把方块改宽 / 改高）而不是改 JS ——
    那正是这组断言存在的理由，两个都被抓到。
  - **有一个变异第一次没抓到，逼出了上面第三条断言**：把聚类容差从 100 改成 0。
    它**既不重叠也不溢出**，因为相差 10 的近重复坐标各占一列后画布也跟着变宽 ——
    树被整体拉宽、节点错位，但前面每条断言都过。「最多 9 列 × 11 行」也抓不到它
    （不同 X 值本来就 ≤ 9 个）。真正能抓的是**把聚类规则本身写成断言**：
    拿渲染结果去对**上游坐标**。加上之后 8/8。
  - 顺带加了「最大 9 列 × 11 行」的上界断言，这个数和第 8 轮 `_box3.js` 独立量出来的一致。
  - 验收：`node tools\run-tests.js` 七行全绿，天赋树渲染那行现在还报
    「方块 4304（重叠 0，超出 0），方块尺寸 62×32（读自 style.css），最大 9 列 × 11 行，聚类越界 0」。
    提交 `f3a4869`，临时文件已清空。
  - **记一条教训**：变异测试漏掉的那一个比抓到的七个有用。抓到的只是确认断言活着；
    漏掉的那个指出了**断言的类别不够** —— 我只检查了「有没有摆坏」，
    没检查「摆的位置对不对」。前者是自洽性，后者要拿产物去对上游。

- **第 10 轮**（提交 `dd71380`）：**推翻了自己写在 README 里的一句话** —— 顺手查「面板能不能
  跟你自己的天赋比」，结果查出天赋导入串的编码解到了一半。
  - 起点：`data/data.js` 里**没有任何天赋字段**（`talent` / `loadout` / `traits` 全是 0 次，
    只有 `specID` 125 次），所以「跟自己的天赋比」在现有扫描数据上做不了。
    但 `WTF` 下 **`TalentLoadoutsEx.lua` / `BtWLoadouts.lua` 存的是官方导入串**，
    36 个文件里挖出 **54 个**去重后的串。
  - **串头 54/54 全解开**：8 位版本（全是 2）+ 16 位 specID + 128 位 `treeHash`，
    标准 base64、每字符内低位在前。解出的 `treeHash` 是**按职业树**的
    （防护/惩戒骑同一个，鲜血/邪恶骑同一个，浩劫/复仇 DH 同一个）。
  - **README 里错了一半的那句话**：原文说导出串要 `treeHash` 和「游戏权威的节点顺序」，
    而「公开数据里没有这两样」。实测 **`fullNodeOrder` 就在 raidbots 那份 `talents.json` 里**，
    而且**同一职业内所有专精的这份顺序完全相同**，正好等于该职业各专精节点集合的并集
    （13 个职业全查：死骑 206 = 114 + 92 + 0；德鲁伊差 2 个、圣骑士差 1 个，其余 0）。
    `treeHash` 确实没有 —— raidbots 每个专精只有 10 个字段，没有 hash。
  - **节点位流的布局是被数据挑出来的，不是我猜的**。18 种布局 × 54 个串扫了一遍，
    最好的是 `sel → purchased → (partial → 6 位点数) → (choice → 2 位下标)`，
    partial/choice **嵌在 purchased 里面**。位宽只能是 6，`--flat`（不嵌）**0/54**，
    还会造出 24~46 个「点数超上限」。
  - **两个假设被数据否掉**（都记下来，免得重走）：
    「布局不对」→ 18 种全试，干净的 0 个；「跟主流 hash 不一致的才失败」→ **事实相反**，
    一致的反而更差（5% vs 42%）。
  - **抓到自己用了一个假指标**。我一开始用「解码再编码，跟原串逐字节比」当证据，
    得到 28/54，还写进了 README。但那是**弱检查**：我读 n 位就写回 n 位，本质是逐位抄一遍，
    字段含义猜错也一致 —— 证据就是把点数位宽从 6 改成 4，一致率只从 28 掉到 27。
    换成能证伪的语义检查（买了别的专精的节点 / 点数超上限 / 二选一下标落在没有选项的节点上）后，
    诚实的数字是 **7/54**。README 和已知限制里的 28 都改成了 7。
  - 产物：`tools/decode-talent-string.js`（9 KB，`<串>` / `--scan` / `--verify` /
    `--flat` / `--rankbits`），中文名从 `app/talent-tree.js` 取，节点顺序从 `.talent-raw` 取，
    缺缓存时提示去跑 `fetch-talent-tree.js`。**`app/` 下没有一行代码用它，面板行为没变。**
  - README 新增一节「天赋导入串：解到一半」，并改了三处旧话（第 307 行的「两样都没有」、
    已知限制那条、目录结构）。新增「待用户决定」第 7 条 + E 任务组。
  - 验收：`node tools\run-tests.js` 七行全绿（数字与第 9 轮一致），临时文件已清空。
  - **记两条教训**：① 顺手查一件小事（「能不能跟自己的天赋比」）比按计划推进更值 ——
    它撞出了一句写在 README 里的错话。② **先怀疑自己的指标**。往返比对看着最严格
    （逐字节相等），其实几乎测不出东西；一个改了字段宽度还不变的指标，测的不是那个字段。

- **第 10 轮（续）**：继续攻 E5，又否掉两个假设，并把工具改成可复用。
  - `tools/decode-talent-string.js` 现在**可以 `require`**：导出 `decode` / `checkSemantics` /
    `loadOrder` / `loadTree` / `scanWtf` / `toBits` / `fromBits` / `setLayout`，
    CLI 只在直接运行时跑。之前它没有任何导出，任何调查都得复制一遍解码逻辑。
  - **先怀疑仪器（第 N 次，又对了一半）**：47/54 栽在「二选一下标放错」上，
    我先去核对 `def.entries` 这个字段名到底存不存在 —— 存在（鲜血死骑 102 个节点 1 项、
    11 个 2 项、1 个 3 项），所以那不是仪器错。但**这些出问题的节点全是
    `type=single entries=1`**，一个没有选项的节点上冒出「选了哪个」，只可能是位流错位了。
  - **新事实：没有一条串是从第 0 个节点就错的。** 每条都先正确解出前 4~42% 才崩，
    而且**同一专精的串，第一个违规下标完全相同**（复原萨满 8 条都是 35，
    复仇 DH 5 条都是 42，浩劫 DH 4 条都是 96）—— 跨不同 hash 也相同。
    内容相关的错位会随串变化，固定下标说明是**顺序表本身**在固定位置和游戏对不上。
  - **假设四（被否）**：「顺序差几个节点」。先做单串修复搜索（在第一个违规处试
    「跳过一个」/「多吃一个节点的位」，回溯 3 格，预算 4）：**7 → 12 条干净，
    修好的 5 条全是 1 个补丁、且补丁下标是该专精的那个固定值**。看着很像成立。
    但真正的检验是**一组补丁要能修好该专精的所有串**（补丁是专精的属性，不是串的属性）。
    按这个更强的约束重搜、预算 12：10 个专精里 **9 个耗尽预算仍然 0/N 干净**
    （惩戒圣骑打了 13 个补丁还是 0/8）。所以「差几个节点」是错的 ——
    单串能修只是补丁把后面的错误吸收掉了，不是找到了真顺序。
  - E5 仍然**没做完**，现在写进 README 和计划的是四条**已排除**的解释，
    留给下一轮的线头是「二选一下标跟着谁走」和「固定下标 35 / 42 / 96 处到底是什么节点」。
  - 验收：`node tools\run-tests.js` 七行全绿，临时文件已清空，
    提交 `b870def`（导出）+ 本轮文档提交。
  - **记一条教训**：一个假设「能修好部分样本」不算成立。补丁越自由，越能把错误吸收进去 ——
    要检验它，就得把它约束到它声称的那个层级（这里是「按专精」而不是「按串」）。
    单串搜索给的 12/54 是虚的，按专精搜索给的 1/10 才是真的。

- **第 11 轮**：无障碍。起因是想核对 README 里「鼠标悬停给中文名 / 二选一 / 层级要求 / 点数」
  这句话是不是真的（是真的，`data-tip` 16 处都在），顺手发现测试从来没检查过任何无障碍属性。
  - **先量，别先修**。写了一次性脚本量四件事，结果**两条是仪器在骗我**：
    · 「2386 个 `data-tip` 元素全都没有可见文字」→ 错，全都**有**。
    · 「621 张图标全都没有 `alt`」→ 错，`app/bis.js` 里是 `img.alt = ''`（**属性赋值**），
      而 `tools/dom-stub.js` 不把属性赋值反射进 `attrs`，所以量到的是桩的缺陷。
    如果照第一次的读数去「修」，就会给 2386 个已经有名字的元素硬塞 `aria-label`
    （反而违反 WCAG 的 Label in Name），再给装饰性图标写上没用的 alt 文字。
  - 修的是**仪器**：`tools/dom-stub.js` 给 `alt / title / src / id / type / value / href /
    width / height / tabIndex` 加了真正的属性↔属性反射（`Object.defineProperty`），
    和浏览器行为一致。加完全套测试仍然全绿，说明反射没改变既有断言的含义。
  - 量准之后真正的缺陷只有一个：**天赋树画布是一堆绝对定位 div 拼的图，没有 role 也没有说明**，
    读屏软件只会念出一串互不相干的天赋名。修法是 `role="group"` +
    中文 `aria-label`。第一版拼出来是「职业天赋**天赋树**，44 个天赋…」——
    `title` 本身已经是「职业天赋」，后面又接「天赋树」，重复了；改成
    「职业天赋，共 44 个天赋，点了 31 个，合计 35 点」（这一句是从渲染结果里
    打出来的真实字符串，不是我照记忆写的）。
    没用 `role="img"`，因为那会把子节点的名字全藏起来。
  - `tools/run-tests.js` 加**第 6 行「无障碍」**，四条断言 + 一个共用的 `checkA11y(n, label)`
    （挂在两个已有的 walk 上，不额外遍历）：图标必须有 `alt` 属性（`alt=""` 合法，
    **缺属性**不合法 —— 缺属性时读屏会去念文件名）、按钮必须有可见文字或 `aria-label`、
    **带 `data-tip` 的元素自己必须有可见文字**（tooltip 是补充信息，不是名字）、
    天赋树画布必须有 `role=group` + `aria-label`。
    实测：7947 图标 / 2859 按钮 / 26378 个 `data-tip` 元素 / 129 个画布，四项全过。
  - **变异测试 5/5**（`tools/mutate-a11y.js`）。其中一个第一次报「漏了」，
    查下去是**我的变异锚点写错了**（凭记忆写了一行 `bis.js` 里不存在的代码），
    脚本因此打了「跳过（找不到锚点）」—— 改成源码里的真实那一行后 5/5。
  - 顺带修了 `app/tip.js` 头注释里两句错话：说有「31 处 title」（实际 48 处），
    说「加上面板」（面板一处都不用 `title`，全走 `data-tip`）。
  - 验收：`node tools\run-tests.js` **八行全绿**，末行改成
    「全部通过：133 项测试 + 装备渲染 + 天赋树渲染 + 无障碍 + 两项格式校验」。
    提交 `8ede310`，临时文件已清空。
  - **记一条教训**：无障碍最容易出的错不是漏改，是**照着坏读数瞎改** ——
    给本来就有名字的元素加 `aria-label` 会让读屏体验变差。所以顺序永远是
    「先确认仪器 → 再量 → 再改」，这一轮四个读数里有两个是假的。

  **第 11 轮后半（提交 `ec49426`）—— 把「靠我记着」换成「让它自己查」**

  - **变异脚本转永久**：`_muta11y.js` 在我自己跑 `rm -f _*` 清临时文件时被删掉了，
    而那时我刚改过 `aria-label` 那一行，正需要重跑它。这已经是第三次了
    （第 9 轮丢了 `_mutrender` / `_muthero` / `_muttree`）。断言留在 `run-tests.js` 里，
    但**证明断言有效的那个东西**每次都得重写。所以它改名 `tools/mutate-a11y.js`，
    不带 `_` 前缀，`rm -f _*` 碰不到它。
  - 转永久带来一个新问题：`tools/` 是被**整个目录递归复制**进发布包的，
    `.gitignore` 管不到。所以它必须写进 `build-release.ps1` 的 `$dropFromPkg`
    （它依赖 `run-tests.js`，而后者本来就被排除，打进去只会给用户一个跑不起来的脚本）。
  - 但「必须写进去」这件事又变成了**靠我记着**，而项目自己的规矩是
    「流程规则用工具兜住，不要靠记性」。于是 `run-tests.js` 加**第 9 行「打包一致性」**：
    扫 `tools/*.js`，凡是提到 `run-tests.js` / `dom-stub.js` 的，都必须出现在
    `$dropFromPkg` 里。实测 3 个（`dom-stub.js` / `run-tests.js` / `mutate-a11y.js`）全在。
  - **守卫自己也验了能失败**：把列表里的一项改成 `NOT-IT.js`，检查报
    「1 个工具会被误打包」、退出码 1。这一步第一次做的时候**没生效** ——
    `sed` 里的反斜杠被 bash 吞了，替换根本没打上，于是我看到的「通过」什么都没测。
    换成 `node -e` 改文件才真的失败。**又一次是仪器的问题，不是被测对象的问题。**
  - 顺带修掉一处自相矛盾的输出：守卫失败时原本仍然打「N 个依赖脚手架的工具
    **全在** `$dropFromPkg` 里」，一句话里既报错又说全在。
  - 验收：`node tools\run-tests.js` **九行全绿**，末行
    「全部通过：133 项测试 + 装备渲染 + 天赋树渲染 + 无障碍 + 两项格式校验 + 打包一致性」；
    `node tools\mutate-a11y.js` **5/5，锚点失效 0**。
  - **记三条教训**：① **别让清理脚本能删掉你的证明工具**。断言是资产，证明断言有效的
    变异脚本也是资产，不是临时文件。② **一个新的永久文件会带出新的约束**
    （这里是「不能进发布包」），而新约束的正确归宿是**一条会失败的检查**，
    不是我的记性 —— 加守卫的成本比记错一次的成本低得多。
    ③ **验证守卫会失败时，先确认变异真的打上了**。第一次试守卫，我用
    `sed 's|tools\\mutate-a11y.js|tools\\NOT-IT.js|'` 去改 `build-release.ps1`，
    结果测试照样全绿，我差点以为守卫写坏了。真相是 **`sed` 里的反斜杠被吞了、
    压根没匹配上**，文件一个字都没改 —— 那个「通过」什么都没测。
    改成先 `grep -c` 确认替换生效、再跑测试，守卫立刻正确报错。
    这和本轮开头的桩保真度问题是同一个毛病的两种形态：**空操作会伪装成好消息**。

- **第 12 轮**：用户一次给了五条决定，这一轮做掉两件（G1 中文名、E5 天赋串），
  顺带补了三道守卫。提交 `6357308`（中文名换 DB2）、`e065c9e`（文档）、
  `9cf9f8c`（天赋串解开）。
  - **G1 中文名：治病根，不是补名字。** 病根是 GearInsight 的 `specRawToCN`
    **按专精英文名建表**，所以 `FROST` 一个键撞死骑和法师（死骑显示成「冰法」），
    `PRESERVATION` / `DISCIPLINE` 干脆没有行。改成从暴雪 DB2 取、**按 specID 建键**，
    结构上就撞不了。新工具 `tools/fetch-class-names.js` → `app/class-names.js`（2.2 KB）。
    解析顺序变成「用户覆盖 > DB2 > 内置表 > 插件表 > 英文 token」。
    实测 13 个职业 + 40 个专精**全中文**，251 → 冰霜、256 → 戒律、1468 → 恩护，
    另外补上唤魔师 / 法师 / 武僧 / 潜行者。
  - **G1 的四道校验，有一道当场救了我。** 生成器要求「有专精的职业数 = 13」，
    第一次跑直接硬失败报「14」。查出来第 14 个是 `ClassID 14 冒险者/Adventurer`
    （只有 1 个专精 1478）；54 行 = 40 个真专精 + 13 个「初始」起始专精 + 1 个冒险者。
    改成两条**互相独立**的定义（专精数 ≥ 3；天赋树 40 个专精所跨职业）且必须一致。
    **一个会硬失败的校验，比一句「看起来对」值钱。**
  - **收紧了三条本来测不出东西的断言。** 旧断言只查「9 个职业有中文名」和
    「另外 4 个非空」——DB2 接上之后它们照样通过，**根本不知道有没有接上**。
    改成「13 个职业一个英文都不许剩」「40 个专精全中文」「死骑冰霜必须等于『冰霜』」，
    再加一条**交叉校验**：DB2 那份和从存档收来的 `L.classZh` 9 个逐字比对
    （两个独立的暴雪来源，不一致说明有一边不是我以为的数据）。
  - **E5 解开了 —— 靠找到「同一个生产者给出的正确解」。** 前十一轮我一直在
    猜布局、然后拿**自我一致性**当证据（读 n 位写回 n 位、位数刚好用完），
    那种检查字段含义猜错了照样通过，所以我「验证」过五个错的假设。
    这一轮 raider.io 的角色 profile 里发现 `talentLoadout` **既有导入串
    （`loadout_text`）又有它自己解出的节点表（`loadout`）**，串和答案同源，
    对不上就一定是我错。第一次比对：真值 76 个节点我**全解出来了**，
    只多 1 个、rank 差 1 处、选项差 1 处 —— 和「7/54 干净」的印象完全相反。
  - **「7/54」不是布局错，是版本不匹配。** 直接判据：同一批角色，
    **赛季历史串干净 3/32，当前 profile 串干净 27/32，「历史脏→当前干净」24 人、
    反向 0 人**。那些历史串是照旧版天赋树编的。
  - 剩下两类残差**都不是解码错误**：① 我多解出的 41 个全是 `granted`
    （raider.io 的 `loadout` 数组不列白给的节点）；② 25 处 `entryIndex` 不符全是
    `type=tiered maxRanks=4 entries=3`，位流里**没有**这个下标，是从 rank 按
    `entries[].maxRanks` 累加推出来的。补上这两条推导 + 「subtree 节点没有
    `maxRanks`、rank 兜底给 1」之后：**32 条串 × 2406 个节点，逐项全对**。
  - **把真值变成永久资产。** `tools/fetch-talent-truth.js`（联网抓）→
    `tools/talent-truth.json`（38 KB，已提交，不含角色名）→
    `tools/verify-talent-decode.js`（离线校验，挂进套件第 9 行）。
    理由很直接：**这个判据我丢过一次就会重新怀疑一轮**。
  - **顺带把 14 MB 的依赖拆掉了。** 解码原来读 `tools/.talent-raw/talents.json`
    （gitignored），所以校验器在新克隆上跑不起来。把 `fullNodeOrder` 加进
    `app/talent-tree.js`（`nodeOrder`，按**职业**存一份 18.2 KB；
    「同职业各专精顺序一致」先实测 13/13 一致、再在生成器里加硬守卫）。
    换数据源之后用同一份真值复测，**35/35 逐项全对、三项各 2630 处零不符** ——
    重构的唯一合格判据是「测量结果一字不变」。
  - **仪器错了六次，全在同一类毛病上：没验证就用。** ① `if (!env.load(f)) throw` ——
    `env.load` 返回 `undefined`，永远抛。② `T.decode(s)` 漏传 `bySpec` → 解出 0 个节点。
    ③ 又在 `decode` 之外重复调用 `checkSemantics` 并传错参数（`decode` 内部本来就调过）。
    ②③ 叠加产生了「0/23」这个和事实相反的读数。④ profile 里 `region`/`realm`
    是**字符串**，排行榜里是**对象**，我按对象取 `.slug`，配对键成了 `undefined/undefined/名字`
    → 0 对。⑤ 变异测试和套件**并行跑**，套件读到被改坏的 `app/bis.js`，
    报「7947 个 `<img>` 没有 alt」，同一次输出里 mutate-a11y 又是 5/5 —— 竞争，不是缺陷。
    ⑥ 又一次栽在 bash 吞反斜杠上，缓存目录守卫的变异「锚点没命中」却打印了通过。
  - **每一个仪器错误都留下了一道会失败的检查**，而不是一条我要记住的规矩：
    `decode` 缺 `bySpec` 直接抛；`checkSemantics` 收到整张表直接抛；
    新增 `tools/mutate-lock.js`（变异工具占锁、套件见锁拒跑，用 `AE_MUTATING`
    区分「父进程是变异工具」和「真的并行」）；打包守卫**扩宽判据**
    （`mutate-*.js` 一律测试专用 —— 原判据「引用脚手架」漏掉了跑校验器的
    `mutate-decode.js`）并**新增缓存目录检查**（`.gitignore` 里每个
    `tools/.xxx/` 都必须在 `$dropDirsFromPkg` 里）。
  - **守卫连着三次自己抓到我漏登记的新文件**（`mutate-names.js`、`mutate-decode.js`、
    `mutate-lock.js`、`mutate-pkg.js`），每次都是在真实使用中报错、不是演练。
  - 新增第四个变异套件 `tools/mutate-pkg.js`（4 个变异：删脚本、拼错脚本、
    删缓存目录、拼错缓存目录），把「守卫本身有效」也钉住。
  - 验收：套件**十行全绿**，末行「全部通过：134 项测试 + 装备渲染 + 天赋树渲染 +
    无障碍 + 两项格式校验 + 天赋串解码对真值 + 打包一致性」；
    四个变异套件**串行** 20/20、锚点失效 0；锁双向都验过能拦（手动占锁时套件拒跑、
    变异工具互相拒跑，释放后恢复）。
  - **教训**：① **自我一致性不是证据，要找同一个生产者给出的正确解。**
    十一轮猜布局，抵不上一份 `loadout_text` + `loadout` 配对。
    ② **「解不开」要先分清是我错还是数据版本不同** —— 7/54 里绝大部分是旧版串。
    ③ **重构的唯一合格判据是测量结果不变**，不是「跑起来了」。
    ④ **两个互斥的读数摆在一起时，先怀疑它们不是同一次测量**（并行竞争）。

### 第 13 轮：天赋树有图标了（G4 完成）

- **背景**：用户核实了三件事 —— raider.io/WCL 到底能不能访问、天赋树为什么只有文字、
  为什么没有导出。前两件本轮有结论，第三件（导出）**还没做**，进第 14 轮。
  - **raider.io 能用，不需要代理**，瓶颈是成本不是可达性（profile 492 ms / 40 KB，
    `fields=gear` 裁到 9.6 KB；排行榜每页 100 人、98% 带官方天赋串）。
  - **WCL 拿不到数据**：域名通，但 v2 API 要 OAuth `client_id`/`secret`，
    本机没有凭据，`/oauth/token` 返回 405。这不是「访问方式不对」。
  - **天赋树无图属实** —— 图标**名字**早就在 `app/talent-tree.js` 的 `icons` 里（2094 个），
    缺的只是图片文件。所以先做 G4。
- **尺寸选 56**（不是 18）。实测两档都 40/40 可得，56 平均 2626 B、18 平均 996 B，
  推算 5.24 MB / 1.99 MB。选 56 的理由：装备图标已经是 56（混用会一大一小）；
  节点里显示 24 px，56 缩下去清晰、18 放大到 24 明显模糊；高分屏更糟。
- **19 张下不到，原因不是「源没有图」，是上游给的名字不存在。** 这一条花了最多时间。
  - 两个源（暴雪 CDN / zamimg）对这 19 个名字**都是 404**，影响 **25/40 个专精、29 个节点**，
    不能当噪声忽略。
  - 拿 entry 里的 `spellId` 去 wowhead 的 spell tooltip 查真名，19/19 全部查到：
    **16 个是 raidbots 把真名里的连字符规范化成了下划线**
    （`spell_frost_ring-of-frost` → `..._ring_of_frost`、`warlock_-bloodstone` →
    `warlock__bloodstone`），**3 个是新图标只有数字 FileDataID**
    （升腾三个变体 → 8026696/7/8，瞄准射击 → 8026692，破天投枪 → 8026700）。
    有意思的是连字符版只有 zamimg 有，数字版反而只有暴雪 CDN 有 —— 所以两个源都要试。
  - **存盘用 raidbots 那个坏名字。** `app/talent-tree.js` 的 `icons` 字典就是坏名字，
    面板按字典取文件名。若按真名存盘，`app/` 就得再带一张映射表 ——
    那张表会变成第二个要跟着上游维护的真相来源。**文件名当键，坏名字就只是个键。**
  - 「坏名字 → 真名」落成 `tools/.talent-icon-fix.json`（**提交进仓库**：它是查出来的事实，
    重查要联网 + 代理，克隆的人不该被迫重跑）。
- **仪器又错了一次，而且是同一个毛病：把 HTTP 错误当成了数据。**
  修复通道第一版并发 2、无重试，结果 **19 个只修好 1 个**，看起来像「wowhead 查不到真名」。
  复现出来是**第一条 200、之后全是 502** —— 查得太快被挡。502 被我 `code !== 200`
  一律归进「这个名字修不好」。改成并发 1 + 5xx 退避重试 + 请求间隔，并把
  「查询失败 HTTP nnn」和「真名两个源都下不到」**分开报**，立刻 18/18 全修好。
  **教训**：`!== 200` 这种写法会把「问得太快」和「确实没有」压成同一个结论。
- **一次真正的假通过。** 图标接进 `renderTreeGrid` 之后套件全绿，而且
  「渲染检查」那行仍然报 **7947 个图标** —— 和一张天赋图都没有的时候**一模一样**。
  原因：天赋渲染走 `checkTalents`，而数图标、验 src、查文件存在的代码只在 `checkRender` 里。
  **4304 个节点、2094 张图，一条断言都没看过。**
  数字没变化本身就是证据 —— 加了 2094 张图而计数一字不差，只可能是没数到。
- **补了 5 条断言**，其中第 5 条是关键：
  ① 每个节点必须有图标（`没有图标 0`）；② src 必须指向 `app/talent-icons/`（`路径错 0`）；
  ③ 文件必须真的存在（并进原有的 `missingFiles`）；④ 必须有 `class=ti`（`缺 class 0`）——
  实测漏过一次：img 建出来了、图也在，但没设 class，`style.css` 那一整段全落空；
  ⑤ **图标和名字必须来自同一个 entry**（`图文配对 4304，图文不符 0`）。
  前四条加起来抓不住「图标取错 entry」：把 `icons[ent[2]]` 写成 `icons[ent[1]]`
  照样得到存在的图标名、存在的文件、正确的前缀 —— 四条全过，而二选一节点会图文不符。
  第 5 条对着上游反查：显示的名字属于哪个 entry，图标就必须是那个 entry 的。
  为此把 `nodeLabel` 里选 entry 的逻辑抽成 **`nodeEntry`**，图文共用同一个选择。
- **新增第五个变异套件 `tools/mutate-icons.js`，6/6。** 它比前四个多一道契约：
  每个变异必须被**指定的那条**断言抓到（`want` 关键词），只要退出码非零不算过 ——
  「抓到了但是别的断言抓的」报成「串了」。6 个变异：不挂图标、固定取第一个 entry、
  不设 class、去装备图标目录找、**反查一次都不做**（证明「图文配对 < 3000」这条下限守卫
  不是空的）、**把一张真在用的图标文件改名**（证明文件存在检查真的在查盘）。
  还额外验了「还原之后基线仍然通过」。
- **几何**：节点框 62×32 → **62×50**（图标 24 在上、名字在下），`CELL_H` 44 → **62**
  保住 12 px 间隙，点数徽章改成右下角绝对定位（留在流里会把总高顶到 58 px、超出框）。
  几何断言自己从 `style.css` 读尺寸，套件如实报出 **62×50**，重叠 0、超出 0。
- **打包**：`build-release.ps1` 的 `$dropFromPkg` 加 `mutate-icons.js` 和
  `.talent-icon-fix.json`（守卫立刻认到 **8 个测试专用脚本**）。
  包 1630.9 KB → **7065.5 KB**，包内 2606 条，**天赋图标 2094 张与 `icons` 字典一一对应**，
  该丢的脚本 / 缓存目录泄漏 0。
- **核包的仪器也错了一次**：zip 条目名用**反斜杠**分隔，我用 `app/talent-icons/`
  匹配 → 读出「图标 0 个」而且「`app/style.css` 也缺」。
  **两个读数里有一个明显不可能，整个测量就不能信** —— 归一化路径后才是真结果。
  顺带第三次栽在 heredoc 吞反斜杠上（`/\/g` 被写成 `/\/g`），改用 write 工具。
- 验收：套件十行全绿，天赋树渲染那行现在带
  「图标 4304（没图标 0，路径错 0，缺 class 0，图文配对 4304，图文不符 0）」；
  五个变异套件**串行** 26/26、锚点失效 0、串 0。提交 `268ef21`。
- **教训**：① **计数一字不变，就是「没数到」的证据** —— 加了 2094 张图而图标数不变，
  比任何报错都清楚。② **`!== 200` 会把「问得太快」和「确实没有」压成同一个结论**，
  重试和分类报错要一起加。③ **上游的「规范化」会造出不存在的名字**；
  修它的时候要想清楚**哪一边当键** —— 键选错就多一张要维护的映射表。
  ④ **变异只要求「退出码非零」太松**，得要求「被指定的那条断言抓到」。

### 第 13 轮（续）：G3 的抽样策略量清楚了，并翻掉第 12 轮一个结论

- **翻案：raider.io 每专精排行榜的 `spec` 参数确实过滤。** 第 12 轮我写的是
  「`spec` 被忽略，返回整个职业」，依据是「请求 frost 却返回 250×20 251×25 252×55」。
  本轮做了第 12 轮没做的那一问：**换 `spec` 值，返回的人是不是同一批**。
  实测 priest 三个专精的 p0 互相只重叠 **2~3 / 100** —— 是三批不同的人，参数生效。
  第 12 轮看到的「混着别的专精」是另一回事：榜单按**打榜时的成绩**排，
  而 `character.spec` 是这个人**现在**的专精，两者本来就会不一致。
  **教训：「返回的内容里混着 X」不能推出「参数没生效」。要证伪一个过滤参数，
  得换参数值看返回的集合是否改变，而不是看返回集合的内部构成。**
- **`spec=all` 不是合法值**（HTTP 400 `could not find requested class/spec combo`），
  不传 `spec` 也 400（`Invalid request query input`）。必须逐专精请求。
- **串头里的 specID 才是专精真值，而且和站点字段一致。** 用已提交的解码器离线读
  1091 条串的头部（8 位版本 + **16 位 specID**），和 `character.spec` 逐条比：
  druid/balance 8 页 800 人，站点 `{balance:629, guardian:99, restoration:57, feral:14}`，
  串头 `{平衡:625, 守护:99, 恢复:57, 野性:14}` —— 差的 4 个正是没带串的那 4 个人。
  **这条很有用**：以后不必信任站点的 spec 字段，串自己带真值，而且是离线可验的。
- **翻页**：druid/balance p0..p5 共 600 人**去重 600、重复 0**；p20 / p60 仍然 200 且满 100 人，
  没探到上界。带串率 98~100%。
- **命中率（决定翻几页）**：holy 90/100、shadow 79/100、balance 79/100、
  **discipline 只有 39/100**。按最低值算，**每专精 3 页足够凑 100 人**。
- **G3 预算据此定下**：约 100 次榜请求（29 MB）+ 4000 次 profile（`fields=gear`，
  9638 B/人，38 MB）≈ **67 MB / 4100 次请求**，并发 4 约 9 分钟，打包前一次性。
  全部数字连同「未量的一件事」（40 个专精只量了 4 个的命中率，冷门专精可能翻 3 页不够，
  工具要按实际命中数决定深度并显式报告凑不够的专精）已写进上面 G3 条目。
- **两次限速经验合并成一条设计约束**：第 12 轮 raider.io 并发 6 在 18 个里出 1 个
  status 0；本轮 wowhead 并发 2 下第一条 200、之后全 502。
  **没有 429 不等于没有限速**；5xx 和 status 0 必须当「问得太快」退避重试，
  当成「没有数据」就会得到一个自信的错误结论（本轮修图标名时就得到过 1/19）。

- **G3 动手前的三项实测（第 13 轮后半）**：
  - **slug 不用维护表。** `class=` / `spec=` 的 slug 可以从 `app/talent-tree.js` 里现成的
    `cls` / `specEn` 机械推导（小写 + 空格转连字符），拿站点自己当判据逐对验证，
    **40/40 全部被认下来**，没有一个是我手写的。验证时只读状态码就掐断连接，
    省掉 11.6 MB 白下载。这条符合本项目的老规矩：**别手写上游的标识符，让上游确认。**
  - **profile 装备形状**（43 份缓存 / 687 件，离线数出来的）：槽位键 **15 个恒有**，
    `shirt` 24/43、`offhand` 18/43 —— 第 12 轮记的「16 槽位」是单份样本的数，
    双持 / 盾才有 `offhand`。每件恒有 `item_id / item_level / icon / name /
    item_quality / is_legendary / bonuses`，而 `enchant` 只有 339/687、`tier` 只有 168/687，
    **这两个不能当必填字段验**。`icon` 和 `item_quality` 是白送的 ——
    那正是 `fetch-icons.js` 现在要逐个查 wowhead 才拿到的两样。
  - **⚠ 一条会改变 G3 形状的事实：raider.io 的物品名 0/687 是中文，全是英文。**
    BisData 给的是中文名，直接换过去就是**功能退化**。补名字的路已经量到：
    `nether.wowhead.com/tooltip/item/<id>?locale=4` 给中文（`271456` →「翡翠督军的淬火战角」）。
    locale 码是试出来并留下证据的：0=enUS 1=koKR 2=frFR 3=deDE **4=zhCN** 6=esES 7=ruRU 8=ptBR。
  - **同一个坑本轮踩了第二次。** 试 locale 时 0/2/4/5/6/9 全部 502，看着像「不支持这些 locale」；
    加退避重试后 0/2/4/5/6 全部 200。**又是限速，不是不支持。**
    前一次是修天赋图标名（1/19 的假读数）。规矩我已经写下来了却还是又犯一次，
    说明它必须**进代码而不是留在纸上**：抓取工具的 HTTP 助手要**默认**对 5xx 退避重试，
    而不是每个调用点自己记着加。

### 第 13 轮补记：G3 抓榜全量实测（40 个专精）

`node tools\fetch-rio.js --rank-only --target 100 --maxpages 6`：

- **98 页 / 3994 个角色 / 25.52 MB / 101 秒**，请求 93 次、**重试 0、失败 0**（状态码全 200）。
  并发只用在装备那段，榜是串行抓的 —— 一页 290 KB，串行已经够快。
- **带天赋串 3960 / 3994 = 99.1%**。之前量的「98~100%」得到确认。
- **翻页深度**：多数专精 **2 页**就够 100 人；4 页的有冰法、狂怒战、冰霜死骑、生存猎；
  毁灭术 5 页；**痛苦术翻满 6 页仍只有 94 人**（唯一不足的一个）。
  所以 `--maxpages 6` 是合适的默认值，而「不足就显式报出来」这条设计当场就用上了。
- 「命中率最低是戒律 39%」那个数只是**单页**的抖动。全量看下来，
  按 `character.spec.id` 筛之后，绝大多数专精 2 页（200 人）就能滤出 100 人。

### 第 13 轮补记：G3 抓装备撞上 429 限速（重要）

`node tools\fetch-rio.js --target 100 --maxpages 6`（并发 4、请求之间无间隔）：

- **请求 5862 次，200 只有 303，429 有 5558，用时 474 秒，最后只拿到 361/3994 份装备。**
  前 300 份是顺的（进度条上 0→300 一路 `失败 0`），之后几乎每一个都被 429 挡掉。
  这不是偶发拥塞，是 **raider.io 对 profile 端点有配额**。
- **同一个坑本轮踩了第三次，而且这次是我自己写的代码在踩。** `getRetry` 的重试条件
  写的是 `e || code >= 500` —— **429 不在里面**，于是每个被限速的角色都被当成
  「这个人没有装备」丢掉。19 个专精 `nGear=0`，**产物照样生成、校验器照样放行**。
- **校验器也有同一个病**：它报的「每专精最少 94 人」是**榜单人数 `n`**，
  而不是**真的拿到装备的人数 `nGear`**。94 是好看的数字，361/3994 才是真相。
  这正是我要替换掉的 BisData 缺陷（没有样本量）在替代品自己的校验器里重演。
- 改法（两处都进代码，不留在纸上）：
  - `getRetry`：429 计入「问得太快」，退避从 `RATE_WAIT`（默认 65 秒）起步逐次加倍，
    重试次数给到 8；并且**全局降速** —— 一吃 429 就把所有后续请求的最小间隔加倍
    （上限 2 秒）且**不再自动降回来**。默认并发从 4 降到 **1**，最小间隔 **1100 ms**。
    这几个都做成命令行可调（`--conc / --gap / --ratewait / --retries`）。
  - 校验器：`nGear` 才是「这份产物能不能用」的判据 —— nGear 少于目标就**硬失败**，
    报告行同时打 `n` 和 `nGear` 两个数。
- **榜单端点不受限速影响**（98 页全 200、重试 0），所以这套只加在 profile 段。

### 第 13 轮补记：G3 装备段撞上 429 限速 —— 同一个坑的第三次

第一次全量抓装备（并发 4、请求之间无间隔）的**实测读数**：

- 请求 5862 次，**200 只有 303，429 有 5558**，用时 474 秒，最后只拿到 **361/3994** 份装备。
- 前 300 份左右是顺的，之后几乎全被 429 挡掉 —— 说明 raider.io 的 profile 端点**有配额**，
  不是偶发拥塞。榜单端点不受影响（98 页全 200）。
- 当时 `getRetry` 只对 **5xx / 网络错**退避，429 属于 4xx 被判为「你问错了」直接丢掉，
  于是「限速」被记成了「这个角色没有装备」。

**后果是一份看起来正常的坏产物**：`app/rio-data.js` 照样生成、517.2 KB、40 个专精，
`verify-rio-data.js` 还报「格式全部通过 / 每专精最少 94 人」。但真实情况是
**19 个专精的 nGear = 0**，37/40 个专精装备样本不足 30，装备合计只有 361 人。
校验器引的是**榜单人数 `n`**，而能不能用取决于**装备人数 `nGear`** —— 两个数被混为一谈。
这正是我要替换掉的 BisData 缺陷（没有样本量），出现在了替代品自己的校验器里。

三处都改了，而且都改成**会失败的检查**而不是「我记着」：

1. **`getRetry` 认 429**：429 与 5xx / 网络错同列为「问得太快」，退避从 `RATE_WAIT`（默认 65 秒）
   起步逐次加倍，重试次数默认 8。另加**全局节流** `throttle.gap`：吃到 429 就把
   所有后续请求的间隔加倍（上限 2 秒），且**不再自动降回来**。
2. **发射前守卫**：`fetch-rio.js` 在写产物之前逐专精检查 `nGear`，任何专精低于
   `--mingear`（默认 min(30, target)）就**拒绝生成**，并逐条列出缺多少。
   原来的守卫只挡「一份装备都没拿到」，19 个专精各自为 0 照样放行。
3. **校验器区分两个样本量**：`nGear == 0` 变成**硬失败**（附上榜单人数，把
   「榜上有人但装备没抓到」直接说出来），报告分「榜单样本 / 装备样本」两行，
   警告也改看 `minGearN`。改完立刻拿那份坏产物验证：**19 条硬失败，如实拒绝**。

**降速后的实测**（`--specs 70 --target 12`，并发 1 + 间隔 1100 ms）：
**12/12 全 200，429 零次**，12 个请求 12 秒 ≈ 1 请求/秒。
专精 70 在上一轮是 `nGear=0` 的那 19 个之一 —— 同一批角色、同一个端点，
慢下来就全拿到了。**「拿不到」是我问得太快，不是上游没有。**

按 1 请求/秒算，3994 份装备约 **67 分钟**。这是一次性的打包前成本，
但单次跑太长，所以改成**分批**（`--specs` 一批几个专精），
缓存 `tools/.rio-raw/prof-*.json` 是 skip-if-exists 的，中断了下次接着跑。

#### 间隔到底要多大：250 个请求的持续实测

1.1 秒是撞完 429 之后拍的保守值，全量 4000 份要 73 分钟，值得再量一次。

| 并发 | 间隔 | 请求数 | 200 | 429 | 用时 |
| --- | --- | --- | --- | --- | --- |
| 4 | 0 | 5862 | 303 | **5558** | 474 秒（崩） |
| 1 | 1100 ms | 12 | 12 | 0 | 12 秒 |
| 1 | 500 ms | 60 | 60 | 0 | 32 秒 |
| 1 | **500 ms** | **251** | **248** | **0** | **154 秒** |

最后一行是决定性的那次：**越过了上次崩塌的第 300 个请求这个关口**（上次是前 300 个顺、之后全 429），
250 个请求一次 429 都没有。剩下 2 个是 **400**——真实的「查不到这个角色」（改名/转服），
不是限速；1 个 ERR 被退避重试救回。所以：

- **并发 1 + 间隔 500 ms 是安全的**，全量 4000 份约 33 分钟。
- **崩塌的原因是并发，不是总量。** 并发 4 时四条连接同时打，瞬时速率是 8/秒；
  并发 1 + 500 ms 是稳定的 2/秒。raider.io 限的是瞬时速率。
- 400 要和 429 分开报：前者是「这个角色没了」，后者是「我问得太快」。
  混在一起的话，几十个改名角色会看起来像限速，反过来也一样。


### 第 13 轮补记：G3 全量抓取跑通（这是换数据源的分水岭）

`node tools\fetch-rio.js --target 100 --maxpages 6 --gap 500`：

- **3994 个角色 → 3977 份装备**，用时 **2829 秒（47 分钟）**，流量 29.28 MB。
  状态码 `{200: 3444, 400: 17, ERR: 8}` —— **429 一次都没有**。
  命中缓存 533（上一轮撞 429 时侥幸拿到的那批），新下 3444。
- 17 个 400 是**真的查不到这个角色**（改名 / 转服 / 删号），不是限速。
  这类失败必须和 429 分开报，否则又会退回「把限速读成没数据」那个坑。
- **中文名 2432/2432 = 100%**。DB2 那条路在 2432 个不同物品上仍然全命中，
  比之前 402 个 id 的样本大了 6 倍。
- 产物 `app/rio-data.js` **849.3 KB**，40 专精 / 2432 件 / 636 部位组 / 13523 行 /
  3960 条天赋串，校验器 **6562 项检查全过、0 警告**。

**和 BisData 的对比（这就是换的理由，全是可量的）：**

| | BisData（GearInsight） | AE_RIO（raider.io） |
| --- | --- | --- |
| 样本量 | **一个都没有** | 每专精 94~100，**每部位单独一个 N** |
| 每部位列几件 | 截断到 1~7 件 | **不截断**，出现过的全列 |
| 使用率之和 | 中位数只有 **72.9%** | **恒等于 100%**（校验器的恒等式） |
| 装等 | 无 | 每件带平均装等 |
| 图标 / 品质 | 要另外查 wowhead | **上游白送** |
| 天赋串 | 无 | 3960 条真串 |
| 赛季 | 不明 | `season-tww-3`，写在产物里 |

- 「最小部位样本量 1」是**真实情况**，不是 bug：比如某专精只有 1 个人带副手。
  校验器如实报出来，面板也必须把这个 N 显示给用户 —— 那正是 BisData 藏起来的东西。

### 第 13 轮补记：把面板切过去之前的覆盖关系实测

`_cover.js` / `_cover2.js`（离线，读两个产物）。**先量再改，因为这一量改变了 G3 的形状。**

第一版探针在 BisData 那侧读出「0 个部位组」，而前面明明量到 1264 组 —— 又是仪器坏了：
**BisData 的装备不在 `slots` 里，而是分成 `raid` 和 `mplus` 两套各 15 槽**。修好后：

| | 部位组 | 覆盖率中位数 | 候选件数中位数 | 不到 50% 的组 | 样本量 |
| --- | --- | --- | --- | --- | --- |
| `BisData.raid` | 632 | **69.7%** | 3 | 111 | **没有这个字段** |
| `BisData.mplus` | 632 | **75.2%** | 3 | 95 | **没有这个字段** |
| `app/rio-data.js` | 636 | **100%** | **20** | **0** | 中位数 **100** |

专精覆盖：**40 vs 40，specId 完全一致，两边都不多不少**。

**⚠ 但 raider.io 只能替换掉「部位分布」这一块。** 面板现在还在用 BisData 的这些东西，
而 rio 的榜单 + profile 里**根本没有对应物**：

- `weights` 属性权重、`target` 单体/多目标偏好、`tol` 装等容差 —— 这些是**模拟器的输出**，
  不是从别人身上能看出来的东西。raider.io 给不了，我也不能编。
- `gems` 推荐宝石、`ench` 推荐附魔 —— rio 的 profile 里**有 `enchant` 字段**（687 件里 339 件带），
  所以这两样有可能从真实数据里数出来，属于**还没榨干的部分**。
- `ilvl` 平均装等 —— profile 每件都有 `item_level`，能算，只是我还没算。
- `weapon` 武器类型分布 —— 能从主副手的物品推，但要 DB2 的物品类别，成本另计。
- 顶层 `srcs` 掉落来源、`tracks` 升级轨道、`consumables` 消耗品 —— rio 完全没有。
  `srcs`/`tracks` 是「这件东西哪来的、能升到几」，属于静态物品知识，不是榜单数据。

所以「不再用 GearInsight 当数据源」这条要拆开看：**每部位穿什么、多少人穿**（G3 的核心，
也是 BisData 唯一真正有缺陷的部分）可以完全换掉；**权重 / 宝石 / 附魔 / 来源 / 轨道**
换不掉，除非砍功能。我不打算悄悄砍功能，也不打算把 rio 没有的数编出来。

**已记进「待用户决定」。** 我的默认做法：部位分布切到 rio 并显示真实 N，
其余字段继续用 BisData 但在界面上**注明各自来源**，不混成一句「数据来自 X」。

rio 白送、BisData 没有的：物品图标名 `i` + 品质 `q`（省掉 `fetch-icons.js` 的大部分活）、
**插槽标记 `sock` 494 件**（B6 卡了很久的那个）。

### 第 14 轮：接面板之前，先量图标缺口

打算让 rio 的分布直接用它白送的 `i`（图标名）+ `q`（品质），
省掉 `fetch-icons.js` 逐个查 wowhead 那一步。**但省不掉全部**，量出来的缺口如下：

- rio 的 **2432 件**物品，`i` 字段 **2432/2432 有图标名（100%）**，一个不缺。
- 但**名字对应的 jpg 只有 966 件（39.7%）在 `app/icons/` 里**，
  缺 **1043 个不同的图标名**。原因很简单：`app/icons/` 那 462 张是按 BisData 的
  498 件下的，而 rio 的分布不截断，物品数是它的 4.9 倍。
- 反过来看：`AE_ITEM_ICONS` 覆盖 568 个 itemId，其中只有 **497/2432（20.4%）**是 rio 用到的。

**双源对比（497 件两边都有的物品）——这是「rio 的字段可不可信」的直接检验**：

- **品质 `q`：一致 494/497（99.4%）**。不一致的 3 件（277783 / 279009 / 279010）
  rio 说 4（史诗）、本机 `item-icons.js` 说 3（精良）。
- **图标名 `i`：一致 496/497（99.8%）**。唯一不一致的 268240，
  **rio 给的是 `inv_misc_questionmark`**（占位问号），本机给的是数字 FileDataID `8095063`。
  也就是说这一件 rio 是错的 / 缺的 —— 印证了 G4 那条教训：
  **上游给的图标名不总是真名**，不能当成权威。

**结论**：rio 的 `q` / `i` 可以用（99%+ 一致），但**不能因此认为图标齐了**。
要么补下 1043 张（约 2.6 MB，按 G4 实测均值 2573 B 估），要么让缺图的物品落回占位块。
这两条都不改变数据正确性，只影响包大小和观感 —— 我按「先接面板、图标缺就出占位块」推进，
补图放到面板能跑之后再做，免得一次动两件事说不清是哪件出的问题。

### 第 14 轮：面板接上 AE_RIO —— 「套件全绿」差点又是假绿

把「实战分布」视角接进 `app/bis.js` 之后，套件**全绿**。但渲染检查的数字
**一字未变**：仍是 123 次渲染、1264 个部位组、7947 个图标。

新视角一次都没被画过。原因是 `tools/run-tests.js` 的渲染循环写死了
`['raid', 'mplus']` 两个视角，而且预载列表里没有 `app/rio-data.js`
（浏览器里那是懒加载的，测试里不显式加载就等于没有）。

**这正是我自己写在断言旁边的那句话**：「因为没数据所以跳过的测试报成通过，
是最难发现的假绿」。这一次它换了个形状回来 —— 不是「跳过」，是**新代码从未被执行**，
而总量断言只会从 1264 掉到 1264，因为它本来就没被算进去过。

修仪器：
1. 预载列表加 `app/rio-data.js`，并注明它是**入库的产物**，缺了就该红（不是可选下载物）。
2. 渲染循环改成 `['raid', 'mplus', 'rio']` —— 40 个专精 × 3 个视角。
3. 徽章分两种验，各自一条正则：`cov`（BisData）「记录 95.9%」、
   `cov sn`（rio）「N=97」。共用一条正则的话两边都验不住。
4. 恒等式从 `cov === slots` 改成 **`cov + sn === slots`**（每个部位组头上恰好一个徽章）。
5. 补三条 rio 专属断言：`rioRenders === 40`、`rioSlots >= 600`、
   **`sn === rioSlots`**（样本量徽章只在 rio 视角出，必须一一对应）。

修完仪器后的实测：渲染 **123 → 163** 次，部位组 **1264 → 1900**
（新增 636 = rio 的部位组数，和校验器数出来的 636 一致），
图标 **7947 → 21470**，data-tip 元素 26538 → 59960。

**然后它立刻抓到一个真缺陷**：
```
引用了 1042 个不存在的图标文件：app/icons/inv_helm_plate_raiddeathknightethereal_d_01.jpg …
```
rio 有 2432 件物品、图标名 100% 齐全，但 `app/icons/` 里只有 **966** 件对得上
（39.7%）—— 那 462 个 jpg 是按 BisData 的 498 件下的。缺的 1042 个图标名
在包里没有对应文件。浏览器里 `img.onerror` 会把碎图去掉，所以**不会显示碎图标**，
但也就是说 1042 行装备**没有图**，而且是静默的。

这条断言的价值就在这里：它把「静默退化」变成了「构建失败」。

**已补齐（同一轮内）。** `tools/fetch-icons.js` 加了一个 rio 名字入口：
`collectIds()` 读 `app/rio-data.js` 的 `items[].i`，并入 `extraNames`。
**故意不并进 `ids`** —— `ids` 是「要去 wowhead 查图标名的 itemId」，而 rio 的名字
已经是现成的（游戏客户端出来的），混进去等于白跑 2432 次网络请求，而且 wowhead
给的变体名（实测 `..._white-`、`..._red--`）还会盖掉正确的那个。

实测跑 `--images-only`：识别出「共 1515 个不同图标，需要下 1052」（1042 个 rio 缺口 +
少量消耗品），**1052/1052 全部下到，0 失败**：
- 暴雪 CDN **996** 个（直连，不用代理）
- zamimg **56** 个（必须走代理，直连 403）—— 又一次印证「当前赛季的新图只有 zamimg 有」

`app/icons/` 462 → **1515 个文件，3574.8 KB**（原 ~1 MB）。
套件复跑：**占位块 0**，渲染检查通过。

### 仪器修好之后，数字才动了

这一轮最值得记的不是补图，是**「全绿」和「新代码跑过」是两件事**：

| | 加 rio 视角前 | 加了视角、仪器没改 | 仪器也改了 |
| --- | --- | --- | --- |
| 渲染次数 | 123 | **123** | 163 |
| 部位组 | 1264 | **1264** | 1900 |
| 结论 | — | 全部通过 | 抓到 1042 个缺图 |

中间那一列是假绿：面板里新增了一个视角、一份 849 KB 的数据、一套新的行格式转换，
套件却**一个数字都没变**，因为渲染循环只跑 `['raid', 'mplus']`。
这正是我自己写在 `run-tests.js` 里的那句话——「因为没数据所以跳过的测试报成通过，
是最难发现的假绿」——只不过这次跳过的不是数据，是**视角**。

所以 `run-tests.js` 这次改了四处，其中三处是「让漏检不可能再发生」：
1. `app/rio-data.js` 进**硬加载列表**（缺了就抛，不是「可选」）；
2. 渲染循环变成 `['raid', 'mplus', 'rio']`，40 个专精 × 3 = 120 次；
3. 徽章断言按 class 拆成两条正则（`记录 95.9%` / `N=97`），**共用一条会两边都验不住**；
4. 恒等式从 `cov === slots` 改成 **`cov + sn === slots`**，并加一条独立断言
   `sn === rioSlots`（样本量徽章只在 rio 视角出，个数必须正好等于 rio 的部位组数）。

第 4 条里那条独立断言是关键：rio 视角要是一个部位都没画，总量断言只会从
1264 掉到 1264 —— 因为它本来就不在分母里。**总量断言防不住「整块功能没跑」，
只有「这块自己的计数」能防。**

### 发布包体积重测（第 13 轮，rio 数据 + 补图标之后）

`tools\build-release.ps1 -SkipBuild` 实测，**3662 个条目 / 9864.6 KB**（上一次是
7065.5 KB / 2606 条目）。构成（压缩后）：

| 部分 | 个数 | 压缩后 | 解压 | 占比 |
| --- | --- | --- | --- | --- |
| 天赋图标 `app/talent-icons/` | 2094 | 4971.7 KB | 5265.9 KB | 54.2% |
| 装备图标 `app/icons/` | 1515 | 3383.9 KB | 3574.8 KB | 36.9% |
| `app/rio-data.js` | 1 | 179.8 KB | 849.3 KB | 2.0% |
| `app/` 其它 | 22 | 159.1 KB | 479.3 KB | 1.7% |
| `tools/` | 19 | 135.9 KB | 400.5 KB | 1.5% |
| `app/talent-tree.js` | 1 | 125.1 KB | 433.5 KB | 1.4% |
| `app/talent-data.js` | 1 | 92.4 KB | 520.1 KB | 1.0% |
| 根目录 | 7 | 67.4 KB | 153.2 KB | 0.7% |
| `app/bis-data.js` | 1 | 47.0 KB | 196.3 KB | 0.5% |
| `app/item-icons.js` | 1 | 6.6 KB | 33.5 KB | 0.1% |

**两批图标合计 8355.6 KB，占 91.1%。** 测试专用 / 缓存条目 0 个（打包一致性守卫
盯的就是这个数）。

涨的 2799 KB 里：装备图标 +1052 张 ≈ +2600 KB，`rio-data.js` +179.8 KB，其余是
`bis.js` / `run-tests.js` 变大的零头。**注意 `rio-data.js` 解压 849.3 KB 但压缩后只
179.8 KB（压缩率 21%）—— JSON 里大量重复的中文物品名和字段名压得很好。**
jpg 压不动（1515 张只省了 191 KB，5.3%），所以体积就是图标数量本身。

**仪器失败第十次**：第一版探针用 `ls *.zip` 的第一个结果，字典序撞上了目录里
留着的 `WowAltBoard-v1.13.3 …zip`，于是量出「30 个条目 / 201.2 KB / 图标占 0%」。
一份 30 个条目的包和一份 3662 个条目的包，数字差 100 倍还能被我念出来，
是因为我没让探针自证「量的是哪个文件」。修法：按 mtime 取最新，**并把选中的
文件名和所有候选都打出来**。教训还是那句 —— 先验仪器，再信读数；
而「让仪器自己报出它量的是什么」是最便宜的自证方式。

---

## 第 14 轮：天赋导入串实测（app/rio-data.js 里的 3960 条）

`app/bis.js` 开头那条注释写着「天赋导出串**做不到**：它要 treeHash 和
serialVersion，两者都只有游戏运行时才有」。这句话现在是**错的**，而且早就错了 ——
G3 抓下来的产物里每个专精都躺着近百条**官方导入串本身**。拿 `tools/decode-talent-string.js`
把它们全解一遍（`loadOrder()` 取节点顺序，离线，只读入库的 app/talent-tree.js）：

| 量 | 结果 |
| --- | --- |
| 串总数 / 去重 | 3960 / **3567** |
| 每专精串数 | 94~100（40 个专精全有） |
| 能解 | **3722**，解不了 238（全是「位读完了但节点还剩 N 个没走到」= 版本漂移，和 E5 同一个成因） |
| 头里 specID 与所在专精一致 | **3722 / 3722，不一致 0** |
| `serialVersion` | 全是 **2**（分布 `{"2":3722}`） |
| `treeHash` 全 0 | **3722 / 3722** |
| 已点节点数 | 17~78，中位数 73 |
| 串长 | 70~120，中位数 102 |

**结论：导出串不需要「做到」，它是现成的。** 三件事被这批数据同时钉死：
1. `serialVersion` 就是常量 2，不是运行时秘密；
2. `treeHash` 全 0 —— 一个发给几百万人复制粘贴的站点全用零 hash，说明**游戏不校验它**
   （这正是第 12 轮「待用户决定」第 7 条想验的事，被数据自己答了）；
3. 串头里的 16 位 specID 和它所在的排行榜专精 3722/3722 对得上，所以这些串**没串台**。

所以 UI 该做的不是「编码」，是「把现成的串挑出来、显示、让用户复制」。
以 Mage/Arcane（specId 62）为例：98 条里 73 种不同，最热那种出现 **15 次（15.3%）**，
解出 72 个已点节点，前几个天赋是法术吸取 / 深寒凝冰 / 永冻缀饰 / 屏障漫射。

原样取出的那一条（**未改一字**，探针直接从产物里 dump）：

```
C4DAAAAAAAAAAAAAAAAAAAAAAMzwYZmZmFMzQzMGAAAGAwMz0sssMDAEbAAsBzMDbWmxMLzYMzMzMswMzMzMAADAAwAMzAMAYYmZA
```

**教训（第三次同一个形状）：动手造之前先找它是不是已经存在。** 我为「生成导入串」
啃过两轮位流，结论写成了「做不到」并留在注释里；而答案在自己抓下来的产物里躺了一整轮。

---

## G5：天赋导入串的界面（挑 / 显示 / 复制）— 已完成

上一轮实测确认「导入串是现成的」之后，这一轮把它接进面板。**面板不编码、不改字符**，
所以断言能用最硬的判据：逐字节相等。

### 做了什么

`app/bis.js`：
- 新增 `rioLoadouts(specId)` —— 把 `AE_RIO.specs[id].loadouts` 按「同一串多少人用」
  聚合后降序，返回 `{list, count, total, uniq}`。同人数时按串本身排序：不这样的话
  `Object.keys` 顺序一变，界面上「#1 热门」指的就是另一串了，而这种不稳定在测试里
  表现为偶发失败。
- 新增 `renderLoadouts(s)` —— 画在天赋页 TCAT 条下面：标题带「N 名玩家共 M 种」、
  一排最多 6 个选串按钮（`#1·15人`，带占比 tooltip）、一个只读 `textarea` 放串本身、
  一个「复制」按钮走既有的 `AE.copyWithToast`（`app/toast.js`，`file://` 下会退到
  `execCommand`），以及一段说明「原样取自 raider.io 排行榜玩家，面板没改一个字符」
  和导入位置（游戏里 N → 右下角「导入/导出」）。
- `state.loadout` 记「显示第几条」。
- 只列前 6 种：再往后都是 1 人用的，列出来只是噪音。

`app/style.css`：`.bis-loadout` / `.lo-head` / `.lo-pick` / `.lo-text` / `.lo-act`，
串框照 `.vault-text` 的既有写法用等宽字体（长 base64 用比例字体没法扫读）。

顺手改掉两处**过时的谎话**：
- 头部注释「天赋导出串做不到：它要 treeHash 和 serialVersion」→ 改成「不自己编，
  直接用现成的」，并说清为什么原来那句话问错了问题。
- 天赋树底下「节点上没有图标是故意的」→ G4 已经把 2094 张图标画上了，
  测试里断言 4304 个节点全有图。

### 断言（`tools/run-tests.js` 新增一整行「天赋导入串」）

| 断言 | 实测 |
| --- | --- |
| 去重后专精数 == 40 | 40 |
| 串框数 == 渲染次数 | 43 / 43 |
| 显示的串与数据里第一条**逐字节相等** | 43 |
| 串头 16 位 specID == 所属专精 | 43 |
| `textarea` 只读 | 全部 |
| 选串按钮数 == min(6, 种类数) | 258 |
| **真点一次「复制」**，交出的串与显示的逐字节相等 | 43 |

真值 `rioLoadoutTruth()` 是**独立算的**，不调 `app/bis.js` 的 `rioLoadouts()` ——
拿被测代码算真值再去验被测代码是个恒等式，永远通过。

### 三次假绿，全是同一个形状

1. **复制桩写了两遍。** 第二个只存字符串、覆盖了存对象的那个，`copied[0].text`
   恒为 `undefined`，43 个专精全报「复制出去的串和显示的不是同一串」。
   那不是面板的错，是**仪器**的错（仪器失败第 12 次）。
2. **计数器加了，但既不打印也没有下界。** 修好桩之后套件全绿 —— 而 truth 取不到时
   那 43 条断言会全部提前 `return`，「一个专精都没画出串框」会安静地全绿通过。
3. **`loSpecs` 数的是渲染次数，不是专精数。** 断言 `=== 40` 恒报 43，因为天赋树
   要额外画 3 次「切类别」。数渲染次数和数专精是两件事。

### 变异测试 `tools/mutate-loadout.js` —— 9/9

跟 `mutate-icons.js` 一档严：每个变异体**必须让指定那句话出现在输出里**，
光「退出码非 0」不算抓到；锚点失效算失败。这一版还加了一条：
**锚点在文件里出现次数必须正好 1 次** —— 第 9 个变异体的锚点 `box.appendChild(bar);`
在 `bis.js` 里有两处（导入串那处和天赋树套路条那处），`replace` 只换第一处，
恰好命中想改的地方是**侥幸**，不是证明。

第一轮跑出 **6 抓到 / 3 串了**，三处「串了」的诊断各不相同：
- 「整块不画」→ 实际触发的是「各应正好 1 个」，我给的 `want` 写错了（改 `want`）；
- 「拿另一个专精的串来显示」→ 被「字节不等」那条先抓走并 `return`，specID 那条
  **一次都没执行过**。修法是把 specID 检查**移到字节比对之前，且不提前 return**。
  被更强的断言遮住的断言，等于没有被证明。
- 「真值恒为空」→ 断言确实触发了，但它排在第 44 条，而打印只印前 20 条。修法是
  新增 `loNote(kind, msg)` **按问题种类限流**（每类最多 2 条），保证每一类都进得了打印。

改完 **9/9 抓到，漏 0，串 0，锚点失效 0，还原后套件仍然通过**。

### 新教训

- **仪器写两遍比写错一遍更隐蔽。** 同名 `var` 重复声明在 ES5 里不报错，后者静默胜出。
- **「串了」和「漏了」要分开看。** 漏了 = 断言太松；串了 = 断言可能是摆设，
  因为抓住它的是别人。后者更值得追。
- **断言的顺序会决定它有没有被执行。** 强断言 + 提前 return 会让后面的断言永远死。
- **限流要按种类，不能按总数。** 按总数限流时，一类问题刷满 20 条，其余类别全部看不见。
- **变异锚点必须唯一，而且要让工具自己检查唯一性。** 靠肉眼保证唯一性，下次还会漏。

---

## 第 13 轮用户指示：README 收手（**硬约束，优先级高于我自己的计划**）

用户原话：「不要给Readme写那么多东西，有毛病吗写那么多，不要写Readme，也不要把来源插件数据写进去」

拆成三条，往后每轮都按这个来：

1. **不再扩写 README。** 做完一个功能不要顺手去 README 加一节。
   前几轮我把它堆到 **1416 行**，其中绝大部分是我自己的实测过程和取舍论证 ——
   那些东西的位置在这个文件（计划.md）和代码注释里，不在给用户看的说明书里。
2. **默认不动 README。** 除非用户点名要改，或者 README 里的话已经**错了**
   （比如写着「做不到」而其实做到了）。改也只改那一句，不新开章节。
3. **README 里不写来源插件的数据。** 插件的版本号、审计出来的行数 / 覆盖率 /
   百分位、字段缺失统计、和它的对比表格 —— 一律不进 README。

这一轮的动作：把 README 从 1416 行砍回去，删掉的是我加的实测长文和插件数据，
保留安装 / 用法 / 目录结构 / 已知限制这些用户真的要看的部分。

**教训：文档的读者不是我自己。** 我把「怎么验证的、为什么这么选」当成 README 的内容，
是把工作日志错放进了说明书。这类内容有它自己的家 —— 计划.md 存过程，注释存原因，
README 只回答「这东西怎么用」。

---

## 第 13 轮：团本列显示过期锁定（用户报的 bug）

**用户问**：主面板「团队副本」里的 8/8 是什么意思？有角色本周没打也显示，
不应该当周打了才显示吗？

**8/8 的含义**：该难度下「已击杀首领数 / 首领总数」。表头悬停给「共 N 个首领」，
格子悬停逐个列出 ✔ / ·。

**bug 确认（实测本机 16 个角色）**：插件存的是 `GetSavedInstanceInfo` 的整份快照，
角色**不上线就没人更新它**，上个周期的记录一直躺在存档里。
而 `app/columns.js` 的 raid 列只读 `progress`/`total`，**从来没看 `locked`**，
于是过期残留照样画成 8/8 还染成绿色。

| 量 | 结果 |
| --- | --- |
| 锁定记录总数（团本 21 + 地下城 23） | 44 |
| `locked=true` 且 `expires>scannedAt` 且 `reset>0` | **11**（本周真在锁） |
| `locked=false` 且 `expires=0` 且 `reset=0` | **33**（过期残留） |
| 中间态（两个判据不一致） | **0 / 44** |
| 会被画成满进度的格子 | 12 行，其中 **7 行是过期残留** |
| 过滤后 raid 列 | 10 → **5** |

**关键测量（决定了改法）**：副本 / 团本中文名正是从锁定记录里学的
（`harvestRaidNames` / `harvestDungeonNames`）。**如果在 `mapRaids` 里就丢掉过期记录，
能学到中文名的副本从 17 个掉到 3 个**，14 个名字（黑暗神殿、孢陨幽境、虚影尖塔……）
会退回英文。所以：

**改法**：记录全留，加 `active`（= `si.locked === true`），由消费端各自过滤。
- `app/model.js` `mapRaids`：加 `active` 字段 + 长注释记下上面的测量。
- `app/model.js` `deriveColumns`：`if (r && r.active)` —— 列本身只从本周锁定派生。
- `app/columns.js` raid 列：`render` 里 `!r.active` 走 `dash()`；`sort` 同样降为 -1。
- `app/panel.js`：明细面板的「团队副本进度」和「地下城锁定」都按 `active` 过滤。

**三条新断言**（`app/model-tests.js`，数据模型 66 → **69**）：
1. 团本列只从本周还锁着的记录派生（每个列必须有角色 `active`）。
2. `active` 与 `expires > scannedAt` 两个独立判据 44/44 一致（带空转守卫）。
3. 过期记录仍然留在模型里 —— 中文名要靠它（**带空转守卫**：本机没有过期记录时报错）。

**手工变异 3/3，各被自己那条抓到**：
- 列的 `active` 过滤去掉 → 「这些列没有任何角色本周还锁着: 虚影尖塔/普通…」
- `active: true` 恒真 → 「44 条里有 33 条两个判据不一致」
- 过滤点提前到学名之前 → 「过期的锁定记录仍然留在模型里…这条断言等于没跑」

**教训**：这个 bug 的形状和这个项目反复踩的那个一样 ——
**渲染层只读了它想读的字段，没读那个决定字段有没有效的字段。**
`progress` 一直是对的，错的是「拿它当本周进度」。
另外，第三条断言证明了一件事：**修 bug 的正确落点不一定在发现问题的那一层** ——
过滤放在数据入口最省事，但会砸掉另一个功能，测量先告诉了我这件事。

---

## 第 13 轮：团本列只显示本周（用户报的 bug）

### 用户的问题
「主面板里团队副本的 8/8 是什么意思？有个角色本周没打，都有显示呢，
不应该当周打了才显示吗」——两问，第二问是真 bug。

### 8/8 的含义
团本锁定里「已击杀首领数 / 该难度首领总数」。表头悬停写「共 N 个首领」，
格子悬停逐个列出 ✔ / ·。数据来自插件存的 `GetSavedInstanceInfo` 快照
（`raids.savedInstances[].encounterProgress / numEncounters`）。

### bug 的成因
那个数字不是「本周打了」，而是「存档里还留着的锁定记录」。角色**不上线**，
插件就没机会更新这份快照，上个周期的记录一直躺在存档里。而 `app/columns.js`
的 raid 列只读 `progress`/`total`，**完全没看 `locked` / `expires`**，
于是过期残留照样画成 8/8 并染成绿色（`.full`）。

### 实测（本机 16 个角色，44 条锁定记录）
只有两种形态，**没有中间态**：

| 形态 | 条数 | 含义 |
| --- | --- | --- |
| `locked=true`、`expires` 在未来、`reset>0` | **11** | 本周真在锁 |
| `locked=false`、`expires=0`、`reset=0` | **33** | 过期残留 |

- `locked=true` 且 `expires=0` 的：**0**；`locked=false` 但 `expires` 未到的：**0**。
  所以 `locked` 与 `expires>scannedAt` 两个独立判据 **44/44 一致**。
- 原先会被画成满进度的 12 个格子里，**7 个是 `locked=false` 的残留**。
- 同一副本同难度能同时出现两种状态（黑龙叔叔「烈毒之渊/随机 2/8」是残留，
  抽啊抽啊抽的同一格是 `locked=true`）——证明 `locked` 是真实状态，不是恒真字段。
- raid 列从 **10 个降到 5 个**；地下城锁定 23 条 → 本周 **0** 条。

### 关键陷阱：过滤点选错会砸掉中文名
副本/团本的中文名正是从锁定记录里学来的（`harvestRaidNames` /
`harvestDungeonNames` 读 `byKey` + `dungeonLockouts`）。实测**只留本周**的话，
能学到中文名的副本从 **17 个掉到 3 个**，14 个退回英文
（黑暗神殿、风暴烈酒酿造厂、孢陨幽境、诸王之眠、塞塔里斯神庙、红玉新生法池、
密谋小径、纳洛拉克的洞穴、夺目谷、虚影尖塔、进军奎尔丹纳斯、虚空之痕竞技场、
梦境裂隙、毒牙祭坛）。

所以做法是：**记录全留，只打标记**。`mapRaids` 加 `active: si.locked === true`，
由三个消费端各自过滤：
- `app/model.js` `deriveColumns`：`if (r && r.active)` 才建列（列本来就是从锁定派生的）；
- `app/columns.js` raid 列：`!r.active` 走 `dash(td)`，`sort` 返回 -1；
- `app/panel.js`：团本进度块和地下城锁定表都按 `active` 过滤。

### 三条断言（`app/model-tests.js`，数据模型 66 → 69）
1. 团本列只从本周还锁着的记录派生；
2. `active` 与 `expires>scannedAt` 两个独立判据完全一致（44/44）；
3. **空转守卫**：过期记录必须仍留在模型里——否则第 1、2 条可能是「本机没有
   过期记录」造成的假绿，而且过滤点一旦提前到学名之前，中文名就会退回英文。

手工变异 3 次，各被**自己那条**断言抓到：
- 去掉列的 `active` 过滤 → 「这些列没有任何角色本周还锁着: 虚影尖塔/普通…」
- 把 `active` 改成恒 true → 「44 条里有 33 条两个判据不一致」
- 把过滤点提前到学名之前 → 被空转守卫抓住（「本机数据里没有过期记录，这条断言等于没跑」）

### 教训
**「数据里有这个数」和「这个数说的是现在」是两件事。** 存档是快照，
快照的新鲜度由「角色上不上线」决定，而不是由扫描时间决定。
凡是从存档里读出来的进度类字段，都要先问一句「这是哪个周期的」。

**过滤要选对层。** 同一份数据既喂显示又喂名字学习，在最上游过滤会连带砸掉
下游；正确做法是打标记，让每个消费端按自己的语义过滤。
