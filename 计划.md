# 毕业装备面板 —— 开发计划与进度

> 每轮开始：读这里 → 找第一个未完成的任务 → 做 → 回来更新。
> **历史记录不写这里，写 git log。** 这个文件只留「约束 + 现在卡在哪 + 待你决定」。
> （第 19 轮从 3209 行压到这个长度：原来那些「已查明的事实」和逐轮日志已经
> 全部落进代码注释和提交信息里了，在这儿留第二份只会让人读岔。旧版见
> git 之外的备份或直接看提交历史。）

---

## 硬约束（每轮都要遵守，不许自己放宽）

1. **不 push、不打 tag、不发 Release。** 发布等用户通知。
   版本号 **1.15.0**，在 `tools/scan.ps1` 的 `$TOOL_VERSION` 一处，
   `build-release.ps1` 从那里读。`data/data.js` 里那三个是扫描产物，不入库。
2. **只在分支 `feat/bis-panel` 上提交。**
3. **不凭记忆手打中文游戏名词。** 只用从本机 SavedVariables / 插件 locale /
   暴雪 DB2 / 按 ID 查出来的官方名。拿不到就**留英文**并记进「待用户提供」。
   猜一个译名比留英文糟得多 —— 用户会照着游戏里不存在的名字去搜。
4. **不联网传任何本机数据。** 只读取公开信息，且必须走代理 `http://127.0.0.1:7897`。
   新增一个上游依赖（新域名、新接口）**要先问**，不自己拍。
5. **临时脚本跑完即删**（`_*.js` / `_*.py`）。留下来的必须是 `tools/` 或 `app/` 里的正式文件。
   不许 `npm install` —— 这个项目**零依赖、无构建步骤**，`file://` 直开。
6. **每次改完代码跑 `node tools\run-tests.js`，全绿才算完成一步。**
7. 不确定要走哪条路的决策，**不自己拍**，写进「待用户决定」。
8. **不要再往 README 里加东西**，也不要把来源插件的数据 / 名字写进 README。
   代码注释里说明来源没问题（那是给维护者的）。

### 测试纪律（这个仓库反复踩过的坑，写死在这里）

- **「跳过」不许报成「通过」。** 取不到真值就提前 return 的断言等于没有断言。
  每个计数器都要有**下界**，而且**必须打印出来** —— 数字没动就是没跑。
- **拿被测代码算真值 = 恒等式**，永远通过。真值要独立算一遍。
- **变异测试的判据是「指定那句话出现在输出里」**，光退出码非 0 不算抓到；
  锚点在文件里出现次数不是 1，直接算失败（`tools/mutate-*.js`）。
- 锚点会烂。第 19 轮发现「方案名」那条的锚点匹配 0 次已经一整轮 ——
  那条断言从来没被验过。跑变异测试时留意「锚点失效」那一行。

---

## 现在的状态

装备页两个视角：**最佳推荐**（maxroll，默认）+ **实战分布**（raider.io）。
天赋页顺序（第 20 轮按易用性调过）：

```
榜上热门天赋串（团本 / 大秘境两类，可复制）
团本 / 大秘境（maxroll 指南的类型）
maxroll 方案列表
maxroll 这一套的导入串（可复制）
出手顺序（折叠，中文）
三棵树（英雄 → 职业 → 专精，图标悬停给天赋原本说明）   ← 1731 px，放最后
```

「先给能用的，再给能看的」：串和出手顺序是拿来用的，树是拿来看的。

数据文件（都是入库的产物，面板运行时**不联网**）：

| 文件 | 大小 | 生成器 | 校验器 |
| --- | --- | --- | --- |
| `app/bis-data.js` | 196 KB | 插件参照表 | `verify-bis-data.js` |
| `app/rio-data.js` | 359 KB | `fetch-rio.js` | `verify-rio-data.js` |
| `app/wcl-data.js` | 122 KB | `fetch-wcl.js` | `verify-wcl-data.js` |
| `app/maxroll-data.js` | 234 KB | `fetch-maxroll.js` | `verify-maxroll-data.js` |
| `app/talent-tree.js` | 434 KB | `fetch-talent-tree.js` | `verify-talent-tree.js` |
| `app/talent-desc.js` | 506 KB | `fetch-talent-desc.js` | （run-tests 逐条对提示） |
| `tools/spell-names-zh.json` | 16 KB | `fetch-spell-names.js` | （maxroll 校验器的 checkZhNames） |

**赛季 / 团本跟着上游走，都不写死：**
- `fetch-rio.js` 开跑前跟 raider.io 核对当前赛季（`season-mn-2`），对不上就停。
  榜页缓存的文件名带赛季 —— 不带的话换赛季会静默命中旧缓存。
- `fetch-wcl.js` 自己筛「在用 + 有史诗难度 + 名字不带 PTR/Complete」的团本 zone。

**天赋串两家，形状统一**（`loadouts: [[串, 人数]…]`，人数降序，各专精最多 30 套；
「一套」按**解出来的天赋**算，不按字节 —— `tools/group-loadouts.js`，两家共用）：
- 大秘境 = raider.io 每专精榜，20000 个角色 / 12755 套；
- 团本 = Warcraft Logs，串在 `ReportFight.talentImportCode(actorID:)` 上，
  10119 条 100% 解得开 / 5292 套 / 40 个专精。**凭证在 `tools/.wcl-auth.json`（gitignore）。**

**两遍跑的依赖**（第 19 轮引入，别以为是 bug）：
`fetch-maxroll.js` 写出 `tools/.maxroll-spell-ids.json` →
`fetch-spell-names.js` 读它补名字 → 再跑一次 `fetch-maxroll.js` 才用上新名字。

---
## 已定的决策（不要再问，也不要自己改回去）

- **maxroll 的属性优先级：放弃抓取。**（第 19 轮）页面里只有编码串，后端接口
  要 Discord 登录且用户实测 403。解码能自圆其说但拿页面散文复核时两篇都对不上，
  没有真值就不写进产物。面板里的「属性目标」用的是本机两份实测数据。
- **tooltip 一律「要么英文，要么人工直译」，不许 AI 润色。**（第 19 轮）
  这条同时管住了 `tools/translate-prio.js`：那张表是人手写的整句模板，
  没命中的整句留英文，**不做半句翻译**。
- **团本天赋走 Warcraft Logs，不走 raider.io。**（第 20 轮）raider.io 的团本榜
  只有公会没有角色；它确实有 `/api/spec-loadout/talents/raid-encounters`，但那是
  未发布的 beta（UI 挂在 `TALENT_BUILDS_NAV` flag 后面，487 个 chunk 没有组件调它，
  参数契约试了 19 种全 400）。别再去试了。
- **maxroll 的串不用自己编码。** WCL 给的是现成的官方串（版本 2、hash 全 0）。
  解码器有一段没建模的位（3722 条里 84 条节点流之后还有非零位），重编会静默丢掉。
- **「按天赋归并」不用再试了：实测几乎不生效。**（第 20 轮）
  「一套天赋」的定义已经从字节改成**解出来的内容**（`tools/group-loadouts.js`，
  两个抓取器共用），但那**不解决**用户报的「标题写 500 名玩家而 #1~#6 只有 47 人」：
  大秘境 21181 条不同串 → 21173 套（1.00 种写法/套，只有 8 套有第二种写法），
  团本 7920 条 → 7920 套。**天赋本来就人人不同**：一个专精 1376 人里 649 套，
  最热门那一套 187 人；另一个 673 人 535 套，最热门 31 人。加起来对不上是真实情况。
  一开始以为是「同一套被不同客户端导出成几十种串」，那个判断来自一个**退化的指纹**
  （字段名写成 `n.node`，解码器给的是 `n.id`），它把完全不同的天赋并成一套
  （7920 → 558，最大一组 250 套），而产物、两个校验器、137 项测试当时全绿。
  守卫现在有三层：`group-loadouts.js` 里节点缺 id 直接抛异常；两个校验器**各自
  独立算指纹**判「两行不许是同一套天赋」；`run-tests.js` 的「天赋归并」一组用合成
  真值盯「点数分布相同而节点不同的两套必须算成两套」（变异测试验过会红）。
- **天赋那一块的说法不改。**（第 20 轮，用户：不用改）标题现在写
  「N 名玩家共 M 套，下面是最热门的几种」—— 上面那条查清了「#1 往往只占几个
  百分点」，问过要不要加分量提示，用户说不用。**别再自己加**。
- **「各首领 / 副本说明」撤掉了。**（第 19 轮，用户：这个数据没用，没人看）
  生成器不再产出 `boss` 字段。反向守卫在 `run-tests.js` 的 checkMrTalents
  和 `verify-maxroll-data.js` 里 —— 再冒出来就报错。

---

## 待用户决定（不要自己拍）

1. **`Sporefused` 的中文名**（73 行装备因此没有轨道徽章）。
   AlterEgo 和 raidbots 都只给英文 `Sporefused: Myth`，全盘 7900 个文件里没有 zhCN。
2. **要不要显示「插槽」标记。** `app/rio-data.js` 里已经有 `sock` 字段（494/2432 件），
   不用多联网就能显示。要不要显示、显示成什么样，等你说。
3. **发布包体积。** 现在带 462 个装备图标（约 1 MB）+ 2094 张天赋图标（5.14 MB）
   + `app/talent-desc.js`（506 KB）。不带图标的话包 ~510 KB，但运行时拉图在国内
   靠不住（zamimg 直连 403）。接受不接受这个体积？

## 待用户提供（本机拿不到的字符串）

- `Sporefused` 的中文名（同上）。
- 两个技能没有官方中文名，正文里留英文：`Strength of the Black Ox`（125060）、
  `Unstable Affliction`（316099）。Wowhead 报 Entity not found，
  `app/talent-tree.js` 里也没有。**留英文是正确行为，不要猜。**
- 出手顺序里约三成句子没命中 `tools/translate-prio.js` 的模板，**整句留英文**。
  那是长尾散句（一句一个写法），要再提高覆盖率只能一条条手写译法。
  想让哪几条变中文，指出来就行。

---

## 常开的收尾项（不算任务，每轮过一遍）

- 全部测试绿（`node tools\run-tests.js`），工作区干净，提交到 `feat/bis-panel`。
- 临时脚本删掉，`git status` 不该有 `_*` 文件。
- 变异测试（`tools/mutate-*.js`）偶尔跑一次，尤其是改了断言之后 ——
  它会报「锚点失效」，那是唯一能发现「断言早就没在验了」的办法。
