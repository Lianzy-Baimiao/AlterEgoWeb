# 毕业装备面板 —— 开发计划与进度

> **这个文件是我（助手）每轮自动继续时的第一读物。**
> 每轮开始：读这里 → 找第一个未完成的任务 → 做 → 回来更新状态和日志。
> 每轮结束：进度必须写回这个文件，否则下一轮等于失忆。

---

## 硬约束（每轮都要遵守，不许自己放宽）

1. **不 push、不打 tag、不发 Release。** 版本号锁在 `1.14.0`。发布等用户通知。
2. **只在分支 `feat/bis-panel` 上提交。** `main` 停在 `08a7e5d`，远端也是。
3. **不凭记忆手打中文游戏名词。** 只用从本机 SavedVariables 或插件 locale 文件里
   复制出来的字符串。拿不到就留英文并记在「待用户提供」里。
4. **不联网传任何本机数据。** 只读取公开信息，且必须走代理 `http://127.0.0.1:7897`。
5. **临时脚本跑完即删**（`_*.js` / `_*.py`）。留下来的必须是 `tools/` 或 `app/` 里的正式文件。
6. **每次改完代码跑 `node tools\run-tests.js`，全绿才算完成一步。**
   它跑三个测试套件 + 渲染检查，退出码非 0 就是有问题。
7. 不确定要走哪条路的决策，**不自己拍**，写进「待用户决定」。

---

## 已查明的事实（不要重复调查）

### 图标（**已解决**，见 A 组任务）
- 本机 `E:\World of Warcraft\_retail_\Interface\Icons\` 有 **22786 个散装 .blp**，共 185 MB。
  全部 6676 字节 = 64×64 BLP2 / DXT5 / 带 mipmap。文件时间 **2024-08-18**。
- 这是一份**旧的、不完整的**提取：`inv_10* / inv_11* / inv_12*` 的图标**一个都没有**（各 0 个），
  是 BFA 前后的旧图标包。**当前赛季装备的图标不可能从这里拿到**，这条路对装备是死的。
- `tools/blp.js` 已写好并验证：BLP2 → PNG，零依赖，出图正确（已肉眼确认宝石和食物两张）。
  现在没有用到它 —— 留着，因为它是唯一能读本机客户端图标的办法，将来若要走 CASC 提取会用上。
- **网络可达性实测（这台机器，中国大陆）**：
  - `render.worldofwarcraft.com/us/icons/56/<name>.jpg` —— **直连 200，不用代理**。只开放 18 / 56 两种尺寸，64 是 403。
  - `wow.zamimg.com/images/wow/icons/large/<name>.jpg` —— 直连 403，**走代理 200**。
  - `nether.wowhead.com/tooltip/item/<id>` —— 走代理 200，返回 JSON，带 **`icon` + `quality` + `ilvl`**。
  - 暴雪 CDN **缺当前赛季新图标**（烈毒之渊那批 `*_ulatek_d_01` 在 us/eu/tw/cn 四个区全 403），
    这 158 个只有 zamimg 有。所以下载必须两个源按顺序试。
  - zamimg 对不存在的名字正确返回 404（试过乱名），所以「下到了」就是真图，不是占位。
- **itemId → 图标名 + 品质已经解决**：`tools/fetch-icons.js` 按 itemId 查 wowhead tooltip，
  产物 `app/item-icons.js`（`AE_ITEM_ICONS` / `AE_ITEM_QUALITY`）+ `app/icons/` 462 个 jpg（约 1 MB）。
  568 个 itemId **100% 覆盖**，0 断链，0 孤儿。
- **品质分布 `{1:14, 3:54, 4:500}`** —— 「BiS 一定是紫装」是错的：附魔卷轴是蓝的、合剂是白的。
  原来的代码硬写 `qualityColors[4]`，把这 68 件都染错了。
- 消耗品用 **BisData 自带的图标名**，不用 wowhead 的：wowhead 对同一件东西可能给出后缀带 `-`
  的变体名（`241324` → `..._white-`，`241326` → `..._red--`，两张图 zamimg 上都真实存在、md5 不同、
  都是合法合剂图标）。客户端那份更权威。

### 数据质量审计（BisData 0.5.78-live / updatedAt 2026-08-29 / addonVersion 0.64.1）
对得上的：
- 8 个大秘境副本和本机赛季 18 完全一致（BisData 用 journalInstanceID，模型用 challengeModeID，两套编号同 8 个副本）。
- 团本是当前的烈毒之渊（instanceId 1320）。
- 66 件低 ID 装备（158366~159255）来自塞塔里斯神庙/诸王之眠/红玉新生法池 —— 旧副本进 M+ 池
  仍掉原物品，**不是脏数据**。
- 每部位 `usagePct` 之和从不超过 100%（最大 100.1），同组内无重复 itemId，戒指 11/12 只有 0/80 组重复。

问题（这些是「数据可能很差」的具体证据）：
- **没有任何样本量字段。** 72% 背后是 5 个人还是 500 个人，数据里查不到。
  （已确认原始 Lua 就没有，不是我的生成器丢的。`count` 的 812 次命中全是 `encounterId` 被 grep 截断。）
- **206 / 1264 个部位组的使用率之和不到 50%**，中位数只有 72.9% —— 尾部被截断了，
  剩下那几十个百分点穿的是什么，数据里没有。
- **`weights`（属性权重）是硬编码的**，40 个专精只有 38 种组合，取值全部量化到 0.1
  （0.3~1.0），不是模拟出来的。
- **`specRawToCN` 只按专精英文名建键**：`DEATHKNIGHT/FROST` → 「冰法」（法师的名字），
  `PRESERVATION` / `DISCIPLINE` 根本没有行。已在 `labels.js` 用拒绝名单挡住。
- **饰品 13/14 有 1/80 组完全相同**（可疑但不严重）。
- **插件的 `sourceCategories` 只有 5 个键**（raid / mplus / crafted / world / other），
  但数据里实际用到 7 个 —— `tier`（292 行）和 `quest`（5 行）没有中文名。
  已在生成器里从数据自己的 `source` 文本补上（tier→「套装转换」、quest→「奇点声望任务」），
  并加了测试盯着。**不手写**：这两个词是从数据里抄的，不是我编的。
  （曾想用 shipped locale 里的短词「套装」，但只找到一处来源，而 `EllesmereUILocales`
  里 `L["Set"] = "设置"` —— 同一个英文词在另一个语境是完全不同的意思，
  说明按英文单词查 locale 不安全。）
- ~~我的 `gen-bis.js` 丢掉了 `bonusIDs`~~ **已处理，见下面「升级轨道」** —— 结论是不存原始
  `bonusIDs`，改存从它解出来的轨道。

### 升级轨道（B3 的产物，**已解决**）
- BisData 的 4066 行装备里 3963 行带 `bonusIDs`（89 种不同取值、591 种不同组合）。
- `bonusIDs` **不是按 itemId 静态的**：498 件里 **386 件有多套**（`158366` 一件就有 12 套），
  所以只能按行存。原样存 19.0 KB（直接）/ 24.6 KB（内化池+下标）。
- 插件里 `bonusIDs` **只有一个用途**：拼游戏内 `|Hitem:...` 链接，让游戏自己的 tooltip 显示
  正确装等（`GearInsight.lua` 里 8 处、`ui/DungeonGuide.lua` 1 处，全是这个）。网页没有这个
  机制，装等本来也已经在行里。**所以原样存是白花钱。**
- 有价值的是解开它。`AlterEgo/Data/UpgradeTracks.lua`（作者注明抄自 raidbots `bonuses.json`）
  给出赛季 17/18 的 10 条轨道 × 6 级 = 60 个 bonusID，数组下标就是升级等级。
  实测 **3601/3963 行（90.9%）能解出轨道，且没有一行同时命中两个轨道**。
- **轨道推不出来，只能存**：装等有 3 个值对应两种轨道（308 / 318 / 321），
  连 `(装等, 来源分类)` 都还有 **5/24 组歧义**（`321/raid` = 英雄 6/6 ×528 vs 神话 2/6 ×10）。
- 解不出的 362 行：`12214`/`12497`/`13751`/`8960`/`13836` 这类非轨道 bonusID（37 种），
  多是套装坯子和旧赛季物品。这些行不显示轨道徽章，不猜。
- 轨道中文名有**两处互相独立的 shipped zhCN locale**，完全一致：
  `EllesmereUILocales/zhCN.lua` 和 `ItemInfoOverlay/Locales/zhCN.lua` 都写「英雄」「神话」。
  生成器两边都读，不一致就 `exit(1)`。
  （`ItemInfoOverlay/Utils.lua` 的**代码注释**里把 Myth 写成「史诗」，那不是 locale，按 locale。）
- 体积：`app/bis-data.js` 186931 → 201010 字节（+13.7 KB，+7.5%）。

### 代码
- `app/bis.js` 的图标层已改完：`iconUrl()` 默认用包内 `app/icons/`，`iconBaseUrl` 变成
  纯覆盖项（留空 = 用包里的）；`itemQuality()` 出真品质；`iconImg()` 统一出 `<img>`，
  带 `onerror` 自摘，缺一张图不会留碎图标记。
- **测试 harness 现在是正式文件**：`tools/dom-stub.js`（DOM 桩）+ `tools/run-tests.js`（跑全部）。
  之前每轮都要重写一个临时 harness，那是纯浪费。
- 渲染冒烟测试已经是 `tools/run-tests.js` 的一部分（不再是临时脚本）：40 专精 × 2 视角 +
  天赋 3 类别共 83 次渲染，真实角色对照能命中 10/16（术士 vs DEMONOLOGY）。
- **行的格式变了**（B3）：`[itemId, ilvl, 使用率, 来源下标, 可升级上限, 轨道码]`。
  后两位可省，但**不许跳着省** —— 有轨道码就必须占住 `mx` 的位置（没有写 0），
  否则 `r[4]` 是 mx 还是轨道码得看长度猜。`mx` 从 `undefined` 变成了 `0`。
- **两个生成器报的 KB 数曾经是错的**：用 `str.length`（字符数）当字节数，中文一个字符 3 字节，
  `bis-data.js` 少报 15 KB、源文件少报 160 KB。已改成 `Buffer.byteLength`。

### 替代数据源调研（B4，**只调研，换不换是用户的决定**）

可达性（本机，中国大陆，代理 `http://127.0.0.1:7897`）：

| 站点 | 直连 | 走代理 | 结论 |
| --- | --- | --- | --- |
| archon.gg | 403 | 403 | **Cloudflare 挡住，两边都进不去** |
| warcraftlogs.com | 403 | 403 | **两边都进不去**（公开页也不行） |
| u.gg | 403 | 403 | 两边都进不去 |
| bloodmallet.com | 200 | 200 | 直连可用，有 JSON 接口 |
| raidbots.com/static/data/live/bonuses.json | 200 | 200 | 直连可用，1.76 MB JSON |
| wowhead.com | 403 | 200 | 只能走代理（已在用它查图标名） |
| murlok.io | 200 | 200 | 直连可用，但**只有 PvP** |
| wowanalyzer.com | 200 | 200 | 是「打完复盘」工具，不出 BiS 表 |

各家能给什么：

- **archon.gg / warcraftlogs**：本机根本访问不到，无法作为数据源。这一条把「换成
  更权威的使用率统计」这个方向基本堵死了 —— GearInsight 的数据本身就是从 WCL 抓的，
  而我在这台机器上抓不了。
- **murlok.io**：`/<职业>/<专精>/3v3` 全是竞技场。试了 `mythic-plus` / `mythic` /
  `raid` / `pve` 四个路径全 404。**PvP 站，替代不了。**
- **bloodmallet.com**：接口是 `/chart/get/<类型>/<战斗风格>/<职业>/<专精>`，返回 JSON。
  - **有样本量**：`simc_settings.iterations = 60000`、`target_error = 0.1`，
    还有 `metadata.SimulationCraft` / `bloodytools` 的 git hash 和时间戳。
    这正是 GearInsight 缺的东西。
  - **有 zhCN**：`translations[物品英文名].cn_CN`，62 个饰品**零缺失**，9 种语言。
  - **但性质不同**：它是 SimC 模拟出来的 DPS 数值（`data[名字][装等] = dps`，
    模拟了 15 个装等档位），**不是「多少人在用」**。
  - **覆盖面窄**：11 种数据类型里只有 5 种有数据（trinkets / secondary_distributions /
    races / potions / phials）；talents / tier_set / gems / enchantments / embellishments /
    food 全部返回 `{"status": "error", "message": "No standard chart with these values found."}`。
    战斗风格也只有 `castingpatchwerk` 有数据，另外 4 种全空。
  - **结论：能补饰品和副属性配比，替代不了全套部位。** 而且换过去等于把「实际在用」
    换成「理论最优」，是产品定位的改变，不是数据源的替换。
- **raidbots `bonuses.json`**：9742 条 bonusID 的权威解释，568 条带 `upgrade`。
  - **已用它独立验证我的轨道解码**：BisData 用到的 12 个轨道 bonusID，装等和 raidbots
    **12/12 完全一致**；赛季 18 那 30 个 ID 的 `upgrade.name` + `level` + `max` 和 AlterEgo 的表
    **30/30 一致**（`upgrade.fullName` 直接就是 `"Hero 6/6"`）。赛季 17 那 30 个在 raidbots 的
    **live** 数据里已经没有 `upgrade` 字段（只剩 `itemLevel`），所以「不一致 30」是
    live/历史的差别，不是错 —— 用装等验证仍然全对。
  - 也证明了解不出轨道的那 37 个 bonusID **确实不是轨道**：`6652`/`13667`/`13696` 是
    `serverside: true`，`8790`~`8793` 是 `craftedStats`，`13695` 是 `socket: 1`，
    `12384`/`13771` 是镶嵌效果，`8960` 是 `item_limit_category`。**不显示才是对的。**

顺带量出来的两件事（都还没做，见任务 B5 / B6）：

- **73 行带 `Sporefused: Myth`**（bonusID `13786`），AlterEgo 的 `upgradeBonusLabels` 里有这个
  英文名，但**全盘搜不到任何 zhCN 译名**。这 73 行现在完全没有轨道徽章。
- **1039 行 / 102 件装备带插槽 bonusID**。「插槽」的 zhCN 有来源（`Syndicator/Locales.lua`
  的 `L["KEYWORD_SOCKET"]`），但**本机没有插槽 bonusID 的清单** —— 那 116 个 ID 是从 raidbots
  的 JSON 里数出来的。要显示插槽就得让生成器多依赖一次 raidbots（打包时联网）。

（测量过程中的一个教训：第一版用 `/bonusIDs\s*=\s*\{[^}]*?\}/` 之类的正则扫行，
跨不过嵌套的 `stats = { … }`，只匹配到 480/3963 行，于是「0 行带 label」——
那是测量假象，不是结论。改成按行边界切分才得到真数字。）

### 天赋树的数据找到了（第 7 轮，**长期卡住的那件事解开了**）

之前的结论是「天赋树画不出来，缺的是树的结构，插件里根本没有」。**结构本身在外面有，
而且本机能访问。** 两个来源，都是直连 200、不需要代理：

| 来源 | 内容 | 大小 |
| --- | --- | --- |
| `raidbots.com/static/data/live/talents.json` | 40 个专精的完整树结构 | 3.2 MB |
| `wago.tools/db2/TraitDefinition/csv?locale=zhCN` | 天赋定义 → 名字 / SpellID | 830 KB |
| `wago.tools/db2/SpellName/csv?locale=zhCN` | SpellID → 中文法术名 | 10.0 MB |

**raidbots 的 `talents.json` 正是面板缺的东西。** 顶层是 40 个专精的数组，每个带
`traitTreeId / className / classId / specName / specId / classNodes / specNodes /
heroNodes / subTreeNodes / fullNodeOrder`。节点字段：
`id, name, type, posX, posY, maxRanks, entryNode, next, prev, entries[]`，
英雄节点多 `subTreeId / requiresNode / freeNode / freeLevel`。
`entries[]` 里有 `id, definitionId, maxRanks, type(active|passive), name, spellId, icon, index`。
**`posX/posY`、`next/prev`、`icon`、`maxRanks` 全都有** —— 就是 `TREE_FORMAT_DOC` 要的那些。
坐标范围实测 X 1500~14100 / Y 1500~7650。

**关键一步：`entries[].id` 就是 `talent-data.js` 里的 `entryID`。**
实测 40/40 专精 specId 都能对上，我的 3040 个 entryID 里 **3039 个（100.0%）**
在 raidbots 里存在，**38/40 个专精逐专精 100% 命中**。

**中文名 100% 有解，而且不是手写的。** 连接链 `entries[].definitionId → TraitDefinition.ID`
→ 优先 `OverrideName_lang`，否则 `SpellID → SpellName.Name_lang`：
3334 个不同 definitionId **全部 3334 个都拿到中文名**（6 个走 OverrideName、3328 个走 SpellName），
查不到 0、拿到但仍是英文 0。样例：`Rake → 斜掠`、`Frenzied Regeneration → 狂暴回复`、
`Power Nexus → 能量枢纽`。**这是暴雪自己的字符串表，比插件 locale 更权威。**

**体积实测（这是要不要做的关键）**：

- 节点条目 40 专精相加 4613，去重后 2893，**按规范化 key 去重 3110**（见下）
- 只留必需字段的 JSON **447.7 KB**；名字/图标做字典后 **378.4 KB**（和现有 `talent-data.js` 520 KB 同量级）
- **图标是真正的大头：2094 个不同图标，按现有 462 张平均 2.2 KB 估算约 4.58 MB**，
  其中只有 3 张已经在包里。这比现在整个发布包（1.5 MB）大三倍。

**职业树能不能按职业共享：不能，但原因不是数据不一致。** 13 个职业里 9 个的三个专精
职业树「不同」，但按节点 id 集合比对后发现**字段差异 0 处** —— 差的只是
**哪些节点存在**（部分节点按专精开放）。所以正确做法是**全局 intern 节点**
（4613 → 3110 个不同节点），每个专精只存节点 id 列表。英雄子树 41 个，每个 14 节点。

**顺手用 `talents.json` 验出一个上游真 bug。**
`WARRIOR/PROTECTION` 的 171 个 entryID 里有 **49 个不属于防护树**：35 个是
武器（Arms）专精树的节点（Mortal Strike / Overpower / Die by the Sword / Tactician），
14 个属于英雄子树 60 = **Slayer**，而防护只能选 Colossus + Mountain Thane。
定位到是**单独一条玩家记录**（`mplusFarm / 红玉新生法池 / 绝密宝宝`，越界 49 项，
120 条记录里就这 1 条），即那一条其实是个武器战。同样地 `MONK/MISTWEAVER`
有 4/85 套越界、1 个 entryID（125051）raidbots 里完全没有（只出现在 1/86 套里）。
**38/40 个专精完全干净。** 这是 WCL 上游认错专精，不是我的转换问题
（转换本身早就验过 4797 条 0 不一致）。

---

## 任务列表

状态：`[ ]` 未做　`[~]` 进行中　`[x]` 完成　`[!]` 卡住（写明卡在哪）

### A. 图标本地化 —— **已完成**

- [x] **A1** `tools/blp.js`：BLP2 → PNG 解码器，零依赖。已验证出真图。
      （最后没用上 —— 散装目录里没有任何 10.x/11.x/12.x 图标，当前赛季没法靠它。文件留着备用。）
- [x] **A2** ~~gen-icons.js 从散装目录转~~ → **改为 `tools/fetch-icons.js`**：按 itemId 从 wowhead 查
      图标名 + 品质，再从暴雪 CDN / zamimg 两个源下图到 `app/icons/`。带断点续传
      （`tools/.icon-cache.json`）、限流、重试、`--limit` 试跑、`--names-only` / `--images-only`。
- [x] **A3** `app/bis.js` 接本地图标：装备 / 宝石 / 附魔 / 消耗品四处全部出图，
      品质改用真值，`iconBaseUrl` 降级为覆盖项。
- [x] **A4** 设置面板 + `settings.js` 注释 + README 三处过时说法全改（原本写着「装备图标显示不了」）。
- [x] **A5** ~~游戏内插件导 fileID~~ **不需要了**。wowhead tooltip 直接给图标名，绕开了
      fileID → 文件名 那一环（那一环需要 50 MB 的社区 listfile）。
- [x] **A6** 两个图标源的分工和原因已记在 `tools/fetch-icons.js` 文件头 + README。

### B. 数据源可替换（为「换数据源」铺路）

- [x] **B1** ~~schema 文档 + 校验器~~ **已完成**：`tools/verify-bis-data.js`。
      **schema 就写在校验器里**（`SCHEMA` / `SPEC_SCHEMA` / `SUB_KEYS` / 行格式注释），
      不另写 .md —— 另写一份迟早和代码脱节，写在校验器里它每次跑都被执行一遍。
      2016 项检查，`--data` 可以指向任意候选文件。已接进 `tools/run-tests.js`。
      **变异测试 31/31**：第一版漏了「`target` 缺 `farm`」（schema 里只写 `type: 'obj'`，
      子键要求只存在于散文描述里 → 补了 `SUB_KEYS` 逐个验子键，检查项 1296 → 2016）。
- [x] **B2** ~~数据质量审计写进 README + 面板显示样本量缺失~~ **已完成**：
      README 新增「这份数据有多可信」一节（对得上的 / 靠不住的两张清单，全是实测数字），
      已知限制里补了样本量那条，功能清单里加了覆盖率徽章。
      面板侧的做法不是再加一句免责声明，而是**每个部位组显示「记录 xx%」徽章**
      （= 列出的几件使用率之和，不到 50% 标警告色，tooltip 说明剩下的百分比数据里没有
      以及本数据不带样本量）。1264 个部位组一一对应，渲染检查里有硬断言。
      顺手用掉了 `.slot-head .tag` / `.tag.ok` / `.tag.no` 三条**从来没被创建过的死 CSS 规则**。
- [x] **B3** ~~修 `gen-bis.js` 丢 `bonusIDs` 的问题~~ **已完成，但不是照抄回来**：
      查清 `bonusIDs` 在插件里只用于拼游戏内 `|Hitem:` 链接（网页没这机制，装等本来就在行里），
      原样存 19 KB 白花。改成**解成升级轨道**（「英雄 6/6」）存 —— 见上面的事实小节。
      行格式变成 `[itemId, ilvl, 使用率, 来源下标, mx, 轨道码]`，尾部 0 可省但不许跳着省。
- [x] **B4** ~~探测替代数据源~~ **已完成，结果记在上面「替代数据源调研」**。一句话总结：
      **没有可用的替代品**。archon.gg / warcraftlogs / u.gg 本机 403（Cloudflare，直连和代理都不行），
      murlok.io 只有 PvP，bloodmallet 是模拟 DPS 而不是使用率、且只覆盖饰品和副属性配比，
      raidbots 只有 bonusID 词典。**换数据源这条路在本机是堵的**，所以重点应该放在
      「把现有数据的缺陷说清楚」而不是「换一家」。
      副产品：用 raidbots 独立验证了轨道解码（装等 12/12、名字+等级 30/30 全对）。
- [ ] **B5** 73 行 `Sporefused: Myth`（bonusID 13786）现在没有轨道徽章。AlterEgo 的
      `upgradeBonusLabels` 有英文名，**但全盘没有 zhCN 译名**，所以卡在「不许手写中文」这条规矩上。
      → 进「待用户提供」。
- [ ] **B6** 1039 行 / 102 件装备带插槽 bonusID，可以显示「插槽」标记（zhCN 有来源）。
      但插槽 bonusID 清单只有 raidbots 有，本机没有 —— 要做就得让生成器多联一次网。
      → 进「待用户决定」。

### C. 质量与收尾

- [x] **C1** ~~重建渲染冒烟测试~~ **已完成**：`tools/run-tests.js` 里的渲染检查，83 次渲染。
      断言是硬的（占位块必须 0、`<img>` 必须过 5000、不得引用不存在的文件）——
      第一版只统计不断言，变异测试（`AE_ITEM_ICONS = {}`）下占位块 0 → 3963 它照样返回「通过」。
- [x] **C2** `app/bis-tests.js` 补了 6 项图标测试（覆盖率、名字字符集、品质取值范围、
      宝石/附魔/消耗品覆盖）。**127 项全绿**（33 解析 / 66 模型 / 28 毕业装备）。
- [x] **C3** README 更新：图标的来源和两个 CDN 的分工、品质字段的发现、换赛季要多跑一步
      `fetch-icons.js`、目录树新文件、测试项数 82 → 127、已知限制里两条过时说法。
- [ ] **C4** 全部测试绿，工作区干净，提交到 `feat/bis-panel`。（每轮都过一遍，不算完成）
- [x] **C7**（本轮新增）**实测发布包内容**，不靠读脚本推断。`tools\build-release.ps1 -SkipBuild`
      产出 1497.9 KB / 502 个条目：
      - `app/icons/` **462 个都在**，解压后 1035.8 KB、压缩后 **987.6 KB**（jpg 压不动），
        占整个包的 **66%** —— 这是「待用户决定」第 4 条要的数字。
      - `$dropFromPkg` 四项全部排除干净（`launcher.ico` / `.icon-cache.json` /
        `dom-stub.js` / `run-tests.js`）。
      - 该在的都在，包括新加的 `tools/verify-bis-data.js`。
      - `data/` 里只有构建时生成的 `说明.txt`（内容是「这个文件夹可以删」的提示），
        没有任何个人数据；`.git` / 临时文件 / 旧 zip 一个都没进。
      - 版本号仍是 **1.14.0**，脚本不联网、不碰 git、不发布。
      **顺手记一个自己犯的错**：第一版检查脚本把所有条目都报成「缺」——
      `Compress-Archive` 写进中央目录的条目名用的是**反斜杠**，而我拿 `/` 去匹配。
      是检查脚本错了，不是包错了。解析 zip 时先把 `\` 归一成 `/` 才对。
- [x] **C5** 修两个生成器把字符数当字节数报的 bug：`out.length` 对中文差 3 倍，
      `bis-data.js` 一直被报成 182.5 KB，实际 196.3 KB。改用 `Buffer.byteLength`。
- [x] **C6**（做 B1 时顺带抓到的两个真 bug）
      1. **来源徽章 292 行显示英文 `tier`、5 行显示 `quest`**：插件自己的 `sourceCategories`
         只有 5 个键（raid/mplus/crafted/world/other），而数据里实际用了 7 个分类。
         中文名不许手写，所以让生成器**从数据自己的 `source` 文本补**，且只在该分类下
         `source` 唯一时才补（`tier` → 「套装转换」，`quest` → 「奇点声望任务」），
         不唯一就留英文并警告。加了测试盯住「每个用到的分类都有标签」。
         · 顺带否掉一个诱人的错解法：`Syndicator` 里有 `L["KEYWORD_SET"] = "套装"`，
           但 `EllesmereUILocales` 里 `L["Set"] = "设置"` —— 同一个英文词在另一个 locale
           里是完全不同的意思。**按通用词查 locale 不安全**，只能用数据自己的文本。
      2. **`.item .tag` 底样式整条是漏的**：CSS 里只有 `.item .tag.use` 和 `.item .tag.trk`，
         没有 `.item .tag` 本身；`.slot-head .tag` 是另一个祖先管不到。于是来源徽章的
         `border-width` 是 0，而 `bis.js` 里 `badge.style.borderColor = catColor(cat)`
         只设颜色 —— **等于给一条不存在的边框上色，徽章一直是裸文字**，而且没有
         `flex: none` / `nowrap`，6 个字的「奇点声望任务」会被压缩或折行撑高整行。
         补了底样式，`.use` / `.trk` 简化成只覆盖颜色。

### D. 天赋树 —— **已完成**（第 8 轮，提交 `ea5c83e` / `5101f5c` / `ee64a85`）

这是面板**最大的已知限制**，已经解决。D 组刻意设计成**不依赖图标**：
2094 张天赋图标实测 4.58 MB，是现在整个包的 3 倍，那件事仍在「待用户决定」第 6 条。
没有图标的树用「名字方块 + 连线 + 点数」照样能看懂点在哪。

- [x] **D1** `tools/fetch-talent-tree.js`（19 KB）→ `app/talent-tree.js` **415.3 KB**，
      中文名 **5387/5387 = 100.0%**，40 专精 / 2891 节点 / 41 子树 / 6579 连线。
      四个源缓存在 `tools/.talent-raw/`（gitignored，14 MB），
      **只把连接后的结果**存进 `tools/.talent-names.json`（271 KB，入库）——
      重新生成既不用 10 MB 的 CSV 也不用联网。`--refresh` / `--offline` / `--proxy`。
- [x] **D2** 节点全局 interning：4613 引用 → 2891 节点。
      **一致性守卫抓到我自己的错误设计**：把 `next`/`prev`/`freeNode` 也全局化时
      **133 个节点冲突**（同一个法师职业节点在奥术通向 108664、火焰 108656、冰霜 108655；
      血 DK 的某个职业节点是白给的而冰霜/邪恶的不是）。
      → **静态字段全局共享，拓扑（`edges` / `free`）按专精存。**
      `tools/verify-talent-tree.js`（22 KB）**76377 项检查**，变异测试 **30/30 全抓**。
- [x] **D3** 交叉验证 `app/talent-data.js`，并且**把两种诊断分开报**：
      「跨专精污染」= entryID 在树里但属于别的专精（WARRIOR/PROTECTION **1/77**）；
      「引用未知 entryID」= raidbots 里根本没有（MONK/MISTWEAVER **4/86**，entryID 125051）。
      两个数都和第 7 轮独立测出来的一致，38/40 专精干净。
- [x] **D4** `app/bis.js` 天赋页画树。**坐标没有除以 150** —— 计划原本那么写是错的：
      实测坐标最大公约数只有 10，还有「相差 10」的近重复坐标，直接缩放会把节点叠在一起。
      实测它本质是网格（职业树严格 600×600，专精树以 600 为主，英雄子树 5×5），
      所以**按容差聚类成列/行再摆网格**，容差 50~250 都试过，同格重叠恒为 0。
      节点上放**全名**而不是缩写：实测同一棵树内「前 2 字重名 496/3447」
      （「强化」既是强化法术吸取也是强化法术反制），缩写会主动骗人。
      顺手把面板里 39 个英文英雄天赋名换成中文（靠 `TraitSubTree` 的英文名连接，39/39 对上）。
- [x] **D5** `tools/run-tests.js` 加第 5 行「天赋树渲染」：40 个专精各画一遍 +
      3 个类别各画一遍，**硬断言** 树数 = 渲染次数 × 3、节点 ≥ 4000、点亮比例 40~95%、
      同一专精内节点 id 不许重复、中文名 0 例外、点数徽章格式。
      变异测试 **9/9 全抓**（含「专精树画成职业树」——
      这条一开始漏了，因为总数看着仍然合理，加了「节点 id 不重复」才抓住）。
      英雄天赋中文名另做 2 个变异，2/2 抓到。
- [x] **D6** README 那一节从「画不出来」改成「能画了，数据从哪来」，
      写清中文名来自暴雪 DB2（不是我翻的）、没有图标是刻意的选择、
      以及仍然做不到的三件事（导出串 / 点着改 / 上游 1 套脏数据）。
      同时更新了已知限制、功能列表、目录结构、换赛季步骤（现在是四步）、换数据源一节。

### E. 天赋导入串 —— **卡在一个只有用户能验的问题上**

第 10 轮开始查的。起因是想给天赋面板补上「和你自己的天赋比一比」，
结果先撞见一件更要紧的事：**README 里我自己写过的一句话是错的**。

- [x] **E1** 找到玩家自己的天赋数据在哪。`WTF` 下 `TalentLoadoutsEx.lua` 和
      `BtWLoadouts.lua` 存的就是**官方导入串**，本机 36 个文件里 54 个串。
      （AlterEgo 的存档里**完全没有**天赋字段，`talent` / `loadout` / `traits` 全 0 次，
      只有 `specID` 出现 125 次。所以「比一比」的数据只能从这两个插件来。）
- [x] **E2** 串头 **54/54 全解开**：8 位版本（全是 2）+ 16 位 specID + 128 位 `treeHash`，
      标准 base64、每字符内低位在前。`treeHash` 是**按职业树**的（防护 / 惩戒圣骑同一个，
      鲜血 / 邪恶死骑同一个，浩劫 / 复仇 DH 同一个）。
- [x] **E3** 纠正 README。原话「导出串要 `treeHash` 和游戏权威的节点顺序，公开数据里没有这两样」
      **一半是错的**：raidbots 的 `talents.json` 里就有 `fullNodeOrder`，
      13 个职业全查过 —— 同一职业内每个专精的这份顺序**完全相同**，
      且等于该职业各专精节点集合的并集（死骑 206 = 本专精 114 + 别专精 92 + 未知 0；
      德鲁伊多 2 个、圣骑多 1 个对不上）。缺的只有 `treeHash`。
- [x] **E4** `tools/decode-talent-string.js`（9 KB，永久工具，不是临时脚本）。
      `<串>` / `--scan` / `--verify` / `--flat` / `--rankbits N` / `--wtf` / `--limit`。
      中文名从 `app/talent-tree.js` 取，节点顺序从 `tools/.talent-raw/talents.json` 取。
- [ ] **E5 卡住** 节点位流只有 **7/54** 是干净的（走完整个 `fullNodeOrder`
      且三条语义检查全 0）。已实测排除的解释：
      · **字段布局不对** —— 18 种布局全试（额外位有无 × 位宽 4/5/6 × 顺序 × 嵌套），
        最好的就是现在这个（嵌套 + 6 位），`--flat` 是 **0/54**，还引入 24~46 个点数超上限。
      · **版本错位** —— 「和该专精主流 hash 一致的串才对」被数据**反过来否掉**：
        一致的 5% 干净，不一致的 42%。
      · **节点顺序取错** —— 只留本专精节点或按 id 排序都是 **0/54**，全量 `fullNodeOrder` 最好。
      · **顺序只差几个节点** —— 按专精搜一组「幽灵节点」补丁（要求修好该专精**全部**串），
        补到 13 个仍然 0/N，10 个专精里只有 Devourer DH 不需要补丁。
      剩下的错误里最大一类是「二选一下标放在了没有二选一的节点上」（47/54），
      而那 47 个节点**全部是 `type=single entries=1`** —— 对齐正确的流里不可能出现这种事，
      所以它是**错了一位之后的下游垃圾**，不是独立的毛病。
      两个很强的线索（下一轮从这里接）：
      · **没有任何一个串是从第 0 个节点就错的** —— 第一个违规点最早在 4%，中位 16%，最晚 42%。
      · **同一专精的不同串，第一个违规点下标完全相同**（复原萨 8 串都是 35，
        复仇 DH 5 串都是 42，浩劫 DH 4 串都是 96），**连 `treeHash` 不同的也一样**。
        按内容漂移会散，下标恒定只能是顺序本身的系统性偏差。
      下一步候选：**别用 raidbots 的 `fullNodeOrder`，改从暴雪 DB2 自己排**
      （wago.tools 上的 `TraitNodeXTraitCond` / `TraitNodeGroupXTraitNode` 等表）。
      **这一项没做完，不要当成做完了。**
- [ ] **E6 等用户** 全 0 的 `treeHash` 游戏认不认 → 「待用户决定」第 7 条。
      认了就能给 40 个专精都编码；不认这条路就封死。

### F. 无障碍 —— **已完成一轮**（第 11 轮，提交 `8ede310`）

第 11 轮开始查的。起因：README 里写「悬停节点给中文名 / 二选一 / 层级要求 / 点数」，
去核实这句话是不是真的，结果核出了一串**关于我自己的测量工具**的问题。

- [x] **F1** 核实 tooltip 确实能显示。`app/tip.js` 是委托到 document 上的，
      读的是 `data-tip`，天赋节点每个都带 —— 这句话是真的。
- [x] **F2** 修掉 `tools/dom-stub.js` 的**保真度缺口**。桩只有 `setAttribute`，
      不做「属性 → attribute」反射。而 `app/` 里到处是 `img.alt = ''`、`td.title = ...`
      这种**属性赋值**（`.title =` 有 48 处），在真浏览器里会反射成 attribute，在桩里不会。
      后果：我用桩量出「621 个 `<img>` 全都没有 alt」，**那是假的**，桩自己看不见而已。
      现在桩对 `id/alt/title/src/href/type/value/width/height/tabIndex` 做双向反射。
- [x] **F3** 用修好的桩重新量，面板本身比我以为的干净：
      `<img>` **全有 alt**（装饰性图标，`alt=""` 是正确写法，不是漏写）、
      `<button>` **全有可见文字**、带 `data-tip` 的元素 **2386/2386 都有可见文字**
      （所以 tooltip 是补充信息，不是元素的名字 —— 这一点很重要，
      否则就得上 `aria-label`，而那会覆盖可见文字，反倒违反 WCAG 的 Label in Name）。
- [x] **F4** 找到并修掉**一个真的缺陷**：天赋树画布是一堆绝对定位的 `<div>` 拼出来的图，
      **没有 role、没有说明**。读屏软件只会念到一串互不相干的天赋名。
      现在每个画布带 `role="group"` + 中文说明（例：「职业天赋树，44 个天赋，已点 31 个」）。
      选 `group` 而不是 `img`：`role="img"` 会把子节点全藏起来，那些天赋中文名就读不到了。
- [x] **F5** 把这四条量化结果**升级成永久断言**（`run-tests.js` 第 6 行「无障碍」）：
      图标全有 alt、按钮全有名字、带 `data-tip` 的元素全有可见文字、画布全有 role + 说明。
      变异测试 **5/5 全抓**（含两个「把可见文字抽掉、只留 tooltip」）。
- [x] **F6** 纠正 `app/tip.js` 头注释里的两处错话：说「31 处调用点，加上面板」——
      实际 `.title =` 有 48 处，而**面板一处都没有**（面板直接写 `data-tip`，
      所以走不到 `title → aria-label` 那条镜像路径）。

---

## 待用户决定（不要自己拍）

1. **换不换数据源，换成哪个。** ~~B4 会给出调研结果~~ **B4 已做完，结论是没有可用替代品**
   （详见上面「替代数据源调研」）。所以这一项现在的实际问题变成：接受现有数据的缺陷
   （无样本量、列表被截断）并把话说清楚，还是干脆不做这个面板？我的判断是前者，
   因为「顶尖玩家在穿什么」本身有价值，只要不假装它是模拟结论。
2. ~~fileID → 图标名的表从哪来~~ **不需要了**，wowhead tooltip 直接给名字。
3. **要不要继续带 GearInsight 的数据。** 这是在转发别人的成果，README 已注明出处。
4. **发布包里多了 1 MB 图标（462 个 jpg）。** 接受不接受？不接受的话只能回到运行时拉图，
   而那在国内靠不住（zamimg 直连 403）。
   **已实测**（`tools/build-release.ps1 -SkipBuild` 出的包，502 个条目）：
   整包 **1497.9 KB**，其中图标压缩后 **987.6 KB —— 占 66%**。jpg 本来就压过，
   zip 再压只省了 4.6%（解压后 1035.8 KB）。也就是说不带图标的话包是 ~510 KB。
5. **要不要显示「插槽」标记（B6）。** 1039 行 / 102 件装备带插槽 bonusID，中文名有 locale 来源
   （`Syndicator/Locales.lua` 的 `KEYWORD_SOCKET` = 「插槽」）。**代价是生成器要多联一次网**：
   插槽 bonusID 清单只有 raidbots 的 `bonuses.json`（1.7 MB，直连可达）有，本机磁盘上没有。
   现在 `gen-bis.js` 是纯离线的，加这个就破了这一点 —— 所以不自己决定。
6. **天赋树的图标要不要打包。** **D 组已做完并入库**，树是**不带图标**发的：
   节点画成方块 + **完整中文名**（不是缩写 —— 实测同树内前 2 字重名 496/3447，缩写会误导），
   坐标 / 连线 / 点亮状态 / 点数都是真的。带图标的包实测会从 **1630.9 KB** 变成约 6 MB。
   三个选项：
   - **不带**（现在的做法，已入库）：包 1630.9 KB，树可读，鼠标悬停给中文名 /
     二选一的两个选项 / 层级要求 / 点数。
   - **带**：要新写一个 `tools/fetch-talent-icons.js`（照 `fetch-icons.js` 的样子，
     图标名已经在 `app/talent-tree.js` 的 `icons` 字典里，2094 个，不用再查），
     跑一次下 2094 张，包变成 ~6 MB。**没写，等这一条定了再写。**
   - **只带热门的**：只下实际出现在 `talent-data.js` 里的节点图标（**实测 1447 张 ≈ 3.17 MB**）。
     但注意这个省法有个逻辑问题：天赋树要画的是**整棵树**，没点的节点也要显示，
     所以缺的那 647 张会变成没图标的空节点 —— 省 1.4 MB 换来一棵半残的树，不划算。

7. **天赋导入串：全 0 的 `treeHash` 游戏认不认？** 这一条**只有你能验**，我在命令行里验不了。
   背景见 README「天赋导入串：解到一半」一节。要点：编码一个官方导入串需要
   `fullNodeOrder`（**有**，在 raidbots 那份数据里，按职业，13 个职业全对上了）和
   `treeHash`（**没有**，公开数据里查不到）。但本机存档里有 **2 组串的 `treeHash` 是全 0** 的
   （一个复原萨满、一个 Devourer DH），说明**插件**收下了全 0 hash。
   如果**游戏**也认，那 `treeHash` 就不需要了，40 个专精都能编码 ——
   这是「把别人的套路一键粘进游戏」这个功能唯一的拦路石。
   **怎么验**：进游戏，天赋界面点「导入」，粘下面这一串。这是**复原萨满**的，
   从你自己 `TalentLoadoutsEx.lua` 里原样取出来的（不是我编的），
   而且它的 `treeHash` 那 128 位**全是 0** —— 这正是要验的那种串：
   ```
   CgQAAAAAAAAAAAAAAAAAAAAAAAAAAAAGAAAAAzMzMzyMWmZMzwsMWMsgFYDmxiGbDIzAbMzMLYWmZmRmtFzMTjZwALzYGbGDziZAAMA
   ```
   认了（能看到一套完整天赋）→ 告诉我，我就能写编码器，40 个专精都能编；
   报「无效的导入串」→ 这条路封死，我把 README 那一节改成结论。
   注意别真点「应用」，看一眼能不能识别就行。

## 待用户提供（本机拿不到的字符串）

- 4 个职业中文名：EVOKER / MAGE / MONK / ROGUE（7895 个存档文件里没有本地化职业表）
- 死骑冰霜专精中文名（现在显示英文 `FROST`，因为插件的表给的是「冰法」）
- `PRESERVATION`（唤魔师）/ `DISCIPLINE`（牧师）的中文专精名
- **`Sporefused` 的中文名**（B5）：影响 73 行装备的轨道徽章。AlterEgo 的 `upgradeBonusLabels`
  只给英文 `Sporefused: Myth`，raidbots 也是英文 `tag: "Sporefused: Myth"`，
  全盘 7900 个文件里没有任何 zhCN 译名。有了译名这 73 行就能显示轨道。

---

## 迭代日志

追加在末尾，一轮一条，写「做了什么 + 验证结果」，不要写「打算做什么」。

- **第 0 轮**：撤销 v1.15.0（Release / tag / 远端提交全删，代码留在工作区，
  安全分支 `wip-bis-v1.15.0` 指向 `14ee307`）。版本号退回 1.14.0。
  查明图标瓶颈是 itemId → 图标名的映射，不是 CDN。写出 `tools/blp.js` 并验证解码正确。
  做完 BisData 数据质量审计。建立本文件。

- **第 1 轮**：图标全部打通，离线可用。
  - 建分支 `feat/bis-panel`，把 v1.15.0 的代码提交在上面（`a624736`），`main` 不动。
  - 先否定了自己上一轮的方案：散装图标目录里 `inv_10*/inv_11*/inv_12*` **各 0 个**，
    当前赛季装备根本不在里面 —— 「纯本地转 BLP」这条路对装备是死的。
  - 改探网络可达性，找到两个能用的源（暴雪 CDN 直连可达；zamimg 走代理，且是
    当前赛季新图标的**唯一**来源）。wowhead tooltip 接口同时给了图标名和**品质**。
  - 写 `tools/fetch-icons.js`，产出 `app/item-icons.js` + `app/icons/`（462 个 jpg，1 MB）。
    568 个 itemId 100% 覆盖，0 断链，0 孤儿。
  - `app/bis.js` 四处调用点全改成本地图标 + 真品质。
  - **抛弃临时 harness**，改成正式的 `tools/dom-stub.js` + `tools/run-tests.js`。
  - 干活中抳出的真 bug：
    1. `fetch-icons.js` 第一版读 `c.itemId`，而 `gen-bis.js` 压缩后的键叫 `c.id` ——
       35 个消耗品被静默漏掉，而覆盖率还报 100%（**漏掉的东西不在分母里**）。
    2. zamimg 那批第一版下成 `medium`（36×36），和其他 56×56 不一致。
    3. 渲染 harness 第一版**只统计不断言**，变异测试下占位块 0 → 3963 仍报「通过」。
    4. 回退路径（BisData 自带图标名）指向 3 个**不存在的文件** —— “有退路”只是句话。
    5. 品质硬写 `qualityColors[4]`，把 54 件蓝的和 14 件白的染成了紫色。
  - `build-release.ps1` 加排除列表：缓存和测试 harness 不进发布包。
  - README 五处过时说法全改（本来写着「装备图标显示不了」）。
  - **验收：`node tools\run-tests.js` → 127 项测试 + 渲染检查全绿。**

- **第 2 轮（B3）**：`bonusIDs` 处理完，但结论和任务原本的说法相反。
  - 任务写的是「修 `gen-bis.js` 丢 `bonusIDs` 的问题」。先量了再做：原样存要 19~24.6 KB，
    而查遍两个插件，`bonusIDs` **只用来拼游戏内 `|Hitem:` 链接**让游戏 tooltip 显示对的装等 ——
    网页没有这个机制，装等本来就在行里。**所以「补回来」本身是没价值的，补的应该是解码结果。**
  - 从 `AlterEgo/Data/UpgradeTracks.lua` 抄到赛季 17/18 的轨道表（10 轨 × 6 级 = 60 个 bonusID），
    解出 **3601/3963 行（90.9%）** 的「英雄 6/6」这种标签，**0 行命中两个轨道**（无歧义）。
  - 证明轨道**推不出来、只能存**：装等 308/318/321 各对两种轨道，连 `(装等, 来源)` 都还有
    5/24 组歧义（`321/raid` 是英雄 6/6 ×528 vs 神话 2/6 ×10）。
  - 中文名不手写：**两处互相独立**的 shipped zhCN locale（EllesmereUILocales、ItemInfoOverlay）
    完全一致才用，生成器两边都读、不一致就 `exit(1)`。
  - 行格式扩成 6 位，`mx` 位不再允许跳过（详见上面「代码」一节）。
  - 顺手抓到的真 bug：**两个生成器报的体积全是错的** —— 用字符数当字节数，中文 1 字符 3 字节，
    `bis-data.js` 少报 15 KB、源 Lua 少报 160 KB。所以我以为加了轨道「体积反而变小」，
    实际是 186931 → 201010 字节（+13.7 KB）。已改用 `Buffer.byteLength`。
  - 新增 4 项数据测试 + 渲染检查加「轨道徽章数」断言。**两批都做了变异测试**：
    7 个数据变异（轨道码越界 / 等级 0 / 等级 7 / mx < ilvl / 缺中文名 / 删轨道池 / 覆盖率清零）
    全部被抓；渲染断言在 `tracks = []` 下从「通过」变成报错。
    第一次变异跑还白跑了一次 —— 改动点插在渲染循环**之后**，什么也没证明，挪到前面才有效。
  - 渲染出的徽章数 **3601** 和生成器统计的解码行数完全一致（两条独立路径互相印证）。
  - **验收：`node tools\run-tests.js` → 131 项测试 + 渲染检查全绿。**

- **第 3 轮**：格式校验器（B1），以及两个顺手抓到的真 bug。
  - `tools/verify-bis-data.js`：**schema 就是文档**，不另写 .md（另写必然和代码脱节）。
    2016 项检查，覆盖顶层字段、`items` / `srcs` / `tracks` / `specs` / 行 / 宝石 / 附魔 /
    消耗品 / 部位名与属性名覆盖 / 来源分类标签覆盖。有 `--data` 参数，可以拿来验**候选**
    数据源的产物 —— 这正是「换数据源只换生成器」需要的那道关。
  - 变异测试 31 个，第一轮 **30/31**：删掉 `target.farm` 它照样过。原因是 schema 里
    `target: { type: 'obj' }` 只保证「是个对象」，三套子键只写在**说明文字**里。
    补成可执行的（`SUB_KEYS` + 属性名逐个验），31/31，检查项 1296 → 2016。
    **教训：写在注释里的约束等于没有约束。**
  - **真 bug 1：292 行套装 + 5 行任务装备的来源徽章一直显示英文 `tier` / `quest`。**
    插件自己的 `sourceCategories` 只有 5 个键（raid/mplus/crafted/world/other），
    数据里实际用到 7 个。中文名不许手写，所以从数据自己的 `source` 文本补：
    这两类各自只有一种 source 文本（`套装转换` / `奇点声望任务`），无歧义才用，
    有歧义就留英文并警告。加了测试，删掉任一标签立刻失败（6 个变异全抓到）。
    · 顺带否掉了一个更省事但不安全的做法：从 locale 里查 `L["Set"]`。
      `EllesmereUILocales/zhCN.lua` 里 `L["Set"] = "设置"` —— 同一个英文词在别的语境里
      是「设置」。**通用词查表不安全，得看语境。**
  - **真 bug 2：`.item .tag` 这条 CSS 底样式是漏的。** 只写了 `.tag.use` 和 `.tag.trk`，
    `.slot-head .tag` 是另一个祖先管不到，所以来源徽章 `border-width` 是 0，而
    `bis.js` 里 `badge.style.borderColor = catColor(cat)` 只设颜色 ——
    等于给一条不存在的边框上色，徽章从一开始就是裸文字，没有胶囊、没有 padding、
    没有 `flex: none`（会被压缩）、没有 `nowrap`（6 个字的「奇点声望任务」会折行撑高整行）。
    补上底样式，`.trk` 简化成只覆盖颜色。
  - `.gitignore` 加 `_*` —— 上一轮我自己把临时文件 `_msg.txt` 提交进去了（`74a849a`）。
    规矩靠 gitignore 执行，不靠我记得。
  - 校验器接进 `run-tests.js`，破坏 `tracks[0]` 验证过两层都会失败、退出码 1。
  - **验收：`node tools\run-tests.js` → 133 项测试 + 渲染检查 + 2016 项格式校验全绿。**

- **第 4 轮（B2）**：把「这份数据有多可信」变成用户看得见的东西，而不是又一句免责声明。
  - **每个部位组加了「记录 xx%」徽章**（使用率之和），不到 50% 标警告色，
    悬停给出「剩下的 xx% 穿的是什么，数据里没有」+「这份数据不带样本量」。
    为什么按部位显示而不是在页脚多写一句：截断程度**每个部位都不一样**
    （1264 组里之和的中位数 72.9%、最低 8.6%、有 206 组不到 50%），
    一句笼统的话既不能告诉用户哪一组不可信，也很容易被跳过。
  - 复用了 CSS 里三条**死规则**：`.slot-head .tag` / `.tag.ok` / `.tag.no` 早就写好了，
    但 `bis.js` 从来没创建过带这些 class 的元素。没删，改成派上用场。
  - 布局上踩到一个点：`.slot-head .tag` 和 `.mine` 都带 `margin-left: auto`，
    两个都在的时候剩余空间被**平分**，`.mine` 就不再靠右了。所以覆盖率徽章紧跟部位名放，
    并用 `.tag.cov { margin-left: 0 }` 覆掉。
  - README 新增「这份数据有多可信」一节，用实测数字写：对得上的四条 + 问题五条
    （没有样本量 / 尾部被截断 / 权重是硬编码的 / 上游分类表缺 tier+quest / 饰品重复），
    并说明为什么仍然选它。已知限制里也加了一条。
  - 渲染检查新增两条硬断言：**覆盖率徽章数必须等于部位组数**（1264 == 1264，
    和生成器的统计独立对上），且文字必须形如「记录 93.6%」。
    两个变异（不画徽章 / 文字写成 `cov 93.6`）都被抓到、退出码 1。
  - **验收：`node tools\run-tests.js` → 133 项测试 + 渲染检查 + 2016 项格式校验全绿。**

- **第 5 轮（B4）**：替代数据源调研，结论是**没有可用的替代品**，并顺带独立验证了轨道解码。
  - 探了 8 个站点的可达性（直连 + 代理各一次）。三个被 Cloudflare 挡死（archon.gg /
    warcraftlogs / u.gg，**直连和代理都 403**），三个可达（bloodmallet / raidbots / murlok.io），
    wowhead 只有走代理能通。
  - 逐个查结构，没一个能替代：murlok.io **只有 3v3 PvP**（`/mythic-plus`、`/raid`、`/pve` 全 404）；
    bloodmallet 是**模拟 DPS 而不是使用率**，而且这个专精只有 5/11 种数据类型、
    只有 1/5 种战斗风格有数据（其余返回 76 字节的 `{"status": "error"}`）；
    raidbots 只是 bonusID 词典，没有任何装备推荐。
  - **副产品：用 raidbots 的 `bonuses.json` 独立验证了上一轮的轨道解码。**
    BisData 实际用到的 12 个轨道 bonusID，装等 **12/12** 和 raidbots 完全一致；
    赛季 18 那 30 个 ID 的 `upgrade.name` + `level` + `max` **30/30** 和 AlterEgo 的表完全一致。
    赛季 17 那 30 个在 raidbots 的 live 数据里已经没有 `upgrade` 字段（只剩 `itemLevel`），
    所以改用装等验 —— 也全对。两条完全独立的路径得出同一个结果。
  - 同时确认了解不出轨道的 37 个 bonusID **确实不是轨道**：raidbots 显示它们是插槽
    （`socket: 1`）、制造属性（`craftedStats`）、镶嵌效果（`effect`）、服务端标记
    （`serverside: true`）、物品唯一分类（`item_limit_category`）。「解不出」是对的行为。
  - 挖出两个可做的改进（都进了待决定/待提供，不自己拍）：**73 行** 带 `Sporefused: Myth`
    却没有轨道徽章（缺 zhCN 译名）；**1039 行 / 102 件**带插槽 bonusID 可以标「插槽」
    （中文名有 locale 来源，但清单只有 raidbots 有，要联网）。
  - **一次测量失误值得记**：第一版统计「带 label / 带插槽的行数」时用
    `/\{[^}]*?bonusIDs\s*=\s*\{([^}]*)\}/g` 切行，`[^}]*?` **跨不过嵌套的 `stats = { … }`**，
    只匹配到 480 / 3963 行，于是两个数都报 0。看着像「没有这种数据」，其实是正则漏了 88%。
    改成按 `{ itemId =` 边界切行之后才得到 73 / 1039 这两个真数。
    教训和上一轮同一个：**先验证测量本身，再相信测量结果**。

- **第 6 轮（C4）**：验证发布包真的装了我以为的东西。
  - `tools/build-release.ps1 -SkipBuild` 出包，**没有任何联网 / git / 发布动作**（读过脚本确认），
    版本号仍是 1.14.0。写了个临时脚本自己读 zip 的中央目录逐条核对。
  - **`app/icons/` 462 个条目全在**，解压后 1035.8 KB、压缩后 987.6 KB；
    整包 1497.9 KB / 502 条目 → **图标占 66%**。jpg 本来就压过，zip 再压只省 4.6%。
    不带图标的话包是 ~510 KB。这个数已经填进「待用户决定」第 4 条。
  - `$dropFromPkg` 四项（`launcher.ico` / `.icon-cache.json` / `dom-stub.js` / `run-tests.js`）
    确认都不在包里；`index.html` / `bis*.js` / `item-icons.js` / `talent-data.js` /
    `verify-bis-data.js` / `tests.html` 确认都在；没有 `data/` 个人数据、没有 `.git`、
    没有 `_` 开头临时文件。
  - 两个「异常」查过是故意的：`魔兽看板.exe` 是启动器本体，`data/说明.txt` 是脚本
    现场生成的空目录占位说明（内容是写死的两句话，不含任何个人信息）。
  - **我的检查脚本第一版全是假阴性**：`Compress-Archive` 写进中央目录的条目名用的是
    **反斜杠**，而我拿 `/` 去匹配，于是「app/icons/ 条目 0」「index.html 缺」——
    全是我自己的 bug。改成解析时把分隔符统一成 `/` 才看到真相。
    教训和上一轮那个 `[^}]*?` 跨不过嵌套表一样：**先怀疑量具**。

- **第 7 轮（打通天赋树的数据来源，代码还没动）**：A/B/C 三组除了卡在用户身上的 B5/B6 已经做完，
  所以给自己定了下一个长期目标 —— **面板最大的已知缺陷「天赋树画不出来」**。
  这一轮只做调研和验证，一行功能代码都没写，因为先要确认数据真的存在。
  - **树的结构找到了**：`raidbots.com/static/data/live/talents.json`，3.2 MB，**直连 200，不用代理**。
    40 个专精，每个带 `classNodes / specNodes / heroNodes / subTreeNodes`，
    节点字段 `id, name, type, posX, posY, maxRanks, next, prev, entries[]` ——
    正是 `AE.TALENT_TREE_FORMAT` 里我自己写下的那份格式所需要的全部字段。
  - **关键的连接验证**：`entries[].id` 就是 `talent-data.js` 里的 `entryID`。
    **38/40 专精 100% 命中**，全局 3039/3040。
  - **两个不命中的专精查清了，是上游数据的真 bug，不是我的转换问题**：
    · 防护战 77 套里有 1 套其实是**武器战**的天赋（含 Mortal Strike / Overpower /
      Tactician 等武器专精节点 + subTree 60 = Slayer，而防护战只能选 Colossus / Mountain Thane）。
      定位到具体记录：`mplusFarm / 红玉新生法池 / 绝密宝宝`，越界 49 项，影响 1/120 条玩家记录。
    · 织雾僧 4/85 套越界，涉及 entryID 125051（raidbots 里根本没有，只在 1/86 套里出现一次）。
    · **38/40 专精完全干净。** 这解释了为什么之前「验证编码 0 处不一致」和这里「有越界」
      能同时成立 —— 我之前验的是「我有没有转错」，没验过「上游给的对不对」。
  - **中文名也有了，而且是最硬的来源**：`wago.tools` 提供暴雪 DB2 的 CSV 导出，
    `?locale=zhCN` 直连可达。连接链 `entries[].definitionId → TraitDefinition.ID
    → (OverrideName_lang 或 SpellID → SpellName.Name_lang)`。
    **实测 3334/3334 = 100% 拿到中文名，0 个漏、0 个回落成英文。**
    这不是插件 locale，是**游戏自己的字符串表**，比现有任何来源都硬。
  - **量了体积**（决定了 D 组怎么切）：
    · 只留必需字段的 JSON 447.7 KB，名字/图标做字典后 **378.4 KB**（和现有 `talent-data.js`
      的 520 KB 同一量级，可接受）。
    · 节点条目 40 专精相加 4613 个，**规范化去重后 3110 个**（可以做全局节点池）。
    · **图标 2094 张 ≈ 4.58 MB，是现在整个发布包的 3 倍** → 进「待用户决定」第 6 条。
      所以 D 组刻意设计成**不带图标也能用**（画圆圈 + 中文名）。
  - **纠正一个「职业树按职业共享」的错判**：第一版脚本报「9 个职业各有 3 种不同的职业树」，
    看着像每个专精的职业树都不一样。按节点 id 集合重新比才发现**字段差异 0 处**，
    差的只是「哪些节点在」（专精门控）。所以能共享的是**节点本身**，不是整棵树。
  - **也纠正了自己刚写进计划里的一个没测量的数**：「只带热门图标 1387 张」是我拍的，
    实测 1447 张 / 3.17 MB；而且这个省法逻辑上有问题（没点的节点也要画，会变成空圈），
    已在计划里改掉。
  - 验收：`node tools\run-tests.js` 仍然 133 项全绿（这一轮没碰功能代码）。

- **第 8 轮：D 组全部做完，天赋树画出来了**（提交 `ea5c83e` / `5101f5c` / `ee64a85`）。
  这是面板最大的已知限制，现在不是限制了。
  - **D1 生成器** `tools/fetch-talent-tree.js` → `app/talent-tree.js` **415.3 KB**：
    40 个专精，节点引用 4613 → **全局去重 2891**，条目引用 5387 → 3414 个不同 entryID，
    连线 6579，英雄子树 41，名字字典 3106，图标字典 2094，**中文名 5387/5387 = 100.0%**。
    四个源缓存在 `tools/.talent-raw/`（14 MB，已 gitignore），
    **只把连接后的结果**存进 `tools/.talent-names.json`（271 KB，入库）——
    重新生成不需要那 10 MB 的 `SpellName.csv`，也不需要联网。
  - **一个自查guard 又抓到我自己的错设计**：`next` / `prev` / `freeNode` 全局 interning
    失败 **133 处**（`prev` 90 / `next` 55 / `freeNode` 30）。原因是这些字段**专精门控**：
    同一个法师职业节点，奥术连 108664、火焰连 108656、冰霜连 108655；
    鲜血死骑的某个职业节点是白给的，冰霜/邪恶的不是。
    → 定下规则：**静态字段（坐标/点数/类型/条目）全局共享，拓扑（edges/free）按专精存。**
    这是第三次靠 guard 抓到错设计（166 处 `srcs` 冲突、133 处拓扑冲突、`target.farm` schema 漏洞）。
  - **D2/D3 校验器** `tools/verify-talent-tree.js`：**76377 项检查**，
    schema 写在校验器里（照 `verify-bis-data.js` 的路子）。**变异测试 30/30 全抓**。
    头两轮只抓到 28/30，漏的两个都补成了**实测出来的硬规则**，不是我拍的阈值：
    · 原来「名字字典允许 5% 英文」是我凭空给的宽容度，实测 **0/3106** 不含中文 ——
      改成**任何一项非中文就失败**，并留一个当前为空的 `ALLOW_EN` 白名单，
      将来真出现未本地化的新天赋时报错信息会指到这里。
    · `maxRanks == 0` 实测**只出现在 40 个 subtree 节点上**（条目层面只出现在 80 个 subtree 条目上），
      所以规则从「0~10 之间」收紧成「非 subtree 节点必须 ≥ 1」。
  - **交叉验证拆成了两种诊断**（原来混成一个数，对不上第 7 轮的测量）：
    · **跨专精污染**（entryID 在树里但属于别的专精）：`WARRIOR/PROTECTION 1/77`
    · **引用未知 entryID**（raidbots 里根本没有）：`MONK/MISTWEAVER 4/86`，1 个 ID（125051）
    两个数现在和第 7 轮独立量出来的 1/77、4/85 完全对上。
  - **对齐了两个口径不一致的计数**：生成器说 5387 条目、校验器说 3414，一度以为有 bug。
    独立数了一遍：上游按引用 4613 节点 / 5387 条目，去重后 2893 节点 / 3414 条目，
    产物 2891 节点（差的 2 个是丢掉的占位节点，对应 6 个引用）。
    **全部对上**，生成器的输出文案改成「条目引用」以免下次再误判。
  - **D4 画树**（`app/bis.js` + `app/style.css`，290 行新增）：
    · **坐标不量化。** 计划里原本写「除以 150」，实测坐标最大公约数只有 10，
      而且存在「相差 10」的近重复坐标 —— 除法会把两个节点叠在一起。
      改成**把不同坐标值按容差聚成列/行**：实测容差从 50 到 250 同格重叠**始终为 0**，
      最大 9 列 × 11 行（职业树是严格的 600×600 网格，专精树 600 为主，英雄子树 5×5）。
    · **节点里放全名，不放缩写。** 本来想在圆圈里塞 2 个字，实测**同一棵树内前 2 字重名 496/3447**
      （「强化」既是强化法术吸取也是强化法术反制）—— 那样的标签是**误导**，不是省地方。
      改成方块节点放完整中文名（2322/3447 个名字正好 4 字）。
    · 连线用旋转的 `<i>` 而不是 SVG：全库没用过 SVG，测试用的 DOM 桩也没有 `createElementNS`。
    · 「套路」按钮按人数排序可切换；`state.build` **故意不持久化** ——
      套路编号是生成时的数组下标，重新生成数据后会指向另一套天赋。
    · **顺手修掉了 39 个英文英雄天赋名**：`TraitSubTree` 的中文名 + 把英文名作为
      子树行的第 4 个字段存下来做连接键，**39/39 全部对上**，全部有中文。
  - **D5 渲染断言 + 变异测试**：
    · 发现旧 harness 的天赋渲染是**假绿** —— 只 `renders++`、什么都不断言，
      而且 40 个专精只测第 1 个。现在**每个专精都画一遍**，渲染 83 → 123 次。
    · 新增一行输出：**40 个专精，129 棵树，节点 4304，点亮 3198，连线 6377，点亮 3990，
      点数徽章 412，英雄天赋名 60**。断言含 `树数 === 渲染次数 × 3`（照 `cov === slots` 的路子）、
      点亮率必须在 40%~95% 之间、节点名必须含中文、同一专精内**节点 id 不许重复**。
    · 变异测试 9 个，第一轮漏 1 个：「专精树画成职业树」—— 因为职业树被画两遍时
      节点总数依然合理。补的办法是**把 nodeId 写进 DOM**（`data-node`）并断言不重复，
      重跑 **9/9 全抓**。另外单独变异了英雄天赋中文名那两条，**2/2 全抓**。
  - **D6 README**：「天赋树画不出来」整节重写成「能画出来了，中文名来自暴雪自己的数据表」，
    含两个数据源的表格、连接验证数字、以及**仍然做不到的三件事**
    （导不出游戏能认的串、不能在面板里点着改、上游有 1+4 套脏数据）。
    另外修了「第三步不能省」（现在有四步）、目录结构、`tests.html` 那句「多两项检查」（现在四项）。
  - **发布包核对**：`tools/build-release.ps1 -SkipBuild` → **1630.9 KB / 505 条目**（+133 KB
    就是 `talent-tree.js` 压缩后的大小）。
    **发现并修掉一个真会出事的坑**：`Copy-Item -Recurse` 是按**磁盘**复制的，
    `.gitignore` 管不着它 —— 那 14 MB 的 `tools/.talent-raw/` 本来会被打进用户的包。
    而 `Remove-Item -Force` 不加 `-Recurse` 删不掉目录，所以光加进文件清单没用，
    另开了一个 `$dropDirsFromPkg` 清单。实测确认已排除，同时排掉了 271 KB 的中文名缓存。
  - 验收：`node tools\run-tests.js` **七行全绿** ——
    133 项测试（33/66/34）+ 装备渲染（123 次 / 7947 图标 / 占位块 0 / 轨道徽章 3601 / 部位组 1264）
    + 天赋树渲染（40 专精 / 129 棵树 / 4304 节点）+ 数据格式 2016 项 + 天赋树格式 76377 项。
    临时文件已清空，三个提交都在 `feat/bis-panel`，没有 push、没有 tag、没有 Release。

- **第 9 轮**：给天赋树补**几何断言** —— 这一轮补的是我自己的一个盲区：
  **这棵树我从来没有真正「看」过。** 之前 21 条断言全是「DOM 里有没有这个元素」，
  而树的位置完全由 CSS 绝对定位决定。方块两两叠在一起、或者跑到画布外面，
  上面每一条都照样打「通过」。桩不算布局，所以按 CSS 的数字自己算。
  - **三条新断言**（进 `tools/run-tests.js`，不是一次性脚本）：
    · **不重叠**：同一画布内任意两个方块的中心距不得同时在宽、高之内。实测 4304 个方块 **0 重叠**，最小间隙 12px。
    · **不溢出**：方块必须完整落在画布矩形内。实测 **0 溢出**。
    · **网格聚类契约**：上游 `posX` 相差 ≤ 100 的两个节点**必须**落在同一列，> 100 的**必须不**落在同一列（`posY` 同理）。实测 **0 违约**。
  - **方块尺寸读自 `app/style.css`**（正则抓 `.tnode` 的 `width` / `height`，读不到就抛异常），
    没有写死在测试里。写死的话改了 CSS 忘了改测试，几何检查会拿旧尺寸算出假的「不重叠」。
  - **变异测试 8/8**，其中两个变异是改 **CSS**（把方块改宽 / 改高）而不是改 JS ——
    那正是这组断言存在的理由，两个都被抓到。
  - **有一个变异第一次没抓到，逼出了上面第三条断言**：把聚类容差从 100 改成 0。
    它**既不重叠也不溢出**，因为相差 10 的近重复坐标各占一列后画布也跟着变宽 ——
    树被整体拉宽、节点错位，但前面每条断言都过。「最多 9 列 × 11 行」也抓不到它
    （不同 X 值本来就 ≤ 9 个）。真正能抓的是**把聚类规则本身写成断言**：
    拿渲染结果去对**上游坐标**。加上之后 8/8。
  - 顺带加了「最大 9 列 × 11 行」的上界断言，这个数和第 8 轮 `_box3.js` 独立量出来的一致。
  - 验收：`node tools\run-tests.js` 七行全绿，天赋树渲染那行现在还报
    「方块 4304（重叠 0，超出 0），方块尺寸 62×32（读自 style.css），最大 9 列 × 11 行，聚类越界 0」。
    提交 `f3a4869`，临时文件已清空。
  - **记一条教训**：变异测试漏掉的那一个比抓到的七个有用。抓到的只是确认断言活着；
    漏掉的那个指出了**断言的类别不够** —— 我只检查了「有没有摆坏」，
    没检查「摆的位置对不对」。前者是自洽性，后者要拿产物去对上游。

- **第 10 轮**（提交 `dd71380`）：**推翻了自己写在 README 里的一句话** —— 顺手查「面板能不能
  跟你自己的天赋比」，结果查出天赋导入串的编码解到了一半。
  - 起点：`data/data.js` 里**没有任何天赋字段**（`talent` / `loadout` / `traits` 全是 0 次，
    只有 `specID` 125 次），所以「跟自己的天赋比」在现有扫描数据上做不了。
    但 `WTF` 下 **`TalentLoadoutsEx.lua` / `BtWLoadouts.lua` 存的是官方导入串**，
    36 个文件里挖出 **54 个**去重后的串。
  - **串头 54/54 全解开**：8 位版本（全是 2）+ 16 位 specID + 128 位 `treeHash`，
    标准 base64、每字符内低位在前。解出的 `treeHash` 是**按职业树**的
    （防护/惩戒骑同一个，鲜血/邪恶骑同一个，浩劫/复仇 DH 同一个）。
  - **README 里错了一半的那句话**：原文说导出串要 `treeHash` 和「游戏权威的节点顺序」，
    而「公开数据里没有这两样」。实测 **`fullNodeOrder` 就在 raidbots 那份 `talents.json` 里**，
    而且**同一职业内所有专精的这份顺序完全相同**，正好等于该职业各专精节点集合的并集
    （13 个职业全查：死骑 206 = 114 + 92 + 0；德鲁伊差 2 个、圣骑士差 1 个，其余 0）。
    `treeHash` 确实没有 —— raidbots 每个专精只有 10 个字段，没有 hash。
  - **节点位流的布局是被数据挑出来的，不是我猜的**。18 种布局 × 54 个串扫了一遍，
    最好的是 `sel → purchased → (partial → 6 位点数) → (choice → 2 位下标)`，
    partial/choice **嵌在 purchased 里面**。位宽只能是 6，`--flat`（不嵌）**0/54**，
    还会造出 24~46 个「点数超上限」。
  - **两个假设被数据否掉**（都记下来，免得重走）：
    「布局不对」→ 18 种全试，干净的 0 个；「跟主流 hash 不一致的才失败」→ **事实相反**，
    一致的反而更差（5% vs 42%）。
  - **抓到自己用了一个假指标**。我一开始用「解码再编码，跟原串逐字节比」当证据，
    得到 28/54，还写进了 README。但那是**弱检查**：我读 n 位就写回 n 位，本质是逐位抄一遍，
    字段含义猜错也一致 —— 证据就是把点数位宽从 6 改成 4，一致率只从 28 掉到 27。
    换成能证伪的语义检查（买了别的专精的节点 / 点数超上限 / 二选一下标落在没有选项的节点上）后，
    诚实的数字是 **7/54**。README 和已知限制里的 28 都改成了 7。
  - 产物：`tools/decode-talent-string.js`（9 KB，`<串>` / `--scan` / `--verify` /
    `--flat` / `--rankbits`），中文名从 `app/talent-tree.js` 取，节点顺序从 `.talent-raw` 取，
    缺缓存时提示去跑 `fetch-talent-tree.js`。**`app/` 下没有一行代码用它，面板行为没变。**
  - README 新增一节「天赋导入串：解到一半」，并改了三处旧话（第 307 行的「两样都没有」、
    已知限制那条、目录结构）。新增「待用户决定」第 7 条 + E 任务组。
  - 验收：`node tools\run-tests.js` 七行全绿（数字与第 9 轮一致），临时文件已清空。
  - **记两条教训**：① 顺手查一件小事（「能不能跟自己的天赋比」）比按计划推进更值 ——
    它撞出了一句写在 README 里的错话。② **先怀疑自己的指标**。往返比对看着最严格
    （逐字节相等），其实几乎测不出东西；一个改了字段宽度还不变的指标，测的不是那个字段。

- **第 10 轮（续）**：继续攻 E5，又否掉两个假设，并把工具改成可复用。
  - `tools/decode-talent-string.js` 现在**可以 `require`**：导出 `decode` / `checkSemantics` /
    `loadOrder` / `loadTree` / `scanWtf` / `toBits` / `fromBits` / `setLayout`，
    CLI 只在直接运行时跑。之前它没有任何导出，任何调查都得复制一遍解码逻辑。
  - **先怀疑仪器（第 N 次，又对了一半）**：47/54 栽在「二选一下标放错」上，
    我先去核对 `def.entries` 这个字段名到底存不存在 —— 存在（鲜血死骑 102 个节点 1 项、
    11 个 2 项、1 个 3 项），所以那不是仪器错。但**这些出问题的节点全是
    `type=single entries=1`**，一个没有选项的节点上冒出「选了哪个」，只可能是位流错位了。
  - **新事实：没有一条串是从第 0 个节点就错的。** 每条都先正确解出前 4~42% 才崩，
    而且**同一专精的串，第一个违规下标完全相同**（复原萨满 8 条都是 35，
    复仇 DH 5 条都是 42，浩劫 DH 4 条都是 96）—— 跨不同 hash 也相同。
    内容相关的错位会随串变化，固定下标说明是**顺序表本身**在固定位置和游戏对不上。
  - **假设四（被否）**：「顺序差几个节点」。先做单串修复搜索（在第一个违规处试
    「跳过一个」/「多吃一个节点的位」，回溯 3 格，预算 4）：**7 → 12 条干净，
    修好的 5 条全是 1 个补丁、且补丁下标是该专精的那个固定值**。看着很像成立。
    但真正的检验是**一组补丁要能修好该专精的所有串**（补丁是专精的属性，不是串的属性）。
    按这个更强的约束重搜、预算 12：10 个专精里 **9 个耗尽预算仍然 0/N 干净**
    （惩戒圣骑打了 13 个补丁还是 0/8）。所以「差几个节点」是错的 ——
    单串能修只是补丁把后面的错误吸收掉了，不是找到了真顺序。
  - E5 仍然**没做完**，现在写进 README 和计划的是四条**已排除**的解释，
    留给下一轮的线头是「二选一下标跟着谁走」和「固定下标 35 / 42 / 96 处到底是什么节点」。
  - 验收：`node tools\run-tests.js` 七行全绿，临时文件已清空，
    提交 `b870def`（导出）+ 本轮文档提交。
  - **记一条教训**：一个假设「能修好部分样本」不算成立。补丁越自由，越能把错误吸收进去 ——
    要检验它，就得把它约束到它声称的那个层级（这里是「按专精」而不是「按串」）。
    单串搜索给的 12/54 是虚的，按专精搜索给的 1/10 才是真的。

- **第 11 轮**：无障碍。起因是想核对 README 里「鼠标悬停给中文名 / 二选一 / 层级要求 / 点数」
  这句话是不是真的（是真的，`data-tip` 16 处都在），顺手发现测试从来没检查过任何无障碍属性。
  - **先量，别先修**。写了一次性脚本量四件事，结果**两条是仪器在骗我**：
    · 「2386 个 `data-tip` 元素全都没有可见文字」→ 错，全都**有**。
    · 「621 张图标全都没有 `alt`」→ 错，`app/bis.js` 里是 `img.alt = ''`（**属性赋值**），
      而 `tools/dom-stub.js` 不把属性赋值反射进 `attrs`，所以量到的是桩的缺陷。
    如果照第一次的读数去「修」，就会给 2386 个已经有名字的元素硬塞 `aria-label`
    （反而违反 WCAG 的 Label in Name），再给装饰性图标写上没用的 alt 文字。
  - 修的是**仪器**：`tools/dom-stub.js` 给 `alt / title / src / id / type / value / href /
    width / height / tabIndex` 加了真正的属性↔属性反射（`Object.defineProperty`），
    和浏览器行为一致。加完全套测试仍然全绿，说明反射没改变既有断言的含义。
  - 量准之后真正的缺陷只有一个：**天赋树画布是一堆绝对定位 div 拼的图，没有 role 也没有说明**，
    读屏软件只会念出一串互不相干的天赋名。修法是 `role="group"` +
    中文 `aria-label`（例：「职业天赋 天赋树，44 个节点，已点 31 个，共 31 点」）。
    没用 `role="img"`，因为那会把子节点的名字全藏起来。
  - `tools/run-tests.js` 加**第 6 行「无障碍」**，四条断言 + 一个共用的 `checkA11y(n, label)`
    （挂在两个已有的 walk 上，不额外遍历）：图标必须有 `alt` 属性（`alt=""` 合法，
    **缺属性**不合法 —— 缺属性时读屏会去念文件名）、按钮必须有可见文字或 `aria-label`、
    **带 `data-tip` 的元素自己必须有可见文字**（tooltip 是补充信息，不是名字）、
    天赋树画布必须有 `role=group` + `aria-label`。
    实测：7947 图标 / 2859 按钮 / 26378 个 `data-tip` 元素 / 129 个画布，四项全过。
  - **变异测试 5/5**（`tools/_muta11y.js`，跑完删）。其中一个第一次报「漏了」，
    查下去是**我的变异锚点写错了**（凭记忆写了一行 `bis.js` 里不存在的代码），
    脚本因此打了「跳过（找不到锚点）」—— 改成源码里的真实那一行后 5/5。
  - 顺带修了 `app/tip.js` 头注释里两句错话：说有「31 处 title」（实际 48 处），
    说「加上面板」（面板一处都不用 `title`，全走 `data-tip`）。
  - 验收：`node tools\run-tests.js` **八行全绿**，末行改成
    「全部通过：133 项测试 + 装备渲染 + 天赋树渲染 + 无障碍 + 两项格式校验」。
    提交 `8ede310`，临时文件已清空。
  - **记一条教训**：无障碍最容易出的错不是漏改，是**照着坏读数瞎改** ——
    给本来就有名字的元素加 `aria-label` 会让读屏体验变差。所以顺序永远是
    「先确认仪器 → 再量 → 再改」，这一轮四个读数里有两个是假的。
