# Everything under data/ is generated by tools/scan.ps1 on every run.
#
# It is also PERSONAL: it embeds your character names, realms, guilds, gold,
# Battle.net account folder names and absolute filesystem paths, plus your
# MySlot strings. Do not commit it.
data/

# Build artifacts of tools/build-launcher.ps1.
*.exe
tools/launcher.ico
WowAltBoard-v*.zip
AlterEgoWeb-v*.zip

# 临时脚本 / 临时文本（_gen_*.js、_msg.txt、_scan.json 那类）。
# 规矩是「跑完即删」，但我自己在 74a849a 里就手滑把 _msg.txt 提交进去了 ——
# 靠记性不如靠这条规则。
_*

# 变异测试的互斥锁（tools/mutate-lock.js 占/ 放）。跑挂了会残留，删掉即可。
tools/.mutating.lock

# 天赋树的上游原始文件缓存（raidbots talents.json 3.1 MB + 暴雪 DB2 两张表 10.6 MB）。
# tools/fetch-talent-tree.js 下到这里，只为了避免反复下载；产出物 app/talent-tree.js
# 才是要提交的东西。14 MB 的上游数据不该进仓库。
# 注意和 tools/.icon-cache.json 的区别：那个是**连接后的结果**（几十 KB），要提交。
tools/.talent-raw/

# 职业 / 专精中文名的上游原始文件缓存（暴雪 DB2 的 ChrClasses + ChrSpecialization，
# 两张表加起来只有 15 KB）。按 .talent-raw/ 的同一条道理不进仓库：**产出物**
# app/class-names.js（2.2 KB）才是要提交的东西，上游原始 dump 随时能重下。
tools/.db2-names/

# raider.io 的原始下载缓存（排行榜 runs.json + 每个角色一份 profile，共约 2 MB）。
# tools/fetch-talent-truth.js 和后面的 fetch-rio.js 下到这里。不进仓库有两条理由：
#   1. 和上面两个缓存同一条道理 —— 产出物（tools/talent-truth.json 38 KB）才该提交；
#   2. 这些 profile 里带**真实角色名和服务器名**。虽然它们本来就是 raider.io 的
#      公开数据，但产出物已经刻意剥掉了角色名，没理由再把原始 dump 放进仓库。
tools/.rio-raw/

# maxroll 指南页的原始 HTML 缓存（每篇 600~750 KB，70 篇装备指南约 45 MB）。
# tools/fetch-maxroll.js 下到这里，按 slug 命名，存在就跳过，所以中断能续。
# 和上面三个缓存同一条道理：**产出物** app/maxroll-data.js 才是要提交的东西，
# 上游 HTML 随时能重下，而且它带 maxroll 的完整页面内容（人家的编辑作品），
# 更没有理由放进仓库。
tools/.maxroll-raw/

# Editor / OS noise
.vscode/
.idea/
Thumbs.db
desktop.ini
*.tmp
